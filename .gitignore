# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnpm-debug.log*
.DS_Store

# Diagnostic reports (https://nodejs.org/api/report.html)
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage
*.lcov

# nyc test coverage
.nyc_output

# Grunt intermediate storage (https://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# Bower dependency directory (https://bower.io/)
bower_components

# node-waf configuration
.lock-wscript

# Compiled binary addons (https://nodejs.org/api/addons.html)
build/Release

# Dependency directories
node_modules/
jspm_packages/

# Snowpack dependency directory (https://snowpack.dev/)
web_modules/

# TypeScript cache
*.tsbuildinfo

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional stylelint cache
.stylelintcache

# Microbundle cache
.rpt2_cache/
.rts2_cache_cjs/
.rts2_cache_es/
.rts2_cache_umd/

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# parcel-bundler cache (https://parceljs.org/)
.cache
.parcel-cache

# Next.js build output
.next

# Nuxt.js build / generate output
.nuxt
dist

# Foundry build artifacts (ABI JSON) are stored in `packages/contracts/out/`. Only keep those tracked files; ignore every other `out/` directory.
out/
**/out/
!packages/contracts/out/
!packages/contracts/out/**

# Vite dev build output
dev-dist

# Gatsby files
.cache/
# Comment in the public line in if your project uses Gatsby and not Next.js
# https://nextjs.org/blog/next-9-1#public-directory-support
# public

# vuepress build output
.vuepress/dist

# vuepress v2.x temp and cache directory
.temp
.cache

# Docusaurus cache and generated files
.docusaurus

# Serverless directories
.serverless/

# FuseBox cache
.fusebox/

# DynamoDB Local files
.dynamodb/

# TernJS port file
.tern-port

# Stores VSCode versions used for testing VSCode extensions
.vscode-test

# yarn v2
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz
.pnp.*
.vercel

# Playwright test results
test-results/
playwright-report/
tests/test-results/
tests/playwright-report/

# Lighthouse CI results
.lighthouseci/
lighthouse-results/

# ================================
# Package Manager Caches
# ================================
# Bun binary lockfile (optional - some teams commit this)
# bun.lockb

# Bun cache directories
.bun/
*.bunx

# pnpm store (content-addressable package cache)
.pnpm-store/

# npm/yarn caches (in case they appear locally)
.npm/
.yarn/cache/

# ================================
# Devcontainer / Docker
# ================================
# Docker volume data (these are mounted, not in repo)
# Note: node_modules/ already covered above

# Docker Compose override (local customizations)
docker-compose.override.yml
docker-compose.local.yml

# Container-specific env files
.env.docker
.env.container

# ================================
# IDE / Editor - Local Settings
# ================================
# VSCode local/user settings (workspace settings are OK to commit)
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
!.vscode/*.code-workspace
*.code-workspace.local

# JetBrains IDEs
.idea/
*.iml
*.iws
*.ipr

# Vim
*.swp
*.swo
*~

# ================================
# Claude Code
# ================================
# Local Claude settings (user-specific, not shared)
.claude/settings.local.json

# Claude conversation/session data
.claude/projects/
.claude/todos/
.claude/statsig/
.claude/*.db

# Keep shared Claude settings
!.claude/settings.json
!.claude/commands/

# ================================
# OS Generated
# ================================
# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Linux
*~
.directory

# ================================
# Monorepo / Workspace
# ================================
# Turborepo
.turbo/

# Package manager workspaces (already have node_modules)
# Nx
.nx/

# Build artifacts in packages (dist already covered)
packages/*/dist/
packages/*/.cache/

# Package-specific generated files
# Note: packages/indexer/generated/ has its own .gitignore for ReScript artifacts
packages/contracts/cache/
packages/contracts/broadcast/

# ================================
# Secrets & Credentials
# ================================
# Private keys (NEVER commit these)
*.pem
*.key
*.p12
*.pfx
id_rsa*
id_ed25519*

# Wallet files
keystore/
*.keystore

# API keys file
secrets.json
credentials.json
