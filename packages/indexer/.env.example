# ==========================================
# INDEXER PACKAGE ENVIRONMENT VARIABLES 🔍
# ==========================================
# 
# 🚀 RECOMMENDED: Use the root .env file for full development
#    cp ../../.env.example ../../.env
#    # Edit ../../.env with your values
#    npm run dev (from root)
#
# 🔧 ISOLATION: For indexer-only development, use variables below
#    cp ../../.env.example .env
#    # Edit .env to keep only INDEXER variables
#    npm run dev (from this directory)

# ==========================================
# REQUIRED FOR INDEXER 🔍
# ==========================================
# To create or update a token visit https://envio.dev/app/api-tokens
ENVIO_API_TOKEN="your-envio-api-token"             # 🎫 Envio indexer service token

# ==========================================
# 📚 INDEXER DEVELOPMENT GUIDE
# ==========================================
#
# FULL STACK (Recommended):
#   cd ../../ && cp .env.example .env && pnpm run dev
#
# INDEXER ONLY:
#   cp ../../.env.example .env
#   pnpm run dev
#
# SETUP STEPS:
#   1. Create account at https://envio.dev
#   2. Generate API token at https://envio.dev/app/api-tokens
#   3. Configure schema.graphql with your contract events
#   4. Deploy indexer to Envio cloud
#
# DEVELOPMENT COMMANDS:
#   pnpm run dev                      # Start local indexer
#   envio dev                        # Alternative local development
#   envio deploy                     # Deploy to cloud
#   pnpm run build                    # Build indexer
#
# TESTING:
#   pnpm run test                     # Run indexer tests
#   # Test GraphQL endpoint at http://localhost:8080/v1/graphql
#
# The indexer provides:
#   - Real-time blockchain event indexing
#   - GraphQL API for querying data
#   - Garden, Action, and Work data
#   - EAS attestation processing
#
# GraphQL Endpoint: http://localhost:8080/v1/graphql
# GraphQL Playground: http://localhost:8080
