/** Only the content region scrolls; sheet titles and tab rails stay fixed. */
export const PWA_SHEET_SCROLL_CLASSNAME = "min-h-0 flex-1 overflow-y-auto";

/** Optically center a tab state above the sheet body's mathematical midpoint. */
export const PWA_SHEET_STATE_CLASSNAME = "flex min-h-full flex-col justify-center gap-4 p-4 pb-32";

/** Let sheet empty states place their icon and title independently of copy length. */
export const PWA_SHEET_FOCAL_STATE_CLASSNAME = "flex min-h-full flex-col gap-4 p-4";
