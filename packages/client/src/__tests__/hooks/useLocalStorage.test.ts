/**
 * useLocalStorage Hook Tests
 *
 * NOTE: These tests were testing localStorage API directly, not the useLocalStorage hook.
 * TODO: Rewrite to actually test the hook behavior with renderHook()
 */

import { describe, it } from "vitest";

describe.skip("useLocalStorage Hook - needs rewrite", () => {
  it.todo("should store and retrieve values");
  it.todo("should handle JSON serialization");
  it.todo("should sync across components");
  it.todo("should handle storage events");
});
