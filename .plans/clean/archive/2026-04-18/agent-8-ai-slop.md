# Agent 8 — AI Slop, Stubs, and Unhelpful Comments (DRY-RUN, re-run)

**Scope:** `packages/{admin,shared,client,contracts}/src` + `packages/contracts/script`. `agent/` and `indexer/` excluded. `__tests__/`, `*.stories.tsx`, vendored `packages/contracts/lib/**` excluded throughout.

## 1. Executive Summary

The codebase stays clean on the hard-pattern anti-patterns: no `debugger;`, no `generated by Claude/GPT/Copilot` markers, no stubs thrown in production paths (the `throw new Error("... not implemented")` hits are deliberate xstate placeholder actors supplied via `.provide({ actors })` at the call site). Two `console.debug` calls in `AppSettings.tsx` violate Rule 12.

Real slop is concentrated in three places:

1. **WHAT-style narrator comments** in client/admin **views** and the xstate/provider layer of `shared/`. ~235 hits across in-scope packages follow the `// Handle / Reset / Clear / Render / Filter / Get / Build / Convert / Sort / Process / Create / Return / Check / Update / Setup / Fetch / Load <identifier>` pattern directly above a line that already says it.
2. **Redundant JSDoc** in `packages/shared/src/utils/{eas,blockchain,action,compression}.ts` — `@param x - <restatement of parameter name>` on 8 `EAS` helpers, 7 address helpers, 4 chain-registry helpers, 5 action-parser helpers, 2 compression helpers. `shared/src/utils` alone has 257 `@param|@returns|@example|@module|@see` tags. `admin/` and `client/` have 0 `@param`/`@returns` (good — all JSDoc slop is in shared).
3. **`contracts/script/`** has its own dialect of slop: five "Extracted from deploy.js" history docstrings, one "Refactored from monolithic deploy.js", 82 WHAT-narrator comments, 52 `@param` tags on obvious internal deploy methods. These are not in `src/` but are TS production code and fit the scope.

Raw counts (excluding tests/mocks/stories):
- WHAT-narrator hits: **client 64 · admin 40 · shared 235 (providers+workflows+stores 45, hooks 113, modules 79, utils 31, other 28ish) · contracts/script 82** — total ~421.
- `@param`/`@returns` JSDoc: **admin 0 · client 0 · shared 441 · contracts/script 52**.
- `console.*` in prod src (non-logger, non-tests, non-JSDoc @example): **2** (both `AppSettings.tsx`).
- `debugger;`: **0**.
- AI-generation markers: **0**.
- Raw history comments: **7** (5 "Extracted from deploy.js", 1 "Refactored from monolithic", 1 "Consolidates … previously duplicated across").
- Stubs in production paths: **0** (all 6 `not implemented` throws are xstate placeholder actors).

**Estimated removable LOC (comment-only, no executable change):**
- WHAT-narrator sweep (admin views + client views + shared providers/workflows/stores only — leave hooks/modules/utils judgment-based): **~150 lines**
- Redundant JSDoc trim in `shared/src/utils/{eas,blockchain,action,compression}.ts`: **~160 lines**
- Console.debug → logger.debug swap in `AppSettings.tsx`: **2 lines swapped** (no net LOC change)
- History docstrings in `contracts/script/`: **~30 lines** (7 blocks avg 4 lines)
- Aspirational commented-out code in `shared/src/modules/transactions/embedded-sender.ts:52–62`: **10 lines**
- Inner-function dash dividers in `AdminTextField.tsx` (8 × 3 lines = 24) + `AdminListItem.tsx` (4 × 3 lines = 12): **~36 lines**
- Legacy-section header in `shared/src/hooks/auth/useAuth.ts:142–144` around a single re-export: **2 lines**

**Total budget: ~390 LOC** deletable without touching runtime behavior. No dead stubs. No unused imports found (tsc would flag them; clean).

## 2. HIGH-CONFIDENCE REMOVE — Debug Leftovers

The only two instances in admin/shared/client production code. Both already gated by `import.meta.env.DEV`, but CLAUDE.md Rule 12 says use `logger`. The file already uses `logger.debug` on line 190 — these two are outliers.

- `packages/client/src/views/Profile/AppSettings.tsx:182` — `if (import.meta.env.DEV) console.debug("[AppRefresh] localStorage clear failed:", e);`
- `packages/client/src/views/Profile/AppSettings.tsx:187` — `if (import.meta.env.DEV) console.debug("[AppRefresh] IndexedDB clear failed:", e);`

Fix: drop the env guard and call `logger.debug("[AppRefresh] localStorage clear failed:", { error: e });` (logger has internal level gating).

### Not leftovers (KEEP)
- `packages/shared/src/workflows/authActor.ts:16`, `packages/shared/src/utils/app/haptics.ts:178,186`, `packages/shared/src/utils/scheduler.ts:168`, `packages/shared/src/utils/storage/quota.ts:80`, `packages/shared/src/utils/action/parsers.ts:40`, `packages/shared/src/hooks/utils/useAsyncEffect.ts:146`, `packages/shared/src/hooks/utils/useEventListener.ts:49`, `packages/shared/src/hooks/blockchain/useTransactionSender.ts:21` — all inside JSDoc `@example` blocks demonstrating consumer usage (documentation, not runtime).
- `packages/contracts/script/**` — CLI deploy tooling; stdout IS the product. 500+ console calls and all legitimate.

## 3. HIGH-CONFIDENCE REMOVE — History Comments

Seven total. All in `contracts/script/` except one that straddles history/WHY.

**`contracts/script/` — "Extracted from" / "Refactored from" (no longer informative once the migration is done):**
- `packages/contracts/script/deploy.ts:3–18` — "deploy.js - Legacy entry point ... All logic has been refactored into: - deploy/cli.js ... - utils/network.js ...". The file IS a delegating wrapper; the current delegation can be documented in 1 line: `"Backward-compat shim that delegates to ./deploy/cli.ts."`.
- `packages/contracts/script/deploy/cli.ts:14–21` — "DeploymentCLI ... Refactored from monolithic deploy.js into modular structure." Trim the second sentence; keep "Main CLI; injects shared NetworkManager/AnvilManager/DeploymentAddresses once".
- `packages/contracts/script/deploy/gardens.ts:10–14` — "GardenDeployer ... Extracted from deploy.js - handles deployment of individual gardens". Drop the "Extracted from" sentence.
- `packages/contracts/script/deploy/core.ts:14` — "Extracted from deploy.js - handles deployment of core protocol contracts". Drop.
- `packages/contracts/script/deploy/actions.ts:22` — "Extracted from deploy.js - handles deployment of actions from config". Drop.
- `packages/contracts/script/deploy/anvil.ts:8` — "Extracted from deploy.js to handle Anvil process management." Drop.
- `packages/contracts/script/utils/validation.ts:4` — "Extracted from deploy.js to provide reusable validation". Drop.
- `packages/contracts/script/utils/network.ts:64–66` — "Consolidates network configuration handling that was previously duplicated across deploy.js, garden-manager.js, and action-manager.js". This is useful for one line as WHY ("single source of truth for RPC URL resolution and chain-id normalization across all deploy subcommands"); delete the "previously duplicated" framing.

**`shared/` borderline:**
- `packages/shared/src/hooks/app/useScrollToTop.ts:10–11` — "This replaces scroll logic that was previously in useNavigateToTop, which caused a visible flash because the old page scrolled to top before the new page rendered." The "caused a visible flash" is legit WHY. Trim to: `"Runs in useLayoutEffect so the reset happens before paint — prevents the flash that occurs when the outgoing page scrolls to 0 before the incoming page mounts."`
- `packages/shared/src/hooks/work/usePlatformStats.ts:96–99` — "Previously `enabled: gardenAddresses.length > 0` caused data loss ..." **KEEP as-is**. The "Previously" is load-bearing because it warns the next editor not to reintroduce the gate. Textbook WHY comment.
- `packages/shared/src/hooks/app/useInstallGuidance.ts:239` — `// Was previously installed - show "Open App" as primary`. "Was previously installed" describes *user state*, not code history. Reword to remove ambiguity: `// User has already installed the PWA — primary CTA is "Open App", not "Install".`

## 4. HIGH-CONFIDENCE REMOVE — WHAT-Comments (Sample top 30 of ~235)

Each is a single line that restates the identifier or operation below. Pure narrator ceremony.

### `packages/client/src/views/` (sample)
1. `packages/client/src/views/Home/WorkDashboard/Drafts.tsx:52` — `// Handle resume draft` above `const handleResume = ...`
2. `packages/client/src/views/Home/WorkDashboard/Drafts.tsx:57` — `// Handle delete draft`
3. `packages/client/src/views/Home/WorkDashboard/Drafts.tsx:88` — `// Render loading state`
4. `packages/client/src/views/Home/WorkDashboard/Drafts.tsx:112` — `// Render empty state`
5. `packages/client/src/views/Home/WorkDashboard/Drafts.tsx:154` — `// Render drafts list`
6. `packages/client/src/views/Home/index.tsx:46` — `// Filter state` above `const [filters, setFilters]`
7. `packages/client/src/views/Home/index.tsx:85` — `// Reset loading state when navigating back to home` above an effect that literally calls `resetLoadingState()`
8. `packages/client/src/views/Home/Garden/Work.tsx:119` — `// Handle individual work retry`
9. `packages/client/src/views/Garden/Details.tsx:50` — `// Handle location toggle`
10. `packages/client/src/views/Garden/Intro.tsx:90` — `// Filter actions by effective domain`
11. `packages/client/src/views/Garden/Intro.tsx:94` — `// Filter gardens by selected domain` (same file, adjacent block)
12. `packages/client/src/views/Garden/index.tsx:323` — `// Create fallback objects for review step`
13. `packages/client/src/views/Login/index.tsx:181` — `// Handle auth errors`
14. `packages/client/src/views/Login/index.tsx:271` — `// Render logic`
15. `packages/client/src/views/Profile/index.tsx:46` — `// Get user-set username (only if not auto-generated)` (borderline — "only if not auto-generated" is the useful bit; collapse)

### `packages/admin/src/views/` & `components/`
16. `packages/admin/src/components/Hypercerts/HypercertWizard/useWizardData.ts:107` — `// Handle browser refresh/close with beforeunload`
17. `packages/admin/src/components/Hypercerts/HypercertWizard/useWizardData.ts:115` — `// Handle blocker state - show confirmation dialog`
18. `packages/admin/src/components/Hypercerts/HypercertWizard/useWizardData.ts:364` — `// Handle clicking on completed steps in the step indicator`
19. `packages/admin/src/components/Hypercerts/Steps/MetadataEditor.tsx:30` — `// Convert seconds to milliseconds for Date`
20. `packages/admin/src/components/Hypercerts/Steps/MetadataEditor.tsx:113` — `// Filter out scopes already added`
21. `packages/admin/src/components/Layout/CommandPalette.tsx:360` — `// Reset state when dialog opens/closes`
22. `packages/admin/src/components/Layout/CommandPalette.tsx:431` — `// Reset active index on debounced query change`
23. `packages/admin/src/views/Hub/components/CookieJarDepositModal.tsx:44` — `// Reset form state when modal opens/closes`
24. `packages/admin/src/views/Hub/components/CookieJarDepositModal.tsx:79` — `// Clear mutation error when inputs change`
25. `packages/admin/src/views/Hub/components/CookieJarWithdrawModal.tsx:43,73` — same two patterns
26. `packages/admin/src/views/Hub/components/CookieJarPayoutPanel.tsx:41` — `// Return null when module not configured, still loading, or no jars exist`
27. `packages/admin/src/views/Hub/components/WorkCard.tsx:22` — `// Transform EASWork to WorkCardData for the shared component`
28. `packages/admin/src/views/Garden/CreateAssessment.tsx:337` — `// Reset validation display when step changes`
29. `packages/admin/src/views/Garden/WorkDetail/helpers.tsx:104` — `// Render generic details`

### `packages/shared/src/`
30. `packages/shared/src/providers/Auth.tsx:436–470` — a chain of nine `// Clear X` comments describing the straightforward teardown: `// Clear from machine`, `// Clear auth mode`, `// Clear query cache`, `// Clear SW caches`, `// Clear stored credential`. Some (e.g., "Clear auth mode ... but KEEP credential for future passkey login / Clearing it would force the user to create a new account") are WHY and must survive. Plain `// Clear X` lines go. Strip ~5 of the 9.

(Another 200+ hits across the four packages follow the same template; the 30 above are representative.)

**Pattern templates to sed-sweep, human-review per file:**
```
^\s*//\s*Handle\s+\w+\s*$
^\s*//\s*Reset\s+\w+(\s+when\s+[\w\s]+)?\s*$   # but NOT "Reset ... / see X" or multi-clause
^\s*//\s*Clear\s+\w+\s*$
^\s*//\s*Render\s+\w+\s*$
^\s*//\s*Build\s+a?\s+\w+\s+for\s+\w+\s*$
^\s*//\s*Filter\s+\w+\s+by\s+\w+\s*$
^\s*//\s*Convert\s+\w+\s+to\s+\w+\s*$
^\s*//\s*Map\s+\w+\s+(by|to)\s+\w+\s+for\s+(quick|O\(1\))\s+lookup\s*$
^\s*//\s*Process\s+registered\s+events\s*$       # specific call-out
^\s*//\s*Return\s+cleanup\s+function\s*$
```
Hand-review required: "Filter X" lines that identify a non-obvious domain (e.g., "gardens without a domainMask (0 or undefined) pass through all filters" — keeps the WHY). When in doubt, preserve.

## 5. HIGH-CONFIDENCE REMOVE — AI-Generation Markers

**None found.** `grep -rniE "generated by (claude|gpt|copilot|ai|chatgpt|anthropic)"` returns zero hits in prod, test, deploy scripts, or `contracts/src`.

## 6. HIGH-CONFIDENCE REMOVE — Redundant JSDoc (Top 20)

All in `packages/shared/src/utils/` — `admin/` and `client/` have zero `@param`/`@returns` JSDoc. Each entry: file:line — symbol — why redundant. Function bodies stay; only the multi-line `/** ... */` block shrinks to one line or disappears.

1. `packages/shared/src/utils/eas/explorers.ts:8–12` — `getEASExplorerUrl` — `@param chainId - The chain ID where the attestation exists` / `@param attestationId - The attestation UID (0x prefixed hex string)` / `@returns The complete EAS explorer URL`. Identifier + types already tell you this. Keep only the inline mainnet-vs-subdomain split on line 18.
2. `packages/shared/src/utils/eas/explorers.ts:26–29` — `openEASExplorer`. 3-line function; drop block.
3. `packages/shared/src/utils/eas/explorers.ts:36–39` — `isValidAttestationId`. One-line regex; drop block.
4. `packages/shared/src/utils/eas/explorers.ts:45–49` — `getBlockExplorerTxUrl`. 2-line wrapper; drop.
5. `packages/shared/src/utils/eas/explorers.ts:56–59` — `openBlockExplorerTx`. Same pattern.
6. `packages/shared/src/utils/eas/explorers.ts:66–70` — `getBlockExplorerAddressUrl`. Same.
7. `packages/shared/src/utils/eas/explorers.ts:77–81` — `getBlockExplorerTokenUrl`. Same.
8. `packages/shared/src/utils/blockchain/address.ts:7–13` — `compareAddresses` — `@param a - First address` / `@param b - Second address`. Keep only `@example` (lowercase-vs-uppercase demo is the teaching bit).
9. `packages/shared/src/utils/blockchain/address.ts:25–34` — `isUserAddress` — 2-line wrapper over `compareAddresses`.
10. `packages/shared/src/utils/blockchain/address.ts:43–51` — `isAddressInList`. Keep `@example`, drop rest.
11. `packages/shared/src/utils/blockchain/address.ts:62–72` — `truncateAddress`. Keep `@example`, drop `@param`/`@returns`.
12. `packages/shared/src/utils/blockchain/address.ts:84–96` — `normalizeAddress`. The 2-overload shape is worth a short WHY (preserves Address type). Drop the `@param/@returns` but keep that 1-line note.
13. `packages/shared/src/utils/blockchain/address.ts:105–109` — `isValidAddressFormat`. One-line regex; drop block.
14. `packages/shared/src/utils/blockchain/chain-registry.ts:83–88` — `getChainConfig` — `@param chainId - The chain ID to look up`. Drop.
15. `packages/shared/src/utils/blockchain/chain-registry.ts:93–98` — `getNetworkName`. Same.
16. `packages/shared/src/utils/blockchain/chain-registry.ts:103–108` — `getEASName`. Same.
17. `packages/shared/src/utils/blockchain/chain-registry.ts:113–118` — `getBlockExplorer`. Same.
18. `packages/shared/src/utils/action/parsers.ts:12–21` — `parseActionUID`. Keep only `@example` showing the `"chainId-uid"` format.
19. `packages/shared/src/utils/action/parsers.ts:30–42` — `findActionByUID`. `@example` is a 3-line `console.log(action.title)` demo; drop entire block.
20. `packages/shared/src/utils/action/parsers.ts:48–55` — `getActionTitle`. Drop.

**Plus** (bonus, still high-confidence):
- `packages/shared/src/utils/action/parsers.ts:65–71` — `buildActionId`. One-line string concat; drop block.
- `packages/shared/src/utils/eas/transaction-builder.ts:16–18` — `AttestationRequest` interface — docstring `"EAS attestation request structure (internal)"` is redundant with typed fields.
- `packages/shared/src/utils/eas/transaction-builder.ts:31–33` — `buildAttestationRequest` — docstring `"Build an EAS attestation request"`. Drop.
- `packages/shared/src/utils/eas/transaction-builder.ts:52–54` — `buildAttestTxParams` — docstring `"Build transaction parameters for an EAS attestation (internal helper)"`. Drop.
- `packages/shared/src/utils/compression.ts:13–15` — `isCompressionSupported` — `"Check if native compression streams are supported"`. Identifier = doc.
- `packages/shared/src/utils/compression.ts:20–26` — `combineChunks` — `@param chunks`, `@returns Single combined Uint8Array`. Drop block.

`contracts/script/` — 52 `@param` tags. Top redundancies:
- `packages/contracts/script/deploy/gardens.ts:28–32, 81–86, 155–158, 164–168` — `deployGarden`, `executeForgeScript`, `setEnvFlags`, `saveGardenRecord` — all `@param configPath - Path to garden config JSON` style. Function names already say it. Collapse to 1 line each or drop.

### Not redundant (KEEP)
- `packages/shared/src/modules/job-queue/index.ts:25–42` (`createOfflineTxHash`) — 4-bullet contract with "never submit to RPC" warning. Gold-standard.
- `packages/shared/src/workflows/authMachine.ts:1–36` — state-machine overview; right place for a top-of-file doc.
- `packages/shared/src/utils/storage/quota.ts:1–12` — Storage API rationale.
- `packages/shared/src/utils/scheduler.ts:1–12` + per-function `@example` — public-API ergonomics.
- `packages/shared/src/utils/time.ts:1–19` — Temporal-API rationale.
- `packages/shared/src/utils/errors/contract-errors.ts:1–8, 23–30` — operational context (how to compute selectors via `cast sig`, last-updated date).
- `packages/shared/src/utils/eas/transaction-builder.ts:127–136` (`buildBatchApprovalAttestTx`) and :171–181 (`buildBatchWorkAttestTx`) — explain the UX/gas rationale for batching (not just parameter restatement). Keep.

## 7. Stubs + NotImplementedError Sites (all deliberate)

All six `throw new Error("... not implemented")` sites are xstate placeholder actors overridden via `.provide({ actors: { ... } })` at consumer sites. They are **not** dead stubs and do NOT need fixing.

- `packages/shared/src/workflows/mintHypercert.ts:209` — `uploadMetadata actor not implemented`
- `packages/shared/src/workflows/mintHypercert.ts:212` — `uploadAllowlist actor not implemented`
- `packages/shared/src/workflows/mintHypercert.ts:215` — `buildAndSignUserOp actor not implemented`
- `packages/shared/src/workflows/mintHypercert.ts:218` — `pollForReceipt actor not implemented`
- `packages/shared/src/workflows/mintHypercert.ts:224` — `registerInSignalPool actor not implemented`
- `packages/shared/src/workflows/createGarden.ts:133` — `submitGarden actor not implemented` (comment on line 131 already explains: "Placeholder actor with proper typing — actual implementation provided when machine is used")

Optional nit (refactor, not cleanup): a single helper `notProvided(name: string)` could deduplicate the 5 identical error strings in `mintHypercert.ts`.

Other `throw new Error` hits in scope (e.g., `Unsupported chain ID for Pimlico`, `Unsupported job kind: ${job.kind}`, `Approvals array must not be empty`) are valid exhaustiveness guards or input validations. Keep.

No active `TODO: implement` on live code paths. TODOs found are all concrete, scoped, and tied to external triggers (see §9).

## 8. Wrapper Forwarders / Over-Engineered Abstractions

**None found** that qualify as slop.

- `packages/shared/src/utils/action/parsers.ts:72–74` — `buildActionId` is a 1-line `${chainId}-${actionUID}` concat. Legitimate: gives the concept a name (anti-stringly-typed). Keep.
- `packages/shared/src/utils/blockchain/address.ts` has seven address helpers (`compareAddresses`, `isUserAddress`, `isAddressInList`, `truncateAddress`, `normalizeAddress`, `isValidAddressFormat`, `isZeroAddress`). Grown by accretion but each has distinct semantics (case, overload, nullable, zero-addr check). The code is fine; only the JSDoc is the target (see §6).
- `packages/shared/src/modules/transactions/{passkey,embedded,wallet}-sender.ts` — three concrete `TransactionSender` impls behind one interface. Justified: each auth mode has genuinely different sending semantics (bundler, wagmi write, EIP-5792 preview). Not over-abstraction.
- `packages/shared/src/workflows/*.ts` — xstate setups with generic actor types. Required by xstate typing; not over-abstraction.

No abstract classes. No 3-level class hierarchies. No generics-with-single-concrete-use found in sampled files.

## 9. MEDIUM (Judgment Calls)

**Aspirational commented-out code (DELETE the commented block, KEEP the TODO):**
- `packages/shared/src/modules/transactions/embedded-sender.ts:52–62` — 10 lines of commented-out EIP-5792 `sendCalls` + `getCallsStatus` usage. This is the "future code as comment" anti-pattern. Collapse to: `// TODO: Replace with EIP-5792 sendCalls + paymasterService once @wagmi/core/experimental is stable.`
- `packages/shared/src/modules/transactions/embedded-sender.ts:93–94` — TODO `"Use EIP-5792 sendCalls for atomic batching when available."` — 2 lines; fine as-is.
- `packages/shared/src/modules/transactions/wallet-sender.ts:68` — similar one-liner TODO. Keep.

**Concrete TODOs with triggers (KEEP):**
- `packages/shared/src/modules/transactions/passkey-sender.ts:57` — "When permissionless supports sendUserOperation with multiple calls, use that for atomic batching." Scoped to an upstream lib release.
- `packages/client/src/views/Home/GardenList.tsx:138` — "Virtualize with @tanstack/react-virtual when gardens.length > 50". Concrete threshold.
- `packages/shared/src/hooks/gardener/useGardenerProfile.ts:128` — "Replace with GraphQL query once indexer supports gardener profiles". Tied to indexer roadmap.
- `packages/shared/src/providers/Auth.tsx:230` — "Verify connector.id against actual AppKit version — may also be 'appKitAuth' or contain 'embedded' depending on @reown/appkit version". Precise, actionable uncertainty.

**Legacy comments — keep or reword?**
- `packages/shared/src/providers/Work.tsx:110` — `// Legacy context for backward compatibility` on exported `WorkContext`. Live API; keep but tighten: `// Combined context retained for consumers of useWork().`
- `packages/shared/src/providers/Work.tsx:362` — `// Legacy combined value for backward compatibility`. Same situation, reword.
- `packages/shared/src/hooks/auth/useAuth.ts:142–144` — `// ============================================================================` + `// LEGACY EXPORTS (kept for backwards compatibility)` + `// ====` around a single `AuthMode` re-export. Three lines of ceremony for a one-line re-export. Collapse to: `// Re-exported for backwards compatibility with old import sites.`
- `packages/shared/src/utils/styles/theme.ts:89`, `packages/shared/src/hooks/app/useTheme.ts:59` — `// Legacy browsers`. Both label the `matchMedia.addListener` fallback branch. Keep.
- `packages/shared/src/utils/eas/encoders.ts:360` — `// Legacy v1 format for backward compatibility during transition`. Labels the smaller branch of a two-format ternary. Keep.
- `packages/shared/src/utils/errors/contract-errors.ts:183` — `// Legacy selector (NotGardenerAccount) - kept for backward compatibility`. Required to keep old attestations decoding. Keep.
- `packages/shared/src/utils/storage/file-serialization.ts:76` — `// Legacy format: direct File object (may fail on iOS when reading)`. Labels a branch. Keep.

**Inner-function dash dividers (admin components):**
- `packages/admin/src/components/AdminTextField.tsx:98–100, 116–118, 136–138, 299–301` — four 3-line `// ------- Shared handlers --------` / `// ------- Filled variant --------` blocks inside a 400+-line component. The file-level `// =======` banners at the top are fine (2 of them); the inner ones are slop. Better solution is to split the component, but for now: delete the dash blocks (12 lines).
- `packages/admin/src/components/AdminListItem.tsx:88–90, 114–116, 140–142, 173–175` — same pattern, 4 blocks (12 lines). Delete.

**Do NOT touch (section-divider infrastructure in multi-export files):**
- `packages/shared/src/utils/index.ts` — ~20 `// ====== CATEGORY ======` blocks. Navigation aid in a re-export barrel. Keep.
- `packages/shared/src/components/Canvas/{AppBar,GardenChip,FabContext,LeftSheetContext,...}.tsx` — file-level `// ----` section banners (3–8 sections per file, 2 lines each). Each is a multi-section file with types + internal helpers + context + provider in one file. Keep.
- `packages/admin/src/components/Admin*.tsx` file-level `// ==== Types ==== / ==== Component ====` — 13 component files, same pattern, 2 lines each. Style consistency across the Admin\* palette. Keep.

## 10. KEEPs — Gold-Standard WHY Comments

Short reference list to preserve in any cleanup sweep:

- `packages/shared/src/modules/job-queue/index.ts:25–42` — `createOfflineTxHash` — warns never to submit the synthetic hash to an RPC.
- `packages/shared/src/hooks/work/usePlatformStats.ts:94–100` — explains why `enabled: gardenAddresses.length > 0` was removed (TQ v5 placeholderData data loss).
- `packages/shared/src/utils/errors/contract-errors.ts:23–30, 183` — operational doc (cast sig, last-updated) + legacy selector label.
- `packages/shared/src/workflows/authMachine.ts:1–36` — state-machine contract.
- `packages/shared/src/utils/scheduler.ts:99–104` — `// For background priority, add small delay to allow other tasks` justifies the `+ 1` in a fallback.
- `packages/shared/src/modules/transactions/embedded-sender.ts:1–15` — module-level explanation of why this is NOT gas-sponsored yet.
- `packages/shared/src/modules/translation/browser-translator.ts:4, 13` — labels `window.translation` (old) vs `window.ai.translator` (new) feature-detect branches.
- `packages/shared/src/providers/Auth.tsx:228–231, 442–444, 530` — AppKit connector-id uncertainty; reason to KEEP stored credential on logout; external-actor race on wallet restore.
- `packages/shared/src/utils/time.ts:64–68` — `normalizeTimestamp` seconds-vs-ms heuristic with `1e12` threshold.
- `packages/shared/src/config/blockchain.ts:170–184` — RPC URL pattern-matching branches (each is short and tied to an actual branch).
- `packages/shared/src/utils/eas/encoders.ts:360` — legacy v1 format branch label.
- `packages/shared/src/utils/storage/file-serialization.ts:76` — legacy File-object branch label (iOS caveat).
- `packages/shared/src/hooks/utils/useAsyncEffect.ts` — inline comments explaining abort-vs-error branching.
- `packages/admin/src/routes/IndexRoute.tsx:42` — `// Loading — mirrors the spinner inside CanvasLayout so there's no layout flash.`
- `packages/client/src/views/Home/Garden/index.tsx:59` — `// Reset scroll position before paint — prevents flash from stale scroll state`.
- All 23 `// eslint-disable-next-line <rule> -- <reason>` comments — each carries a concrete justification inline/trailing.
- All 6 `// @ts-expect-error <reason>` comments in tests + 1 in `packages/client/src/views/Garden/Media.tsx:46` (`iOS Safari standalone mode`) — each explains the escape hatch.

---

## Recommendation for human reviewer — three focused passes

1. **Micro pass (safe, 2 lines).** Replace the two `console.debug` in `AppSettings.tsx:182,187` with `logger.debug(...)`. Drop the `import.meta.env.DEV` guard (logger has internal level gating).
2. **JSDoc trim in `packages/shared/src/utils/{eas,blockchain,action,compression}.ts`** (§6). ~160 lines deletable, preserves module-level `@module`/`@see`/non-trivial `@example` that teach usage.
3. **WHAT-narrator sweep in `views/` only** (§4). Client `views/` + admin `views/` + admin `components/Hypercerts/*` + admin `components/Layout/CommandPalette.tsx`. ~120 lines. Stay out of `shared/providers/`, `shared/hooks/`, `shared/modules/` until the first two land and are merged (higher WHY-comment density, more judgment needed).
4. **Contracts/script history comment cleanup** (§3). 7 blocks, ~30 lines. Zero runtime risk.
5. **Inner-function dash dividers in AdminTextField.tsx + AdminListItem.tsx** (§9). ~24 lines. Do AFTER (1)–(4) land — these will probably get caught in a future "split this component" refactor anyway.

**Total achievable: ~340 LOC** of comment/JSDoc reduction with no executable-code changes, no API surface change, no test changes.
