# Docs Accuracy Review — Codex Pass 1

Date: 2026-04-01
Branch: `codex/docs-pass-1/2026-04-01`
Scope: Builders + reference docs accuracy review against the current repository state

## Docs reviewed

- `docs/docs/builders/getting-started.mdx`
- `docs/docs/builders/agentic/codex.mdx`
- `docs/docs/builders/testing/vitest.mdx`
- `docs/docs/builders/testing/playwright.mdx`
- `docs/docs/builders/deployments/status.mdx`
- `docs/docs/reference/faq.mdx`
- `docs/docs/reference/glossary-community.md`

## Fixes applied

### `docs/docs/builders/getting-started.mdx`

- Corrected the admin dev port from `3000` to `3002`.
- Corrected the indexer local GraphQL port from `8081` to `8080`.
- Updated the troubleshooting row so the port conflict guidance matches the actual app ports.

### `docs/docs/builders/agentic/codex.mdx`

- Updated the default Codex model from stale `gpt-5.3-codex` text to the current `.codex/config.toml` values: `gpt-5.4` with `model_reasoning_effort = "xhigh"`.
- Reworded the sandbox description to match the current split between agent execution and setup/maintenance scripts.
- Updated the environment setup snippet to reflect current behavior:
  - Node.js install is part of setup.
  - `VITE_CHAIN_ID` uses the current fallback form.
  - `.agents/skills` symlink creation is part of setup.
- Updated the prose summary and comparison table to match the current Codex config surface.

### `docs/docs/builders/testing/vitest.mdx`

- Removed the incorrect claim that all packages enforce the same 70% coverage thresholds.
- Added the real per-package thresholds from the live `vitest.config.ts` files.
- Documented the `agent` package exception: `node` environment plus `fileParallelism: false`.
- Replaced inaccurate root-level watch / coverage / single-file commands with package-scoped commands that match current scripts.

### `docs/docs/builders/testing/playwright.mdx`

- Replaced misleading “default” usage guidance with the repo’s actual root entry points:
  - `bun run test:e2e`
  - `bun run test:e2e:smoke`
  - `bun run test:e2e:fork`
  - `bun run test:e2e:passkey`
  - `bun run test:e2e:testnet`
- Added the current behavior of `scripts/test-e2e.js`: it starts `bun dev`, waits for client/admin health, then runs Playwright with `SKIP_WEBSERVER=true` and `SKIP_HEALTH_CHECK=true`.
- Removed the stale implication that mobile projects are simply “disabled by default.”

### `docs/docs/builders/deployments/status.mdx`

- Corrected the production client URL from `app.greengoods.app` to `greengoods.app`, matching repo references such as `packages/shared/src/config/app.ts`, client metadata, and Docusaurus nav links.

### Reference metadata fixes

- `docs/docs/reference/faq.mdx`
  - Replaced the broken `source_of_truth` path `docs/docs/intro.md` with `docs/docs/community/welcome.mdx`.
- `docs/docs/reference/glossary-community.md`
  - Added the missing `source_of_truth` frontmatter so the page passes docs audit.

## Validation run

- `bun run docs:audit`
  - Result: no warnings
- `git diff --check`
  - Result: clean

## Remaining suspected inaccuracies

- `docs/docs/builders/packages/contracts.mdx`
  - Broadly aligns with the repo, but several implementation details are dense and should get a second human pass for nuance, especially the adaptive build explanation and command wording around deploy / upgrade flows.
- `docs/docs/builders/packages/client.mdx`
  - The service-worker section is mostly grounded, but it is worth cross-checking against current product behavior in a running browser because the repo contains at least one stale inline code comment that still says `registerType: "autoUpdate"` while the Vite config is `prompt`.
- `docs/docs/reference/faq.mdx`
  - Content is mostly fine after metadata repair, but several user-facing answers depend on community docs outside this pass. A second pass should verify that all linked community pages still exist and still describe the live UX.

## Files that need a second human / Claude pass

- `docs/docs/builders/packages/contracts.mdx`
- `docs/docs/builders/packages/client.mdx`
- `docs/docs/reference/faq.mdx`

