# Local-First Extended Tooling Specification

> **Archived record:** implementation is closed. Operational handoffs, artifacts, and lane files were removed; preserved reports and any references below describe historical execution, not live work.

The preserved [brief.md](brief.md) is the discovery source. This idea is not an implementation specification.

## Question to resolve

Re-evaluate one narrow capability such as offchain attestations, media preparation, or zero-download quality checks instead of restoring the former umbrella.

## Required before promotion

- Verify every claimed existing hook, route, schema, document, and browser capability against current develop.
- Select one bounded outcome and move unrelated follow-ons back to ideas.
- Name affected packages, privacy/security boundaries, offline or failure semantics, and explicit non-goals.
- Record maintainer decisions and current validation commands before any implementation lane becomes active.
