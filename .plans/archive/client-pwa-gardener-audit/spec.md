# Client PWA Gardener Audit Spec

The completed pre-policy implementation softened gardener-facing protocol vocabulary and gated operator-only surfaces. This hub now exists to track the remaining QA proof: a live browser pass through login, garden detail, profile account details, and assessment detail.

No new implementation should happen under this hub unless the browser pass finds a concrete regression against `fixes.md`.
