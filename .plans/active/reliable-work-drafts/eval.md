# Acceptance

Verify stale picker references, compression fallback, all-field/audio restart, resume without duplication, ordered writes, empty removals, failed migration/quota and draft cap preservation. Interrupt media/audio/metadata upload, reload and reuse confirmed CIDs; wallet cancellation/fallback and checkpoint write failure retain work. Verify video limits and photo minima, rounded location metadata and opt-out. Verify sheets in authenticated Brave and Android process termination after Saved. No clean-commit or device claims until observed.


## Reopened review acceptance

- [ ] Confirmation: direct/queue/batch timeouts, offline retry and restart never send a known broadcast again; success completes the same identity, revert requires explicit retry, checkpoint-write failure retains the in-memory guard.
- [ ] Lifecycle: dashboard deletion, in-flight autosave, failed deletion, unrelated deletion and account changes preserve the correct composer state.
- [ ] Previews: repeat refresh/text saves, replacement, removal, cleanup, teardown and remount keep resources bounded and never return a revoked cached URL.
- [ ] Legacy recovery: mixed readable/unreadable evidence, reload, replacement, removal, cleanup failure, quota and limit failure retain stable identities and source data.
- [ ] Authenticated Brave: pending confirmation, deletion, mounted previews and partial recovery.
- [ ] Physical Android Chrome/PWA: complete the original capture, media, restart and process termination matrix.

Automated evidence may close individual code checks in the repair report; live acceptance remains open until observed. Uncommitted working-tree results are not a commit-attributed readiness receipt.


## Required proof for the accepted offline extension (2026-09-12)

Establish failing regression proof before each repair, then render the validation selector before
running each check batch. Promote relevant temporary diagnostics into repository suites.

Submission coverage: failed UserOperation inside a successful enclosing transaction; delayed
receipts/restart; failed checkpoint and draft-retirement writes; duplicate admission; account
switch during upload/signing; healthy/exhausted/reverted queues; cancellation without automatic retry.
Draft coverage: work-only, avatar-only and mixed upgrades; interruption; newer destination conflicts;
quota failure; blocked upgrades; preserved attachment hashes, active identity and account isolation.
Reading coverage: cold/warm/partial caches; hanging approval reads; cached refresh failure; distinct
same-action submissions; metadata reconnect recovery; prepared content after seven days offline.
Storage/media coverage: first install, worker activation, changed avatar, URL variants, preview versus
original, interrupted preparation, 150 MiB enforcement, eviction and persistence failure.

After focused proof, run Shared/Client/Admin compatibility and the full selector-required critical
readiness gate, production PWA builds and applicable design/i18n/story checks. Current-head CI and
clean revision receipts are needed for release readiness; dirty working-tree runs are explicitly
labelled and never attributed to HEAD.

Physical acceptance remains required: fresh and upgraded installed Android apps, all four wizard
steps in actual airplane mode, process termination, wallet and passkey reconnection with exactly one
correct Work attestation and evidence retained until confirmation. Record build, browser/wrapper,
worker version, connectivity events and banner geometry. Run installed iOS storage/migration/safe-area
compatibility. Do not force worker activation during active work; stage the rollout first. Aggregate
telemetry excludes payloads and identifiers. Production readiness stays blocked until observed.
