# Remaining device acceptance

Use the existing authenticated Brave profile and a physical Android Chrome/PWA. Verify photos, completed audio and all fields after Saved, then background/resume, Android back gesture, offline restart and process termination. Verify accepted MP4/WebM playback and approximate location opt-out. Interrupt a five-attachment upload, restart, and confirm only unfinished attachments upload. Do not use real wallet submission without the user's authorization for that test.

The local code and automated checks do not establish physical Android acceptance. No Android bridge executable was available in the current shell; device availability was asked in the task and is awaiting a response.
