# Commitment Pooling — Visual Asset Index

**Updated**: 2026-08-04 (the Story source now includes the three-community pathways, the staged path to fuller pooling, and the one bounded future-pool loop. The exact shareable artifact passed prerender verification; live publication and Google Doc image placement remain explicit manual steps. Previous 2026-08-02 founder-feedback pass: **Diagrams were renumbered so numeric order is the reading order** — overview → a promise in the field → states/claims/exchange → data and value rails → deployment last — which retired the `b`/`c`/`d` suffixes and un-numbered the permission table; every pre-renumbering anchor is preserved as an alias, and `diagrams.md`'s preamble carries the old→new decode table. Story tab: intro rewritten out of chain vocabulary, a "Who confirms?" explainer added, a restrained bold pass over load-bearing invariants, inline links to primary sources, and a closing "Go deeper" block. **No docs-site links anywhere — several sections there are stale.** `synthesis-circular-gd` gains the services return leg; `rollout-timeline-band`'s Build card no longer overflows; the bilateral exchange sequence no longer clips its actors or spills its predicate note; diagrams that mix arrow styles now carry an auto-derived arrow-meaning key. Reference tab reordered with Open questions last, and an eighth question records the return-leg decision. Previous update 2026-08-01: CPP-alignment and exchange-wave pass after the Grassroots Economics review — `synthesis-ge-protocol` recalibrated with explicit August/current-roadmap/later-roadmap status, NET-NEW `synthesis-three-layers` added, story tab gains "Three layers, named honestly", and the flywheel/tiers borrow-and-repay prose reflects the 2026-08-01 August-wave unblock behind its own gates. The Screens panel now carries 37 wireframe screens, including the seven §8 CPP-alignment deltas and W28–W31 exchange/template frames. Previous update 2026-07-28: group-commitment amendment — D3, W2b, repeatable requirements, contribution-derived recognition, explicit garden retention, and contributor child payouts entered the gallery; wireframes.md then had 26 frames with per-frame hi-fi deep links. The Reference panel also records the settled Needs architecture: four schemas across `NeedsResolver` and `FundingAttributionResolver`, EAS recipient/refUID normalization, and directional `bool support` signals.)
**Gallery**: [Commitment Pooling — Visual Asset Gallery](https://claude.ai/code/artifact/007ef090-9e26-4b1d-898c-615155304d9d), built by `visual-assets-artifact.build.ts` in this folder. The source now carries 21 Story sections in a deliberate reading path: concrete promise loop → roles and grounded journeys → three community pathways → offer-over-time → three-layer framing → staged path to fuller pooling → one bounded future pool → six-function framing → value movement and proof → support tiers/flywheel/circulation → rollout and document ownership. The Architecture tab is generated wholly from `diagrams.md` (**42 Mermaid blocks across 29 named D-sections, D1–D29**). Since 2026-08-02 the D-number *is* the reading position: D1–D4 overview, D5–D7 a promise in the field, D8–D14 states/claims/exchange, D15–D24 the data and value rails, D25–D26 operate and deploy, D27 offering over time, D28 the three-identity boundary, and D29 the staged full-pool path. Each diagram is footnoted with its own colour key, every ERD carries a cardinality key, and any diagram mixing arrow styles also carries an arrow-meaning key. The Screens tab comes from `wireframes.md`. A fourth **Reference** tab carries the deep material that used to open the Architecture tab — the `diagrams.md` preamble, the 36-asset coverage matrix, the un-numbered sensitive-action permission table, and the ship-time appendix — and closes with the audited **Open questions** panel (eight items, including the G$ services return leg). The 21-section source and exact publishable artifact passed the prerender verifier on 2026-08-04; the live Claude artifact still requires an explicit republish to the same URL.
**Pipeline**: every asset is a hand-crafted, self-contained SVG (Warm-Earth palette, explicit 2x `width`/`height` so Linear sizes it correctly) with a 2x PNG companion for Linear upload. Linear docs cannot be written with embedded images over MCP — **uploads are manual** (drag the PNG into the doc at the placement below). The SVG is the print/PDF source; the PNG is the Linear-upload source.

**Editorial repair 2026-08-04**: `synthesis-circular-gd` was **redesigned as a four-block circulation loop** for the Deeper Reference. The orbit and the `local goods and services` block are retired; the asset is now House of Alignment → GG protocol pool → Garden accounts → Gardeners, with **two return arrows that both land on the protocol pool** (gardeners spending G$ on Green Goods services, and gardens seeding new commitments). `protocol treasury` is renamed **`GG protocol pool`** and carries an in-block clarification that value stays on the separate Celo rail, so no reader can infer that the pooling module custodies G$. Only the upstream House of Alignment relationship is drawn solid; protocol-to-garden delivery, contributor rewards, and both return paths are gated ochre dashed, with a two-line legend naming the distinction. The Google Doc placement is **pending a manual re-upload** of the regenerated PNG.

## Style contract

Reference implementation: `artifacts/visuals/external-brief-loop.svg`. Paper `#FBF8F2`, ink `#2A2722`, stone text `#6E6857`, Built = fill `#DFEBDE` / stroke `#4C7A57` solid, Planned/gated = white fill / stroke `#988D77` dashed `5 4`, negation `#A65D57`, arrows `#988D77`, system font stack, rx 12, footer caption `Green Goods · Commitment Pooling — v1 (2026-08)`. Solid = Built/live, dashed = Planned/gated, everywhere.

Three clarifications added 2026-08-02, each after finding the same drift in more than one asset:

- **Card and panel outlines are stone `#988D77`, never ink `#2A2722`.** Ink is for type only. Four assets had drifted to ink outlines (`synthesis-ge-protocol`, `synthesis-three-layers`, `rollout-timeline-band`, `external-brief-money-map`); all are corrected. The tell is a card that reads hard-edged next to its siblings.
- **Ochre `#8A6D3B` is a named role: gated or later-roadmap.** It is a *sub-category* of dashed-Planned, not a competing convention — use it for something inside the wave but behind its own extra gates (borrow-and-repay) or for a roadmap seam that extends the shipped system (`synthesis-ge-protocol`'s partial functions). Plain dashed stone `#988D77` remains the default for ordinary Planned. Do not introduce a third accent; `#B98A3E` was one and has been normalized away.
- **The footer stamp is `v1` — no minor version.** `v1` names the Commitment Pooling release, not the asset's revision count, so `v1.1`/`v1.2`/`v1.4` were misreadings and are normalized. The date tracks **when the asset's claims were last reviewed**, not when the PNG was last rendered, so a pure re-render does not move it. A green check or red failure label may describe an authenticated lifecycle outcome inside a planned dashed boundary; outcome color never reclassifies the component as built. On paper, stone text passes 4.5:1 and planned boundaries/arrows pass 3:1; planned number badges use ink rather than white.

**Typography and accessibility contract**: optimize each source for a 624–720 px document placement, not only its 2× export dimensions. Body copy should normally use at least 16–20 viewBox units, with larger type where the canvas is 1000 or 1100 units wide; short evidence-state labels may use 14–16 only when the surrounding card and explicit solid/dashed treatment carry the same meaning. Every informative SVG requires `role="img"`, a unique `aria-labelledby`, and matching `<title>` + `<desc>`. The gallery's inline desktop view fits the complete composition to the reading column; the dedicated preview plane is the detailed-reading path, cloning the inline SVG or rendered Mermaid with zoom plus pan/scroll and focus return on close. **Rendered architecture diagrams fit to width, not to viewport height** (changed 2026-07-27): binding that inline view to a laptop-height plane drove the large ERDs to ~0.15 scale — roughly two-pixel type — so a tall diagram is now simply tall (against an absolute 1500 px ceiling) and the page scrolls past it. This applies to the Mermaid overview sizing only: the hand-drawn story SVGs keep their deliberate `height:min(78dvh,820px)` document-scale cap on desktop, and the preview dialog's **Fit** control still fits both axes by definition. Narrow screens fit the whole composition to the card rather than holding a 720 px floor, which used to show only the left quarter of an asset — usually empty canvas — and forced a pan before anything was visible.

**Content constraints (all assets)**: architecture diagrams and reusable synthesis diagrams stay garden-agnostic so they survive cohort changes. A narrowly labelled **case-study/story asset** may name Tech and Sun or another currently named garden only when it is used in that garden's external-document example, marks quantities and future behavior as illustrative/gated, and is not reused as general architecture. Presentation prose names the three confirmed candidate gardens only — the fourth slot is open and is never given a name (Decision Log #29). External-facing assets use the human role names Gardener, Garden Steward, Evaluator, Funder, and Community; implementation specs may retain `operator` as the permission term. A Need is a problem paired with a desired outcome; Request / Offer appears only as commitment direction. Show only the modeled ProtocolToGarden funding route (HoA → protocol Safe stays upstream). Whenever an asset quantifies the upstream pilot, use **$800 per month paid in G$, July through September 2026, $2,400 total**; never use a transaction-level G$ token count as the funding amount. Keep *Queued*, *Dispatched*, derived *delivery delayed*, derived *Celo executed / acknowledgment pending*, authenticated *Confirmed*, authenticated *Failed*, and origin-specific *Cancelled* distinct; cancellation is available before dispatch or after authenticated failure, never from delay. Show command retry and acknowledgment retry as separate paths and state that neither can execute G$ twice. The selected circulation return leg is gardens spending earned G$ on Green Goods team services, pending GoodDollar mandate confirmation; local merchant/store routes remain explicitly unmodeled. No banned vocabulary (streak/countdown/leaderboard/FOMO/urgency terms); no rankings imagery.

**Canvas convention**: square (or ≤1.3:1) viewBoxes only — macOS `qlmanage` (the local render pipeline) slice-crops wide-landscape SVGs from the left, so wide canvases fail local verification even though browsers/Linear render them correctly. Square canvases are safe everywhere.

## Asset index

| File | Target document | Placement | Shows |
|---|---|---|---|
| `artifacts/visuals/external-brief-loop.svg/.png` | **01 External Brief** | after "How the loop works" | The 7-step loop as a circle, Built/Planned coded |
| `artifacts/visuals/external-brief-roles.svg/.png` | **01 External Brief** | before "An invitation" | Five roles × their loop actions |
| `artifacts/visuals/use-cases-journey-strip.svg/.png` | **03 Use Cases & Scenarios** | after the section introduction | Three garden-agnostic Need → Request/Offer commitment → proof journeys, including no-token and gated G$ paths |
| `artifacts/visuals/synthesis-three-layers.svg/.png` | **02 GE Learnings & Full Flywheel** | at the TL;DR, before the six functions | One Commitment Pooling system built in stages: August coordination foundation → outcome-linked support (built rails + gated G$) → later reciprocal routing/exchange seams, with the gated August borrow-and-repay companion between |
| `artifacts/visuals/synthesis-ge-protocol.svg/.png` | **02 GE Learnings & Full Flywheel** | at §1.1 | Six functions with per-function v1 status: curation/limitation/repair full in the approved August scope; valuation recorded; exchange reference + bilateral paired acceptance in approved contract scope but not yet shipped; route partial; multilateral/transferable execution reserved |
| `artifacts/visuals/external-brief-money-map.svg/.png` | **01 External Brief** | in "Trust and money" | Arbitrum command/ack ∥ Celo G$, bounded route, no-bridge; HoA mechanism/receipt remains visibly evidence-pending |
| `artifacts/visuals/rollout-settlement-states.svg/.png` | **04 Rollout, Measurement & Claims** | in the blank image location below "What the settlement states mean" and above its italic caption | Queued→Dispatched→Celo outcome stored→Confirmed/Failed state machine, origin-specific cancellation boundaries, no human verification path, and no implication that a stored failure moved G$. Wording **inside this drawing**: the rail is named as the Celo settlement command and authenticated acknowledgment rather than as CCIP, so the picture stays readable without transport vocabulary. This scopes the asset only — the canonical Google Doc's own evidence-label table defines `Dispatched` and `CCIP-confirmed` for readers and is unaffected |
| `artifacts/visuals/external-brief-funding-rails.svg/.png` | **01 External Brief** | in "How delivered outcomes attract funding" | Three rails converging on delivered outcomes |
| `artifacts/visuals/synthesis-three-tiers.svg/.png` | **02 GE Learnings & Full Flywheel** | at §4.4 | Mutual aid → G$ paid work → borrow-and-repay ladder |
| `artifacts/visuals/synthesis-flywheel.svg/.png` | **02 GE Learnings & Full Flywheel** | at TL;DR or §2.0 | Loop at center, four funding rails, gated borrow-repay ring |
| `artifacts/visuals/synthesis-circular-gd.svg/.png` | **02 GE Learnings & Full Flywheel** | at §2.8 | Four-block loop: HoA → GG protocol pool → garden accounts → gardeners, plus two return arrows from gardeners and from garden accounts back to the pool. Only the upstream HoA relationship is solid; delivery, rewards, and both returns are gated dashed |
| `artifacts/visuals/rollout-timeline-band.svg/.png` | **04 Rollout, Measurement & Claims** | directly below "Sequence" | Four native phases plus two separately labeled operational checkpoints |
| `artifacts/visuals/rollout-ownership-map.svg/.png` | **04 Rollout, Measurement & Claims** | under "Single sources of truth" | One canonical external document supported by distinct repo, Linear, and Google Doc truth surfaces; no prose mirrors. Section cards carry the live tab names, including 02 Deeper Reference, 03 Applied Reference, and 04 Rollout, Measurement & Claims |
| `artifacts/visuals/offer-that-continues.svg/.png` | **02 GE Learnings & Full Flywheel** | with the offer-over-time section | One noun used two ways: Offer once, or Offer over time. Saved offer details (signed offchain, reusable input to either path) → pool-scoped ongoing Offer → already-reserved available places → immutable instances → exact-count Story. A claim accepts a place; it never creates one |
| `artifacts/visuals/standing-capacity-reserved.svg/.png` | **02 GE Learnings & Full Flywheel** | beside the offer-over-time section | Offer reserves its class and provider slot at creation; Request reserves only when a provider accepts — so a displayed place is genuinely open and acceptance never reserves twice |
| `artifacts/visuals/standing-compounds.svg/.png` | **02 GE Learnings & Full Flywheel** | after the capacity contrast | One ongoing Offer across cycles with exact accumulated counts, against the rejected replacement model where each cycle recreates an unlinked commitment and no history can accumulate |
| `artifacts/visuals/standing-rest-without-erasure.svg/.png` | **02 GE Learnings & Full Flywheel** | with the lifecycle discussion | Resting stops new places and pool discovery only; taken-up promises, open places, and the whole Story continue. Resume creates no availability; retire is terminal and still rewrites nothing |
| `artifacts/visuals/standing-succession-horizon.svg/.png` | **02 GE Learnings & Full Flywheel** | closing the offer-over-time section | Rest/resume/retire drawn solid as the first implementation; five later consent-gated acts drawn dashed below a labelled horizon. A promise already made never moves, and nothing is saleable |
| `external-brief-loop` + `external-brief-roles` (reuse) | **04 Rollout, Measurement & Claims** | at "One grounded story" | Same assets as the brief, reused for visual consistency |
| `diagrams.md` D13 (Mermaid gallery source) | **Visual Asset Gallery — Architecture** | promise-lifecycle group, after D12 | Bilateral exchange creation reference → atomic paired acceptance → two independent fulfillment/confirmation lanes, with the no-coupling boundary explicit |
| `diagrams.md` D28 (Mermaid gallery source) | **Visual Asset Gallery — Architecture** | after D27 | Promise instance, ongoing Offer series, and future voucher class remain distinct; a versioned adapter consumes fulfillment facts without moving promise authority |
| `diagrams.md` D29 (Mermaid gallery source) | **Visual Asset Gallery — Architecture** | after D28 | Coordination → evidence → fulfilled backing → one bounded pool → separately gated capacity backing/federation, with G$ support kept separate |

### Story and Google Doc additions — source complete, publication and Doc placement open

**State 2026-08-04.** All three SVG/PNG pairs are created and synchronized. They are wired into
`visual-assets-artifact.build.ts`, bringing the Story to 21 sections, and the exact shareable
artifact passed the prerender verifier with 43 Mermaid blocks rendered and zero failures.
`external-brief-pilot-pathways` remains the approved three-community examples asset. It is not
used to imply a Tech and Sun future voucher path; the canonical external prose carries that
two-horizon distinction. The Google Doc prose was direct-edited, reloaded, and re-read on
2026-08-04. The live Claude artifact and all three Google Doc image placements still require
explicit manual publication/upload by the user.

| Asset | Story / document role | Required claim |
|---|---|---|
| `synthesis-path-to-full-pooling.svg/.png` | Story tab, after the current three-layer framing; Deeper Reference in `How Commitment Pooling can grow in stages` | The base is useful and adaptable: coordination → evidence → fulfilled-backed voucher/redemption → one bounded pool → separately gated capacity backing/federation. No stage is automatic |
| `synthesis-single-pool-loop.svg/.png` | Story tab beside the deeper exchange explanation; Deeper Reference in `What one bounded pool would still need to prove` | Exact future loop: authorized class → bounded issuance → seed → exchange in/out → redemption → visible failure/repair. Promise authority and G$ support remain outside the loop |
| `external-brief-pilot-pathways.svg/.png` | Case-study Story section and External Brief "What this can look like on the ground" | Three grounded pathways: Tech and Sun, Muizenberg / Deep South, and AgroforestDAO / Redemption Hill. Quantities and participation remain provisional; the adjacent prose, not this image, explains Tech and Sun's two horizons |

Story reading order, navigation, alt text, and source asset count are updated. Publish only
`/tmp/cp-visual-shareable.PUBLISH-THIS.html` after rerunning the verifier; do not publish either
`DO-NOT-PUBLISH` build target.

## PNG upload alt text

Use the exact intent below when manually uploading a PNG to Linear or the canonical Google Doc. Do not use the filename as alt text, and do not prefix the description with “Image of.”

| PNG | Alt text |
|---|---|
| `external-brief-loop.png` | Seven-step commitment loop from a Need through baseline, promise, proven work, recipient confirmation, community learning, and impact certificate, with each step labeled Built or Planned. |
| `external-brief-roles.png` | Five role cards for Gardener, Garden Steward, Evaluator, Funder, and Community, with every action individually labeled Built or Planned. |
| `external-brief-money-map.png` | Arbitrum command and acknowledgment states above Celo G$ movement, with Dispatched separated from Confirmed and an explicit rule that G$ never bridges to Arbitrum. |
| `external-brief-funding-rails.png` | Impact certificates, supporter-vault yield, donations, and planned G$ settlement converging on delivered and confirmed outcomes. |
| `use-cases-journey-strip.png` | Three garden-agnostic journeys in which a problem and desired outcome become a Request or Offer commitment, followed by proof and confirmation; one moves no token and one uses separately gated G$ settlement on Celo. |
| `rollout-timeline-band.png` | Four native phases from July 22 through September 30, plus separate July and September operational checkpoints; the two September closures run in parallel. |
| `rollout-ownership-map.png` | One six-section external document supported by three distinct truth surfaces: repo specifications and evidence, Linear delivery status, and Google Doc external narrative, with no mirrored prose in the repo. |
| `rollout-settlement-states.png` | Settlement flow from Queued to Dispatched to Celo execution and acknowledgment, then Confirmed only after an authenticated success acknowledgment for the current execution key and attempt, or Failed after its authenticated failure acknowledgment; cancellation is allowed before dispatch or after authenticated failure, never because delivery is late; no human verification path. |
| `synthesis-flywheel.png` | Commitment loop at the center of donations, supporter-vault yield, impact-certificate sales, and planned G$ settlement, surrounded by an evidence-gated borrow-and-repay ring. |
| `synthesis-three-tiers.png` | Three-step ladder from planned mutual aid to planned G$ paid work to evidence-gated borrow-and-repay. |
| `synthesis-circular-gd.png` | Four-block Green Goods circulation loop from the House of Alignment to the GG protocol pool, garden accounts, and gardeners, with separate return paths from gardeners and garden accounts back to the protocol pool. The pool coordinates claimable services and commitments while G$ remains on its separate Celo rail. |
| `synthesis-ge-protocol.png` | Six commitment-pooling functions with explicit time horizons: curation, limitation, and repair carry full August mechanics; valuation is an August record; exchange combines the reference record with bilateral paired acceptance; route is partial; multilateral and transferable exchange plus relative pricing stay on the later roadmap. |
| `synthesis-three-layers.png` | One Commitment Pooling system built in stages: the August coordination foundation, outcome-linked support on built and gated funding rails, and later reciprocal routing/exchange seams — with records-only borrow-and-repay as a gated August companion. |
| `synthesis-path-to-full-pooling.png` | Six stages of Commitment Pooling shown as a snaking path: commitment coordination, compatibility freeze, field evidence, fulfilled backing, one bounded pool, and separate decisions about capacity backing or federation. The coordination stage is drawn solid because it is specified and useful on its own; every later stage is dashed and gated. The field evidence stage records that not supported or unavailable is a valid result. The separate G$ support rail is useful at every stage and is never voucher redemption by implication. |
| `synthesis-single-pool-loop.png` | A seven step exchange loop that one bounded pool would have to prove: authorized class, bounded fulfilled backed issuance, seed inventory, quote and limit, exchange in and out, redemption, and settled or visibly repaired. The loop is drawn open on the left because no return leg is specified. The promise record and the G$ support rail sit outside the loop; only explicit class terms connect G$ to redemption, and it is never redemption by implication. Every element is a later capability, not built and not authorized, and federation is not shown. |

### Known asset issues (2026-07-18)

- ✅ **`rollout-ownership-map` reconciled 2026-07-21.** The asset now shows one canonical six-section external document supported by three non-overlapping truth surfaces. The Google Doc owns all external prose; the repo owns specs/evidence/artifacts; Linear owns current delivery status. No repo or Linear prose mirror is prescribed.
- ✅ **`synthesis-ge-protocol` redrawn 2026-07-20; recalibrated 2026-08-01; restyled 2026-08-02.** The 07-20 redraw retired the four-part quadrant and rendered six equally visible functions. The 08-01 recalibration (Grassroots Economics review response and exchange wave, plan registers #73–#75) adds explicit August/current-roadmap/later-roadmap status, the paired-start exchange rung, and more card padding, so the asset no longer implies all six are equally implemented or clips its subtitle. The 08-02 restyle moves status off per-card caption text and onto a colour contract — solid green rule + green heading = full August mechanics, dashed brown rule + brown heading = August foundation with a roadmap seam — keeping the solid/dashed encoding this set already uses for Built versus Planned, so status survives greyscale printing and colour-vision differences. It also returns the asset to house style: `#988D77` warm-stone card stroke (it had drifted to `#2A2722` ink) and the square 900×900 / 1800px canvas shared with `synthesis-circular-gd` and `synthesis-three-tiers`. The stale "shown equally" PNG must not be re-uploaded; regenerate before any manual upload.
- ✅ **`synthesis-three-layers` added 2026-08-01; restyled 2026-08-02.** NET-NEW three-layer framing presents one Commitment Pooling system: August coordination foundation / outcome-linked support / later reciprocal routing and exchange. It carries borrow-and-repay as a gated August companion rather than a deferred follow-on. The 08-02 restyle brings it back to house style alongside `synthesis-ge-protocol`: `#988D77` warm-stone card stroke and `#988D77` connectors (both had drifted to `#2A2722` ink) and the square 900×900 / 1800px canvas. Solid-versus-dashed outlines still carry built / gated / later, and the per-layer caption colours are unchanged. Needs its first manual PNG upload to GDoc tab 02.
- ⚠️ **Six PNGs were stale for a day — regenerated 2026-08-02.** Commit `64bc43b9b` (2026-08-01) applied the CPP vocabulary correction — "members" → "gardeners", "community need" → "Need" — to six SVGs and regenerated only the two PNGs it also redrew. `external-brief-funding-rails`, `external-brief-loop`, `external-brief-money-map`, `synthesis-circular-gd`, `synthesis-flywheel`, and `use-cases-journey-strip` therefore kept shipping the retired word in the only artifact anyone uploads. All six re-rendered at their declared sizes. **This is the failure mode to watch: the SVG is the source of truth but the PNG is the deliverable, so an SVG-only commit silently publishes nothing.** Re-render in the same commit that edits an SVG.
- ✅ **`synthesis-three-tiers` recalibrated 2026-08-02.** Tier 3 read `designed · evidence-gated` while Tiers 1–2 read `Planned · August`, which told readers borrow-and-repay was outside the August wave after the 2026-08-01 unblock had already moved it inside. It now reads `August wave · 3 gates`, and its outline moved from the receding `#BFB5A1` to the gated ochre `#8A6D3B` that `synthesis-three-layers` already uses for the same object, with the heading restored to ink so the tier is no longer visually demoted. Separately, `operator-executed` → `steward-executed`: the canonical Google Doc states its own rule — "External material uses Garden Steward; implementation permissions may still use operators" — and this is external material. It was the last `operator` in any asset.
- ✅ **`synthesis-flywheel` recalibrated 2026-08-02.** The outer ring label read `designed · evidence-gated`; it now reads `August wave · 3 gates`, and the ring moved to gated ochre for consistency with the two assets above. The ring stays dashed on purpose — the three dispatch gates are real. The arc's top gap was widened (`M 310 155.1 A 430 390 0 1 0 690 155.1`) because the label overlapped the stroke at the previous 310px opening.
- ✅ **`rollout-timeline-band` + `external-brief-money-map` restyled 2026-08-02.** Both carried the same `#2A2722` ink-stroke drift as the two synthesis diagrams: four phase cards plus the legend swatch on the first, the two chain panels on the second. Now `#988D77`, with the timeline's checkpoint ochre normalized from `#B98A3E` to the house `#8A6D3B`. No claim changed in either. The timeline's four dates were checked against the canonical Google Doc's Sequence table and match exactly (Jul 22 / Jul 31 / Aug 12 / Sep 30).
- ✅ **`use-cases-journey-strip` added 2026-07-21.** It covers the missing Use Cases section with three garden-agnostic paths and encodes the locked Need-versus-commitment model without tying the asset to a provisional cohort.

Tab **02** currently renders the older embedded `synthesis-ge-protocol` plus `synthesis-flywheel`, live-verified 2026-08-01 and re-confirmed by a full document read on 2026-08-02. The corrected `synthesis-ge-protocol` and new `synthesis-three-layers` PNGs both require manual replacement/insertion after this pass; `synthesis-three-tiers` also remains pending. `synthesis-circular-gd` was replaced and visually rechecked after reload on 2026-08-04, then **redesigned again in the same pass as a four-block circulation loop**, so its Doc placement is pending re-upload once more.

**Scope widened 2026-08-02: every embedded PNG in the Google Doc fell behind the repo.** The remaining differences are ordered by whether they are worth a reader's attention:

| Priority | Assets | Why it matters to a reader |
|---|---|---|
| **Replace before the next partner read** | `synthesis-circular-gd` | **Redesigned 2026-08-04 as a four-block circulation loop for the Deeper Reference.** The orbit and the `local goods and services` block are gone; `protocol treasury` is now `GG protocol pool`, carrying an in-block clarification that value stays on the separate Celo rail. Two return arrows land on the pool. Only the upstream HoA relationship is solid; everything downstream is gated dashed under a legend, so the loop is not presented as operational. The Doc still embeds the previous orbit version. |
| **Replace before the next partner read** | `external-brief-loop`, `external-brief-money-map`, `external-brief-funding-rails`, `synthesis-flywheel`, `use-cases-journey-strip` | These still show the retired **"members"** (and "community need"). The correction has been in the SVGs since 2026-08-01 but never reached a PNG, so the document has been publishing the retired word. |
| **Replace before the next partner read** | `synthesis-ge-protocol`, `synthesis-three-layers`, `synthesis-three-tiers` | Claim changes: the six-function colour contract, the net-new layers diagram, and the borrow-and-repay August-wave/steward corrections. |
| **Replace before the next partner read** | `rollout-ownership-map`, `rollout-settlement-states`, `rollout-timeline-band` | **Rollout pass 2026-08-04.** These are no longer cosmetic. The ownership map carried three retired Google Doc section titles and now names the live tabs: `02 Deeper Reference`, `03 Applied Reference`, and `04 Rollout, Measurement & Claims` with the subtitle "Audiences, sequence, measurement, and claims". The settlement asset dropped the reader-facing `CCIP command and acknowledgment` label for `Celo settlement command and authenticated acknowledgment`, and its description now states that execution on Celo alone is never confirmation. The timeline band changed no date, phase, or claim, but the Doc still embeds the July render, so its PNG needs replacing too. All three titles and footers are now free of em dashes; the footer separator is a middot. |
| Cosmetic, batch whenever convenient | `external-brief-roles` | Stamp normalization only. No claim moved. |

The published gallery artifact (`007ef090`) was last republished on 2026-08-02. Its source and exact shareable file were rebuilt and verified on 2026-08-04; the live URL still needs an explicit republish. The Google Doc still embeds the previous orbit version of `synthesis-circular-gd`, so it joins the pending image replacements listed above.

**Anchors for pending Google Doc image work.** The prose anchors below were re-read after the
accepted 2026-08-04 Google Doc update. Every image replacement or insertion remains a manual user
step; this pass did not edit, replace, upload, or reposition any image.

| Asset | Action | Anchor in tab 02 |
|---|---|---|
| `synthesis-ge-protocol.png` | **replace** the existing image | The image between the `The six protocol functions` heading and the bold paragraph beginning "The design is more than coordination…". Right-click → Replace image → Upload from computer, which preserves the current sizing and placement. |
| `synthesis-three-layers.png` | **insert** (first placement) | The empty paragraph closing the `TL;DR` section — directly after the paragraph beginning "**Three layers, named honestly:**" and ending "…relative pricing remain later roadmap seams.", and directly before the `What we take from the practice` heading. |
| `external-brief-pilot-pathways.png` | **insert** (first placement) | Tab **01 External Brief**, directly after the three provisional garden examples and before `Built today and planned next`. |
| `synthesis-path-to-full-pooling.png` | **insert manually** (first placement) | Tab **02 Deeper Reference**, in `How Commitment Pooling can grow in stages`, on the blank paragraph immediately above the caption beginning `Commitment Pooling can grow from useful coordination to one bounded pool…`. |
| `synthesis-single-pool-loop.png` | **insert manually** (first placement) | Tab **02 Deeper Reference**, in `What one bounded pool would still need to prove`, on the blank paragraph immediately above the caption beginning `One bounded pool would have to make fulfilled backing, bounded issuance…`. |

The two new Tab 02 PNGs are 1800×1800; Google Docs will downscale on insert. The prose at each
anchor already states the diagram's claim, so a failed placement degrades to text rather than to a
missing argument.

## Vocabulary source

Every state and enum label in these assets resolves to the machine-readable ontology sidecar, **`packages/shared/src/ontology/green-goods-ontology.json`** (human-readable render: `docs/docs/reference/ontology.generated.mdx`), which became repo canon with the ontology foundation on 2026-07-26. The commitment-pooling vocabularies carry `status: "spec"` and are transcribed from the contract and settlement specs, including contributor policy and payout-plan status.

Three rules bind the assets:

1. **1:1 mapping.** Display copy may prettify — a GraphQL mirror may read `APPROVAL_GATED`, an edge label may read "accepted (creator)" — but every label must map onto exactly one canonical member. No asset may introduce a term the sidecar does not carry; see `plan.todo.md` § *Ontology sidecar gaps* for the families flagged rather than invented.
2. **Provenance is visible.** The sidecar flags each state `on-chain`, `derived`, or `off-chain`, and the Architecture tab renders that: paper = on-chain, amber = derived, grey = app-only. A derived state must never read as a chain write. `Reconciled` is derived for commitments and on-chain for cycles — the two diagrams style it differently on purpose.
3. **`PoolType` is two vocabularies.** `commitment-pool-type` (Garden/Protocol) and the live Gardens V2 `signal-pool-type` (ActionSignal/HypercertSignal) share a Solidity identifier and must never be labelled interchangeably.

The 21 hand-drawn Story SVGs carry prose rather than enum labels, so they hold no vocabulary surface. `bun run check:ontology` guards the code layers only — it parses neither Markdown nor images, so this index and `diagrams.md` are the manual leg of that contract.

## Truth sources

Audience assets simplify, never contradict, the engineering diagrams in `diagrams.md`: money map ↔ D18 + D19 + `settlement-spec.md`; roles strip ↔ D4 capability summary and permission table exact permissions; settlement states ↔ D22 + **D23** (the 5-stored → 9-rendered derivation) + D21.0/D21.1/D21.2; loop ↔ D5.0 overview and D10.0 overview. If a spec changes, regenerate the affected asset in the same pass.

**Status vocabulary across the two tabs**: the story assets label *actions* Built or Planned; the Architecture tab labels *components*, using three treatments — Built/live, Planned/gated, and **existing surface with a planned delta** (`diagrams.md` § Visual status contract). A live surface carrying a planned action is the third class, which is why the client PWA, editorial website, Admin, and the Envio read model read as Built in the story tab and as an existing-surface delta in D1/D2 without contradiction.

## Optional: product screenshots as Built-proof

`docs/static/img/screenshots/` holds ~20 shipped-surface captures (`client-work-offline.png`, `client-garden-home.png`, `admin-work-queue.png`, …). They may be inserted into the brief's PDF export as Built evidence — with a version/date caption per the docs screenshot rules. Decide at upload time; not wired into any document automatically.

## Regeneration

**After editing any asset, rebuild the gallery** so the shared link reflects it. There are **two steps, and the second is not optional for publishing**:

```bash
bun .plans/active/commitment-pooling/visual-assets-prerender.ts
```

That script runs the builder read-only and then freezes the result, producing all three outputs:

1. `visual-assets-artifact.build.ts` writes the two build targets — four audience tabs: story (narrative opener + **21 Story sections**, including the three 2026-08-04 compatibility assets); **30 Architecture sections** (the hand-written opener plus 29 named D-sections) rendered as **42 Architecture Mermaid blocks**; numeric order is the reading order (D25 splits creation/lifecycle from settlement/offline recovery; D13 sits with the promise lifecycle; D27 covers Offer-over-time capacity; D28–D29 close future adaptability; every diagram carries an auto-derived colour key, every ERD a cardinality key, and every mixed-arrow diagram an arrow-meaning key); the canonical CP screen states, reconciled to the hi-fi registry with per-frame deep links (six frames carry an *includes wireframe-only detail* tag); and Reference with the 36-asset coverage matrix, the un-numbered permission table, and the audited Open-questions findings panel, which now closes the tab. The Screens flow contributes the 43rd Mermaid block overall.
   - `LOCAL_OUT` → `/tmp/cp-visual-local.DO-NOT-PUBLISH.html` — the complete local `file://` preview with the locked Mermaid runtime embedded. **This is the file to open for visual validation.**
   - `ARTIFACT_OUT` → `/tmp/cp-visual-artifact-body.DO-NOT-PUBLISH.html` — body content that still contains `<pre class="mermaid">`.
2. The prerender step renders every block through the already-installed Playwright chromium (light + dark), splices the resulting inline `<svg>` into the body, and writes `SHAREABLE_OUT` → `/tmp/cp-visual-shareable.PUBLISH-THIS.html`.

**Publish `SHAREABLE_OUT`, not `ARTIFACT_OUT`.** An artifact containing `<pre class="mermaid">` cannot be shared publicly — the host renders Mermaid at view time and unauthenticated viewers can't invoke that renderer, so the share is refused with "This version can't be shared publicly" (confirmed empirically 2026-07-22). The frozen body has no `<pre class="mermaid">`, no embedded runtime, and no external script references; the prerender asserts all three before writing.

### The one thing that keeps going wrong

Publishing the wrong file is the **only recurring defect in this pipeline** — 2026-07-22, 2026-07-31, and again 2026-08-01. It is worth knowing by shape, because it does not present as a publishing mistake:

> **Symptom**: the reader is told the latest version can't be viewed, or the share/version picker refuses. **`diagrams.md` is completely healthy.** It reads like a Mermaid rendering regression, so the hunt starts in the source and finds nothing.

**Diagnose from the published side first.** Fetch the artifact URL (WebFetch saves the full HTML locally) and grep that file — `<pre class="mermaid">` present and `dia-frozen` absent means the wrong build is live and the fix is a republish, not a source edit. A healthy published body greps as `<pre class="mermaid">`=0, `dia-frozen`=164, `<svg>`=107.

Three guards now sit in the way, because bold prose here and a console warning in the builder both failed to stop it:

1. **The filenames say so.** The two unpublishable outputs carry `DO-NOT-PUBLISH`; the publishable one carries `PUBLISH-THIS`. The path string is the last thing read before it becomes the Artifact tool's `file_path`.
2. **A sentinel comment inside each body**, which survives renames and `*_OUT` env overrides. The builder stamps `GG-GALLERY-BUILD unfrozen`; the prerender swaps it for `GG-GALLERY-BUILD shareable` at the moment the body actually becomes publishable. If the two constants ever drift, the prerender hard-fails instead of quietly disarming the guard.
3. **A one-command verdict**, which the prerender also runs against its own output on disk, so the check that clears a file is the check that produced it:

```bash
bun .plans/active/commitment-pooling/visual-assets-prerender.ts --verify /tmp/cp-visual-shareable.PUBLISH-THIS.html
```

It prints `✅ SAFE TO PUBLISH` or `⛔ DO NOT PUBLISH` with the specific reasons, and exits non-zero on the latter. **Run it on the exact path you are about to publish.**

The builder hard-fails on missing or duplicate required D sections, Mermaid-count drift, a diagram-bearing section losing its diagram, a reading-guide section losing its panel, and any nav link that does not resolve to a section or sub-block anchor. The prerender additionally fails if any diagram does not render to SVG — its `render status: … (rendered N, failed 0)` line is the parse proof. `LOCAL_OUT`, `ARTIFACT_OUT`, and `SHAREABLE_OUT` may all be overridden by environment variable. W6 and the dissolved lo-fi variants are not standalone frames. Republish only after validating the local preview.

The per-asset SVG/PNG steps:

1. Edit the SVG directly (they are plain hand-written XML).
2. Validate: `python3 -c "import xml.etree.ElementTree as ET; ET.parse('<file>.svg')"`.
   **Never land an SVG edit without step 3 in the same commit.** The PNG is what gets uploaded to
   Linear and the Google Doc; the SVG is read by the gallery build but by no human reader. An
   SVG-only commit therefore publishes nothing while looking done, and the drift is invisible in
   review because the diff shows the corrected text. This has bitten once already — see § Asset
   status, 2026-08-01/02. Audit with:
   `for f in *.svg; do [ "$f" -nt "${f%.svg}.png" ] && echo "STALE: ${f%.svg}"; done`
   (identical mtimes are in-sync; confirm a hit with `git log -1 -- <file>.svg` versus `.png`).
3. PNG at 2x: `qlmanage -t -s <2xW> <file>.svg -o <dir>`, then rename `<file>.svg.png` → `<file>.png`. With square canvases this yields the exact 2x size with no cropping. (Headless Brave hangs on this machine with both `--headless` flags; qlmanage slice-crops wide-landscape SVGs — hence the square-canvas convention above.) **Non-square exception** (`artifacts/visuals/external-brief-loop.svg`, 1.3:1): qlmanage slice-crops even at 1.3:1 — wrap the SVG content in a temporary square 1800×1800 canvas (`<g transform="translate(0,105)">` for the 690-tall viewBox), render the square, then `sips -c 1380 1800` center-crops back to the exact 1800×1380 export (verified 2026-07-18).
4. Check dimensions: `sips -g pixelWidth -g pixelHeight <file>.png`.
