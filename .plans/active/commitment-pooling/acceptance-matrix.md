# Commitment Pooling — Cross-Surface Acceptance Matrix

**Status**: canonical execution companion  
**Updated**: 2026-08-02
**Sources**: `standing-commitments-spec.md`, `contract-spec.md`, `settlement-spec.md`, `pilot-evidence-spec.md`, `uiux-spec.md`, `wireframes.md`
**Purpose**: one exact target for the handoffs' copy/state matrix, public claim/copy matrix, and final QA acceptance matrix. Specs win if this summary drifts.

## 1. State and copy matrix

| Condition | Client PWA | Admin | Editorial / docs | Required exit |
|---|---|---|---|---|
| `None` / `UNKNOWN` sentinel | Loading, not-found, or retry; never a state chip | Recovery row; never a selectable state | No claim | Creation event or explicit not-found |
| Pool NotReady | Pool absent | Checklist: charter, non-zero provider open-commitment cap, qualifying Baseline assessment | Planned/readiness only | App enables `markPoolReady` only after all three; the contract enforces charter + non-zero count cap, while current non-revoked Baseline remains an app/shared/admin preflight |
| Pool Paused | “This pool is paused” + reason; browse, evidence/linkage, and recovery stay available, while claim/decline, acceptance, exchange, Ready submission/override, and confirmation actions are absent | Register #103 full freeze: all creation plus claim/decline, acceptance, exchange, Ready submission/override, and both confirmation paths revert `PoolNotInState`; evidence/linkage, roster-safe wind-down, cancellation/expiry/dispute recovery, and the non-blocking Work-decision hook remain available. Paused `registerPool` backfill is an owner/steward release operation, not a member action | Neutral quiet-period copy | Resume event clears reason; no blocked mutation survived the pause |
| Ongoing Offer Active | Series detail shows finite available places, Add places, Rest, Retire, and Story | Group linked instances by internal series; no steward lifecycle mutation | Pool-level aggregates only | Holder may add pre-created Offer instances, rest, or retire |
| Ongoing Offer Resting | No new-place action; Story and existing instance links remain | State/context only; existing Offered and Accepted instances keep their own actions | No person-level claim | Holder resumes or retires |
| Ongoing Offer Retired | Terminal context; Story remains; no new places | Read-only terminal context | No person-level claim | None |
| Standing Offer availability | Only synced Offered instances whose provider capacity is already reserved are counted; claim accepts exactly one | Available count equals current Offered instances, not series metadata or a requested quantity | Approved pool-level count only | Claim, cancel, or expiry removes that place |
| Pooling trust-root configuration | No public UI | Pooling module initializes paused; register initialized with module zero is non-operational until one-time wiring; module dependency/schema setters require pause and emit old/new facts; the onchain unpause guard requires six non-zero dependencies plus four non-zero, pairwise-distinct UIDs; release sequencing additionally keeps pooling paused until GardenToken and WorkApprovalResolver upgrades establish and verify both reverse links; the frozen module interface exposes `paused()` because register replacement after initial wiring calls it and requires the current module paused | No UI copy | Interface/implementation ABI proof plus zero/collision/unpaused/incomplete-unpause/max-plus-one tests fail before storage or events; transaction-plan proof rejects unpause before both reverse links and all chain-2/chain-3 readiness facts |
| Named confirmer bound | Confirmer picker never submits more than the ABI-reported `MAX_CONFIRMERS` | Admin shows the ABI-reported limit and validates before write | “Choose up to the current confirmer limit” | First contracts PR records the 8/16/24/32 benchmark and freezes the value; selected-bound max-plus-one rejects before class/commitment mutation; duplicate/contributor filtering remains threshold-safe |
| Confirmation reachability | Creation/review explicitly offers “Green Goods team fallback” only as a selected safety path; contributor copy explains why it may be needed | Pre-acceptance confirmer editor shows ordinary reachability, local fallback, selected protocol fallback, and the registered protocol-garden prerequisite | No person-level claim | An ordinary-unreachable rule blocks when unselected; the selected path may reach Ready after `protocolPoolId` exists; no contributor can confirm |
| Confirmation provenance | Ordinary confirmation names its confirmer; local fallback says “confirmed by garden steward — fallback”; protocol fallback says “confirmed by Green Goods team — fallback” | Queue/detail show the emitted actor, exact path, and mandatory fallback reason without inferring from wallet role | Aggregate confirmed count only | `CommitmentFulfilled.confirmer`, `confirmationPath`, and `reason`; `DisputeResolved -> Fulfilled` is not mislabeled as a confirmation |
| Claim Pending | Canonical claimant + “requested by” actor + provider context | Same stored row; accept/decline key canonical claimant | Counts only | Accept, decline, supersede, cancel, or expire |
| Claim Declined | Reason + fresh-request CTA | Selected row only | Not public | New request, never retry old row |
| Claim Superseded | Taken/no-longer-available copy; no retry | Indexed terminal row | Not public | Browse exit |
| Exchange proposed | Pair chip names the other Offer and reads “in exchange for”; no acceptance CTA unless the viewer created A | Same reference and eligibility facts | Aggregate counts only | A creator opens W30 or either Offer leaves `Offered` |
| Exchange matched | “Both promises started together; each is kept on its own” + the two ordinary commitment timelines | `ExchangeAccepted` marker beside the two ordinary `CommitmentAccepted` records | Neutral exchange-feed row | Each commitment proceeds independently |
| Exchange counterpart lapsed | The surviving commitment keeps its own state; pair status explains that its counterpart lapsed | No transition action on the surviving side | Not public as a person-level fact | None — S5 boundary: counterpart cancellation or expiry never transitions the other commitment |
| Team forming | Lead + active contributors + join/approval policy; requirement assignments are planning only | Roster editor, eligibility, contribution credit, and confirmer reachability | Counts/roles only | Add/remove/join events |
| Contributor roster and credit ledger frozen | “Team locked for confirmation” + who is credited | No roster edits or new contribution credit; contributor exclusion is visible beside confirmer rule | Recognition roster may be reported | ReadyForConfirmation transition |
| Evidence-only Accepted | Evidence + declared assessment requirements | Attach assessment; submit Ready; override separate | Active counts only | Evidence, assessment when declared, submit Ready |
| DomainImpact Accepted | Repeatable per-action Work/approval progress; no manual Ready-submit | Requirement rows may repeat domains; Add requirement is not capped at four in UX; override separate | Active counts only | Every requirement `approvedCount >= requiredCount` + assessment when declared |
| Payout-plan draft | Recognition and proposed payment shown side by side; garden-retained amount explicit | Steward can reopen and atomically resubmit the complete vector/retention/reason before finalization; invariant and derived recipient accounts visible | No “paid” claim | Exact declared = retained + contributor total |
| Settlement queued | “Support on its way” | Dispatch action + fee quote/reserve | Planned, not sent | Successful command dispatch |
| Command dispatched | “Support on its way” | Command message ID, elapsed time, fee/route health | Sent, never received | Indexed Celo execution or authenticated failure acknowledgment |
| Executed; acknowledgment pending | “Support on its way” | Celo execution + retry acknowledgment guidance | Confirming, never received | Authenticated success acknowledgment |
| Confirmed settlement | “Support arrived” | Command, Celo execution, acknowledgment, Celo ref | Confirmed | Authenticated current success acknowledgment |
| Payout partially complete | Per-contributor “on its way” / “arrived” receipts; never one blanket success | Parent status derived from child disbursements; failed contributor requeues independently | “Partially distributed” | Every non-zero child terminal/confirmed |
| Payout complete | Every non-zero contributor row arrived; retained amount stays in garden | Recognition/payment comparison and child receipts preserved | “Distributed” with retained amount | Every non-zero child Confirmed |
| Authenticated execution failure | “Support is being rearranged” plus a calm action explanation, never the internal state noun and never a success phrase | Bounded failure code + per-member new-attempt/cancel controls | Failed | Explicit reconciliation |
| Cancelled from Queued | “Support was withdrawn before it was sent” | Cancellation reason + Queued origin | Cancelled before send | Terminal; no execution key |
| Cancelled from Failed | “Support was closed after delivery could not complete” | Failed attempt/code + cancellation reason/origin | Cancelled after authenticated failure | Terminal; no new execution key |
| Queued batch cancelled | Each member uses Cancelled-from-Queued copy | One whole-batch action, reason, immutable member list, and blast radius; no member-level cancel | Batch and every member Cancelled before send | Atomic terminal transition; no partial queued-batch state |
| Delivery/fee delay | “Support on its way”; no transport-state vocabulary | Retry same command or stored acknowledgment; manual-execution guidance | Delivery delayed, not payment failure | Delivery/fee recovery |
| Member delivery disabled | No G$ balance/send or first contributor-child preparation CTA; fulfilled commitment, payout plan, retention, unprepared rows, and historical children remain visible | Gate reason; the separate treasury funding route remains | Explicit delivery-blocked copy with no retry for an unprepared row | AA/paymaster exit evidence + enabled event |

## 2. Identity, permissions, and payout formulas

| Contract fact | Canonical formula | UI / test assertion |
|---|---|---|
| Claim type | runtime `kind == stored claimType` | Mismatch is a contract error, not eligibility copy |
| Individual claim | `claimant = requestedBy = msg.sender`; counterparty = caller | One identity row |
| Garden claim | `claimant = gardenContext`; `requestedBy = authenticated operator`; counterparty = GardenAccount; neither claimant nor requestedBy may equal creator, and ApprovalGated acceptance rechecks the stored requester | Show both GardenAccount and requester; creator-operated Garden claims are disabled/rejected in both modes |
| Lead provider | Offer → creator and slot reserved at creation; Individual Request → accepted counterparty; Garden-claimed Request → authenticated Open `claimCommitment` caller or consumed ApprovalGated pending claim's stored `requestedBy`, while `providerGarden = gardenContext` | One accountable human lead in both claim modes; lead alone consumes the register slot; GardenAccount remains counterparty/provider scope |
| Commitment series authority | non-zero `commitmentSeriesId` resolves to an Active same-pool module-owned series whose current holder is the direct Individual Offer creator; zero means one-shot | Unknown, wrong-pool, Resting/Retired, Request, Garden, and `onBehalfOf` attachment paths fail before instance/class mutation |
| Saved Offer persistence | reusable Offer metadata is signed offchain and private by default; `LOCAL_DRAFT` → `SAVING_REMOTE` reaches `SAVED_REMOTE` only after a confirmed owner-authenticated response, while failure/offline/conflict retain `SAVE_FAILED`, `OFFLINE_LOCAL`, or `VERSION_CONFLICT` truth | Device loss does not erase confirmed saved metadata; local, in-flight, failed, offline, and unknown results never display Saved/Synced or cross-device durability |
| Standing Story | current linked-instance outcome counts plus unique non-zero fulfilled-cycle IDs | “Kept N times across M cycles” is exact; no score, rate, rank, inferred participant count, cross-pool merge, or protocol permission input |
| Contributor roster | resolved lead is revalidated against the current `providerGarden` membership predicate before register/roster mutation; Open permits eligible self-join/self-leave and no managed expulsion; LeadManaged alone permits lead/steward add/remove before freeze, and every added address must pass the same predicate; lead, credited contributors, and contributors with uncounted linked Work cannot leave or be removed; measured `MAX_CONTRIBUTORS_PER_COMMITMENT` bounds the end-to-end vectors | A creator who lost their Hat or an ineligible StewardCaptured member cannot become the lead; arbitrary external addresses cannot enter recognition/payment; provisional cap 32 is a transaction-safety bound, not a four-person rule; every frozen contributor is excluded from confirmation |
| Work linker | active contributor, lead provider, or steward; Work attester must be active; DomainImpact link names the exact requirement index; a non-zero caller-scoped `operationKey` is persisted before send; link/unlink require Accepted plus unfrozen, and unlink requires current inactive credit | Exact payload replay is a no-op even after a later unlink; conflicting key reuse reverts. Catch-up preflights resolver decisions, and link lifecycle uses an independent block/log cursor so reverse Link/Unlink cannot regress |
| Evidence attribution | exact CID de-duplicated per commitment; queued payload persists a non-empty, unique, bounded active-roster address vector through retry; attachment must land while Accepted and unfrozen; each contributor receives at most one evidence-derived recognition credit per commitment | Distinct evidence remains visible provenance without multiplying recognition; a late queued attachment fails without partial writes; recognition eligibility additionally requires the commitment to be Fulfilled |
| Assessment attachment | one-time non-zero v2/v3 UID while Accepted and unfrozen; recipient equals immutable `providerGarden` | Replacement or post-readiness attachment reverts before lineage changes; assessment-first and approval-first orders still reach Ready |
| Direct dispute fulfillment | steward-only terminal resolution with opened/cycle-less recognition policy and pre-freeze verified credit | A current or frozen contributor-steward reverts `SelfConfirmation`; roster/credit ledger freezes before `DisputeResolved(finalState = Fulfilled)` when not already Ready; indexer applies the same Fulfilled history, attribution, Need-lineage, pool/cycle, and live-count projection as ordinary fulfillment |
| Protocol root + confirmation fallback | initializer fixes a non-zero canonical `rootGarden`; Protocol registration requires that exact GardenAccount before writing the pool or write-once `protocolPoolId`; pre-acceptance `protocolFallbackEnabled`; ordinary reachability evaluated first; fallback caller is a current local pool steward/owner (`PoolFallback`) or, only when selected, a current protocol-pool steward/owner (`ProtocolFallback`); local wins for dual-role callers; module owner alone is ineligible | Wrong-root Protocol registration reverts before any pool/protocol ID mutation. Small-garden fixture with the creator/lead on the contributor roster has no eligible local confirmer, explicitly selects Green Goods team, reaches Ready, and is confirmed by a non-contributor protocol steward; flag-off, contributor, module-owner-only, and missing-protocol-pool cases fail closed; UI renders actor/path/reason distinctly |
| Cycle terminal transitions | `closeCycle` and `cancelCycle` require `liveCommitmentCount == 0`; creation increments and the cursor-ordered lifecycle helper applies every live-to-Fulfilled/Cancelled/Expired decrement, including terminal `DisputeResolved`; Expired-to-Disputed re-increments and its RestorePrevious(Expired) or Cancelled resolution decrements once | Offered/Requested commitments are included; an Offered Offer already contributes provider exposure while an unaccepted Request does not. A lifecycle event delivered before creation remains in the typed commitment-keyed projection buffer and consumes no cursor/delta until creation facts arrive; the atomic drain, duplicate/reverse delivery, and `Expired -> Disputed -> Cancelled` preserve one current outcome and exact counts. W26 reads liveCommitmentCount. |
| Pool terminal transition | `closePool` requires `Pool.liveCommitmentCount == 0` and `Pool.nonTerminalCycleCount == 0`; every commitment is Fulfilled/Cancelled/Expired and every cycle is Cancelled/Composted before the write | Open/Paused UI exposes wind-down rows while either counter is non-zero, no close call is reachable, and the contract rejects stale preflight races |
| Claim/contributor reverse delivery | request rows, contributor membership, requirement assignment, and Work link membership each own independent position cursors; set-like relationship indexes are unique and deterministically sorted | Acceptance/terminal before older ClaimRequested never revives Pending; Remove-before-older-Add stays removed; assignment and Work Link/Unlink permutations converge without corrupting the frozen roster |
| Terminal member-history replay | Acceptance facts are stored independently of the lifecycle cursor; nullable `memberHistoryOutcome` owns the reversible lead bucket; Fulfilled participant history waits for `acceptanceSeen`, the frozen roster count, and the exact bounded active contributor rows before its once-only guard flips | Fulfilled before Accepted/ContributorAdded/ContributorRosterFrozen, freeze before contributor rows, and Cancelled/Expired before Accepted all converge; duplicate delivery produces no second member-history delta |
| Pre-accept cancellation | creator or steward | Both paths tested |
| Accepted cancellation | steward only | Member control hidden |
| G$ beneficiary | a Request claimed through `ClaimType.Garden` pays exactly one recipient, the external claiming garden's active registered Celo Safe; every other case pays each frozen eligible contributor's derived same-address Celo account | Garden-pool institutional claims and protocol-garden self claims revert before pending/acceptance; beneficiary Safe path is not AA/gardener-delivery gated; contributor path is; arbitrary recipient input impossible; an Offer never pays its own claimant |
| Commitment-consideration source | `ArbitrumExternal` declares a non-zero payer/token; `CeloSettlement` declares zero source/token sentinels, then plan creation derives/stores SettlementModule's write-once `gDollarToken` and the registered Celo Safe for the commitment's immutable `payerGarden` | Pooling has no independent canonical-G$ authority; the **asking** side pays, so a protocol-pool Request spends the protocol Safe and a protocol-pool Offer spends the claiming garden's Safe, while garden-internal commitments are unchanged; ProtocolToGarden remains a discretionary top-up and is no longer how the protocol pays for commissioned work; never send to Arbitrum GardenAccount |
| Recognition authority | CommitmentPooling recomputes the complete sorted vector/hash from frozen eligible count, effective on-chain credits, and immutable opened-cycle policy or cycle-less 20/80 preset | Equal-policy and verified-policy bps each use an independent deterministic remainder pass before their row results are added; integer allowlist units use a separate largest-fractional-remainder pass that conserves each commitment budget; Settlement always uses the validator; Hypercert composition uses it only after rejecting `cycleId == 0`; a steward-supplied self-consistent split is insufficient |
| Payout authority | create/edit/finalize/prepare resolve operator/owner stewardship from immutable non-zero `payerGarden`; its account remains Active for every new value authorization. `GardenBeneficiary` also freezes and rechecks the external beneficiary garden's active registered Safe at creation/finalization/first preparation/batch/dispatch | Root-pool steward cannot authorize a claimant garden Safe; missing/zero/inactive identities fail closed without erasing reads, acknowledgments, cancellations, exact-child returns, or same-key retries |
| Immutable payout shape | Request + Garden claim derives `GardenBeneficiary` with one Safe, full amount, and no contributor rows; every other shape derives `ContributorConsideration` | Beneficiary cannot enter `setContributorPayouts`, contributor plan cannot gain beneficiary fields, and no edit crosses shape |
| Garden retention | contributor shape: `declared consideration = gardenRetainedAmount + Σ contributor payouts`, with retention allowed only when payer = provider; beneficiary shape: zero retention/contributor total and `declared = beneficiaryAmount` | Retained amount remains explicit garden-internal accounting, never a cross-garden withholding or fake Safe-to-self transfer |
| Recognition/payment default | complete sorted recognition vector matches its snapshot hash; initial amounts use deterministic full-consideration base-unit apportionment; payment weights derive from amounts | Canonical base-unit rounding is labeled matching and needs no reason; any noncanonical Draft amount/retention divergence needs a reason and visible before/after comparison; explicit finalization freezes before dispatch |
| Payout snapshot observability | plan persists immutable ascending contributor order; creation starts version 1; every full edit increments once and emits all version-tagged rows plus one trailing summary with row count, retention, total, and payment hash | Hash preimage is the exact ordered immutable `{ contributor, recipient, recognitionWeightBps, paymentWeightBps, amount }` tuple plus chain, plan, version, retention, and contributor total; mutable child fields are excluded; indexer buffers and atomically replaces only after the summary matches |
| Payout-plan state | Draft/Pending/Partial/Complete/Failed derives from explicit finalization, general payable-payout counters, and every commitment-bound child | Idempotent contributor or beneficiary preparation creates one Queued child; only a zero-payable garden-internal contributor plan completes locally; beneficiary requires Confirmed child; acknowledge/fail/requeue/cancel update identical parent counters and preserve pointers |
| Consideration-rail exclusivity | `ArbitrumExternal` may use core `recordConsiderationPaid`; `CeloSettlement` may use SettlementModule; `None` has zero consideration fields | A commitment cannot record both rail outcomes; Celo queue rejects the external rail and core payout recording rejects the Celo rail |
| Canonical funding config | initializer-locked `protocolGarden` + canonical Celo G$; protocol Safe to garden Safe value is queued only as `Funding` / `ProtocolToGarden` through `queueFunding` | No post-initialization setter; Celo router upgrade must preserve immutable G$; no garden-beneficiary commitment-consideration alias |
| Funding UI authority | Operations route visible with any of `isDeployer`, `canQueueFunding`, or `canOperateSettlement`; `queueFunding` submit visible only to protocol steward or SettlementModule owner | Deployer alone cannot submit; cancel creates no entity; success renders the emitted Queued Funding/ProtocolToGarden row with no commitment ID |
| Offline queue boundary | exactly commitmentSeries, commitment, claim, evidence, workLink, confirmation | Series uses `clientSeriesId`; every Commitment/place uses `clientCommitmentId` plus a creator-scoped creation key; Work link uses a caller operation key. Recovery reads through and reuses the same key, validates the full immutable payload, and cannot duplicate a series, promise, reservation, or link. No settlement/funding job, per-device attempt, or permissionless `queueDisbursement(commitmentId)` |
| Settlement trust-root configuration | both source and executor initialize paused; source dependency changes require pause and emit old/new; source unpause requires complete route, active protocol account, and fee floor; executor unpause requires source peer, caps, period policy, and reserve floor | Incomplete or unpaused configuration fails before authority or dispatch changes; indexed configuration shows the active dependency addresses |
| Assessment v3 placement | `assessmentV3` is one additive schema UID on the existing upgraded `AssessmentResolver` proxy | The proxy upgrade uses only `upgrade.ts assessment-resolver`; every tx-plan has an explicit sender verified against live `owner()`; registration computes the deterministic EAS UID and either registers an empty record or reconciles an exact existing record after partial persistence failure; read-before-set skips the exact UID and rejects a conflicting non-zero UID; v2 UID/address/owner/state stay preserved; no `AssessmentV3Resolver` contract or resolver artifact key; live `42161 schemaUID() == 0` is remediated from the verified v2 artifact before v3 activation; the `421614` rehearsal deploys/pins current v2 before upgrading |
| Community Testimony activation | preparation one-way pins the deterministic exact UID while module is zero; named `--finalize-community-testimony` reconciles the exact EAS record and sets the verified module last | no zero-UID wildcard; UID zero/conflict rejects and exact repeat is a no-op; module zero or module-before-UID rejects; permissionless pre-registration is accepted only when the record is already exact and remains inactive; deployment retries reuse only exact predicted implementation/proxy code, ERC-1967 implementation, owner/EAS/state and ordered recovery state; finalization accepts no address override, rejects module-before-record state, and no attestation succeeds until exact record proof is followed by module activation |
| Canonical G$ amount | queued `amount` is exact net recipient receipt; zero-fee and sender-pays are supported; non-zero receiver-pays fails closed | quote `getFees`; enforce absolute and bps fee limits plus gross Safe-debit caps; exact source/recipient balance deltas; forbid source-as-recipient/duplicates; no guessed gross-up |
| Celo member AA gate | production remains Kernel `0.3.1`, which Pimlico supports on Arbitrum One and Celo Mainnet but not Celo Sepolia; testnets use the supported Kernel `0.2.4` profile on both `421614` and `11142220` | the same-address Kernel `0.2.4` sponsored surrogate transfer proves testnet mechanics but never enables delivery; enablement requires exact Kernel `0.3.1` Arbitrum One/Celo Mainnet derivation, code/policy/passkey proof, and one separately authorized included sponsored Celo Mainnet first-use canonical-G$ transfer with receipts, EntryPoint event, deployed code, and exact balance deltas recorded without secrets |
| Safe/Zodiac topology | deterministic Safe v1.4.1 proxy + one Roles Modifier + native `WithinAllowance(allowanceKey)` | no separate Allowance Module; minimal ABI hand-declared locally; immutable permissions hash excludes mutable caps/allowance balances; persist factory/singleton/initializer/salt plus recovery and permission hashes; allowed/denied live probes |
| Garden account Safe-owner spike | pinned Arbitrum/Celo forks use the canonical ERC-6551 registry and real Safe v1.4.1 factory/singleton; foreign Garden account + one nested recovery Safe executes, either alone fails, both nested recovery Safes recover through EIP-1271, wrong Garden/untrusted/replay fail | mechanics do not change the pilot owner set; exact same-address GardenAccount deployment and a Garden-bound authenticated cross-chain relay remain separate blocking gates |
| Temporary Garden Safe bootstrap | absent Safe; already bootstrapped; ready-to-swap; already swapped; unexpected owner/threshold/module/native-or-canonical-G$ balance/replacement; interrupted batch | derive all 18 from the reviewed Garden inventory; deploy exact native/G$-clear 1-of-2 EOA + live 2-of-3 recovery-Safe proxies; explicitly record that arbitrary ERC-20/ERC-721/ERC-1155 inventory is not enumerated; atomically checkpoint each receipt; swap only the EOA through the live predecessor; verify zero configured value authority and exact post-state; resume without replay |
| Delegated dispatch | exact optional `dispatcher` address | Dispatch/retry only; never queue, requeue, cancel, or configure |
| Fee reserve | Arbitrum command sends always spend the module reserve; Celo acknowledgment sends identify `reserveFunded`; every reserve-funded send and withdrawal leaves native balance `>= configured minimum` | Floor, live balance, low state, funding, source fee spend, Celo sponsored fee spend, and caller-funded Celo retry are distinguishable and observable |
| Batch safety | configured limit is 0–24; zero disables batch commands only | Both chains match before enable and can return to zero; unbatched commands require one recipient and remain governed by non-zero transfer/aggregate/period caps |
| Batch command shape | every member shares executor garden, source, token, kind, and funding route; recipients unique. Both commitment kinds recheck payer; GardenBeneficiary also rechecks each beneficiary account/frozen Safe; Funding rechecks protocol source and every target account | Mixed kinds/routes, duplicate recipient, inactive source/beneficiary/target, or beneficiary Safe mismatch reverts before fee quote/mutation; contributor teams may split across batches |
| Execution-key domain | `keccak256(abi.encode(sourceChainSelector, sourceSettlementModule, isBatch, settlementId, attempt))` | A disbursement and batch with the same numeric ID/attempt produce different keys; command tuple carries the same authenticated `isBatch` value |
| Source selector identity | Arbitrum implementation constructor immutably stores the official local CCIP selector; Celo recomputes from authenticated `message.sourceChainSelector` | Both sides derive the same key without substituting `block.chainid`; upgrade verifier rejects a source-selector change |
| Remote EVM chain identity | verified generated `SettlementConfiguration.remoteEvmChainId` is required for each exact supported source/executor lane independently of its CCIP selector and remains null for independent component rehearsals | Production source `42161` uses executor `42220` and production executor `42220` uses source `42161`; `421614` and `11142220` component rows never claim a peer pairing or route readiness without a freshly published exact lane/router; Celo route/execution/message relationships fail closed when the field is null and never translate selectors or use the local Celo chain as Garden identity |
| Command destination binding | initial dispatch snapshots destination selector/executor/gas/version/payload hash; retry and acknowledgment validation use that snapshot | Peer rotation cannot reroute the same key to a replacement executor, and a globally accepted previous peer cannot acknowledge a command that was sent to the new peer |
| Acknowledgment origin binding | `originatingCommandMessageId` maps to the same execution key through the initial/retry message registry | An out-of-order acknowledgment may name any known send for that key, but cannot import an unrelated or invented command ID |
| Batch cancellation | `Queued batch -> cancelBatch(batchId, reasonCID)` atomically cancels the immutable member set; `cancelDisbursement` rejects queued members with `batchId != 0` | UI exposes one reasoned whole-batch action; indexer replay can never show a partially cancelled Queued batch |
| DomainImpact requirement shape | repeatable `{ actionUID, requiredCount }` rows; 1–`MAX_REQUIREMENTS`; domains derived from ActionRegistry and may repeat | UI starts with four visible rows plus Add requirement; contract benchmark compares 8/16/24/32 before freezing the provisional 16 bound |
| Work approval credit | `requirementIndex` selects exactly one requirement; Work action matches and Work attester is an active contributor; greatest resolver-assigned sequence selects the effective pre-freeze decision | Approval activates and a newer rejection reverses exactly one requirement/contributor credit; same-block/out-of-order sync converges, repeated same-state decisions do not double-mutate, and a pre-upgrade sequence-zero decision requires re-attestation |
| DomainImpact progress | `approvedUnits = floor(targetUnits * Σ_i min(requirement.approvedCount, requirement.requiredCount) / Σ_i requirement.requiredCount)` | Per-action counts and per-commitment units remain visible; pool/cycle totals never mix labels |
| Hypercert gardener recognition | gardener class divides equally across fulfilled commitments from one non-zero, on-chain Reconciled cycle; within each, the opened cycle policy allocates equal participation and verified contribution, then expands into exact integer units; cycle-less uses the immutable protocol preset for recognition/payment only | The shared composer used by W26 and `/hub/certify/create` rejects every non-Reconciled cycle before metadata/allowlist work; commitment-level bps stay stable while integer units are certificate-scoped |
| Lead-provider concurrency cap | non-empty/non-zero class creation plus a non-zero count cap and `Registered -> Committed -> Released\|Fulfilled`; Offer calls `commitUnits` at creation, Request calls it at acceptance; only the lead provider is the register account | Every displayed Offer place is already capacity-backed; Offer acceptance and `acceptExchange` do not recommit; unaccepted Offer cancel/expiry releases, unaccepted Request cancel/expiry does nothing; adding contributors never consumes extra slots |
| Admin past-due expiry | A due date/cycle end does not transition state by itself. `expireCommitment` is permissionless and moves an eligible live commitment to Expired, releases an Offer/accepted reservation once, supersedes pending claims, and decrements pool/cycle live counts once | `W7@due-live` retains the live row and **Expire now** until transaction success; only then does `W7@expiry-queue` show Expired plus re-seed/history. A failed send never renders the terminal result |
| Register authority audit | `ModuleUpdated(oldModule, newModule)` is global to the register, not a pool transition | One pool-less `CommitmentEvent` sets `configurationKey = null` and stores the normalized old/new module addresses in generic `previousValue`/`newValue`; pool/cycle/commitment relations stay null, no pool `0` sentinel is invented, no actor comes from `transaction.from`, and no accounting row changes |
| Exact-label unit summaries | ID uses chain + POOL/CYCLE scope + scope ID + `viem.keccak256(viem.stringToBytes(unitLabel))` over the exact stored UTF-8 string | Indexer declares direct `viem@2.55.0` dependency matching the repo pin; two `hours` commitments share a row; `Hours` is distinct; composed/decomposed Unicode remains byte-distinct; replay changes neither row |
| Cross-commitment rate | `promiseKeptRate = commitmentsFulfilled / commitmentsDue` | Sole aggregate percentage; active progress uses state counts and exact-label groups |

### 2.1 Bilateral exchange and template-first acceptance

| Case | Expected contract/read-model result | Surface assertion |
|---|---|---|
| Offer×Offer success | B's direct creator submits B with existing same-pool A. Before allocating/storing B or registering its class, `createCommitment` atomically requires direct Individual B plus Offered Individual A, distinct creators, and A's exact class/provider reservation; a stale A reverts with no B. A creator later calls `acceptExchange(B)`; both remain `Offered` and reserved. B creator claims A and A creator claims B atomically, while each Offer creator remains that Offer's lead provider. Emit two ordinary `CommitmentAccepted` events, `ContributorAdded(A, A.creator)` and `ContributorAdded(B, B.creator)`, plus self-describing `ExchangeAccepted(A, B, poolId, B.creator, A.creator)`; each ordinary acceptance sweeps every unrelated Pending request for its commitment even when no matching request exists; perform no second registry commit, slot increment, or provider-cap headroom check. One `CommitmentExchange` entity exists, and its existence is the atomic marker | W30 repeats “You'll receive [B]. [B's creator] will receive [A]. Both promises start together; each is kept on its own.” W29 and the pool feed render the pair from creation references plus the marker event |
| Direct B consent | `createCommitment` rejects non-zero `counterCommitmentId` on `StewardCaptured` / non-zero-`onBehalfOf` Offer B; `acceptExchange` repeats the B-kind guard before mutation. A may have been captured earlier only because A's represented creator must personally call acceptance | No steward can bind a represented gardener as B's exchange creator. W28 is available only in a direct creator's Individual Offer flow |
| Declared-value replay | `CommitmentCreated` initializes the value/basis pair only when no `ValueDeclared` cursor exists; each `ValueDeclared` applies only when its `(blockNumber, logIndex)` is later than the nullable stored cursor | Creation→update, update→creation, duplicate delivery, and two updates delivered in either order converge to the same latest pair; zero/empty is treated as a valid value, never an unset sentinel |
| Decline before request delivery | `ClaimDeclined` upserts the claimant-keyed request and index even when `ClaimRequested` has not arrived: `requestSeen = false`, request-only payload is null, and the decline outcome/cursor is terminal. An older later-delivered Request fills payload and sets `requestSeen = true` without reviving Pending; only a genuinely newer request may become Pending | Reversed delivery never resurrects an actionable claim. The row becomes fully readable when the request payload arrives and retains the same Declined reason/outcome |
| Sparse parent and series delivery | Pool, cycle, series, commitment, contributor, Work attribution, and claim rows use explicit base-event seen flags plus nullable base-only payload/cursor fields. A sparse event never invents an enum, address, ID, or zero cursor | Ordinary queries exclude unseen placeholders; handlers can load them by ID. The later base event fills immutable facts without regressing any newer independent cursor or terminal outcome |
| `ExchangeCounterpartMismatch(exchangeCommitmentId)` | B has no counterpart, A does not exist, or A belongs to another pool; no state, event, class accounting, or slot changes | A creator is returned to W29/W30 with the pair marked unavailable and a route back to the pool |
| `ExchangeDirectionInvalid(commitmentIdA, commitmentIdB, directionA, directionB)` | At least one side is not an Offer; no writes | The confirmation action is removed and both actual directions are explained without suggesting an unsupported path |
| `ExchangeStateInvalid(commitmentId, actual)` | Directions are Offer×Offer but either side is not `Offered`; no writes | Pair status shows the current state and the confirmation action remains unavailable |
| `SelfExchange(creator)` | A and B have the same creator; no writes | Creation validation asks the creator to choose another member's Offer |
| `ExchangeClaimTypeUnsupported(commitmentId, actual)` | Either side is not `Individual`; no writes | Garden×garden is labeled a later extension seam and no call is offered |
| `ExchangeCreatorConsentRequired(exchangeCommitmentId)` | B is `StewardCaptured` / on-behalf rather than direct-created; no writes | The pair cannot start; the represented gardener must create a new direct Offer if they want the exchange |
| Existing cycle predicate failure | A or B has `cycleId != 0` and its cycle is not open; the ordinary named cycle error wins and no writes survive | Pair status names the closed season or campaign and offers no retry action |
| Index namespace | Exactly one canonical UUPS Commitment Pooling module/register proxy pair is indexed per chain; implementation upgrades retain those proxy addresses. Duplicate blocks or proxy replacement fail the boundary check until a versioned namespace and full-replay migration exist | Saved Offer links retain `moduleAddress` to reject stale/wrong-proxy recovery; this does not imply two modules can be queried in one chain namespace |
| Existing creator/identity failure | Caller is not A's creator, or an ordinary per-side identity predicate fails; no writes | Only A's creator sees the action; a stale deep link exits to the pair detail |
| Existing register/cap invariant failure | If either already-Committed class, account, quota, provider membership, or current cap predicate is invalid, both acceptance transitions, both ordinary events, and the marker revert; no registry balance or slot changes | One calm failure identifies the side and the steward action needed; the UI never renders a half-matched pair |
| Post-acceptance independence | Fulfillment, cancellation, expiry, and dispute on either side never transition the other; direction-aware confirmation and contributor self-confirmation exclusions apply independently | W29 shows “counterpart lapsed” only as an app/indexer-derived pair status while the unaffected promise keeps its own state and actions |
| Exchange-pair UX states | Proposed derives from B's creation reference; matched derives from `ExchangeAccepted`; counterpart-lapsed derives from either ordinary lifecycle | Picker, review line, pair chip, detail, feed, confirmation, submitted, error, matched, and counterpart-lapsed states match uiux Appendix E.1 and W28–W30 |
| Template-picker creation | W31 precedes the blank Offer/Request form; Rotation, Work party, Harvest share, Tool lending, Mentorship circle, and Exchange circle pre-fill only existing commitment/cycle/campaign/claim/roster/exchange fields | Every default is editable, en/es/pt content uses the §10 key family, and choosing a template creates no new chain state or module call |

### 2.2 Ongoing Offer acceptance

| Case | Expected contract/read-model result | Surface assertion |
|---|---|---|
| Create series | Current pool member creates non-empty metadata in a Ready/Open pool; module emits `CommitmentSeriesCreated`; caller is both immutable creator and initial current holder | Pending state becomes Active only after indexed sync; no available count appears merely because the series exists |
| Add two places | Two ordinary Offer instances reference the Active series; each has its own persisted `clientCommitmentId`/creator-scoped key and independently reserves its class and one provider slot exactly once | Series shows two available only after both creations sync; interrupted retry reuses the same key and cannot create a third hidden place |
| Claim one place | Claim transitions one pre-created Offered instance to Accepted; series link remains unchanged; no new instance or second provider slot is created | Available changes from two to one; user lands on the accepted instance detail |
| Rest with live instances | Series becomes Resting; existing Offered/Accepted instances and Story remain unchanged | Add places disappears; clear copy says existing promises continue |
| Resume | Resting becomes Active with no implicit instance creation | Add places returns; available stays at the indexed Offered count |
| Retire | Active/Resting becomes terminal Retired; existing instances remain unchanged | Confirmation names terminal effect; no new-place or resume control |
| Metadata revision | Holder updates Active/Resting series metadata; prior/open instances preserve their snapshots; metadata and lifecycle handlers use separate cursor pairs so cross-type reverse delivery converges | Story shows prospective current description without rewriting historical terms or regressing Rest/Resume/Retire state |
| Next-cycle posture | Default is Ask me again next cycle; any carry-forward option prepares a draft only | No protocol or background process auto-creates obligations |
| Saved Offer persistence | Confirmed signed metadata survives device change; Saving, failed, offline-local, and version-conflict states retain the local draft without claiming that guarantee | SB-37 passes through Saving before Saved; SB-38 no-signal remains visibly unsaved |
| Series Story | Lifecycle replay converges to exact current counts and unique fulfilled-cycle IDs, including dispute restore paths | Absolute counts only; no score/rate/rank, no inferred participant count, no cross-pool identity |
| Succession preview | Initial ABI exposes only rest/resume/retire; `createdBy` and `currentHolder` remain distinct storage fields | Co-holder, apprentice, handover, fork, and community-held concepts are clearly labelled later work with no active control |

### 2.3 Member-funded priced Offers and pause freeze

| Case | Expected contract/read-model result | Required proof |
|---|---|---|
| Member files a priced claim | An eligible member may create an ApprovalGated pending request on a priced Offer; no custody, acceptance, funding consumption, or unit mutation occurs. An Open-mode priced Offer still reverts because it has no steward decision step | Unit test proves request succeeds only for ApprovalGated, `acceptClaim` remains steward-only, and a failed path emits/stores nothing |
| Funding race | Two members may have distinct funding rows for the same Offered commitment. The first steward-accepted claimant alone can move `DepositRecorded -> Consumed`; the other row cannot consume and its complete recorded deposit is refund-eligible after supersession | Test both acceptance orders, event order, and exact one consumed/one refund-eligible outcome without a second capacity commit |
| Nothing-owed withdrawal replay | `queueFundingRefund` on a still-Pledged row moves it once to `Withdrawn`, emits `FundingWithdrawn(fundingId, commitmentId, funder, withdrawnBy)`, and creates no disbursement or obligation | Phase 4 unit test proves exact event fields and zero child. Matrix EO-29 binds the later indexer dispatch to withdrawal-before-pledge replay without fabricating deposit or consumption facts; this increment adds no indexer code |
| Refund idempotency | First eligible call stores one funding-to-disbursement and reverse relationship before `DisbursementQueued(kind=Refund)`. Every retry returns the same child. Failure/requeue changes only the ordinary child attempt; direct and whole-batch cancellation both reject Refund children before mutation, so no cancellation, duplicate call, or replay can strand the obligation or create another refund | Unit + invariant tests assert cancellation rejection, at most one refund child, and at most one Confirmed refund for every funding ID |
| Decline after deposit | `Pledged -> DepositRecorded`; steward decline makes the row mechanically refund-eligible; queue uses immutable garden Safe, refund account, canonical G$, and full deposited amount | Test deposit equal to price and deposit above price; both refund the full recorded amount on non-delivery, while fulfilled excess stays with the garden |
| Safe below obligations | Earmarks remain accounting, not locks. A refund whose Safe cannot cover the transfer receives the bounded authenticated failure outcome, remains `RefundQueued`, and requeues the same child only after replenishment | Fork/integration test proves failure is not Refunded, no timeout/log-only fallback exists, replenishment + requeue confirms the same child, and obligations never go negative |
| Bounded source acknowledgment | `consumeFunding` stores one write-once `consumedFundingOfCommitment` pointer. Successful payout acknowledgments close funding from that local pointer and never call Commitment Pooling, including ordinary plans with no member funding | Gas/integration test covers a batch where every entry completes a distinct funded plan under the fixed 300,000 source-receiver budget at the proposed configured limit, plus a reverting-pooling dependency test. If hard-max 24 exceeds the budget, batching remains disabled until a lower measured limit is frozen |
| Full pool pause freeze | While PoolState is Paused, `claimCommitment`, `acceptClaim`, `declineClaim`, `acceptExchange`, `submitForConfirmation`, `markReadyForConfirmation`, `confirmFulfillment`, and `confirmFulfillmentAsFallback` all revert `PoolNotInState` before mutation | One table-driven unit family covers all eight blocked selectors and separately proves evidence/linkage, roster wind-down, cancel/expire/dispute resolution, and the non-blocking Work hook remain available |
| Registration while module paused | `registerPool` retains exact Protocol/Garden authority, root, uniqueness, and idempotency gates but no longer uses the operational-pause modifier. `onGardenMinted` and every other operational mutation remain paused. The unchanged Garden authority means a current root-garden steward can still consume the root's one-pool slot with a Garden registration before the owner submits Protocol registration; register #104 accepts that residual availability risk | Unit + backfill-script tests prove paused registration, wrong-authority/wrong-root/duplicate failures, executable root-Protocol-first paused backfill output, and no unrelated un-gating. The verifier must reject a plan that registers the root as Garden, but the contract adds no new authority guard |

## 3. Public claim and copy matrix

| Claim | Allowed wording | Forbidden wording | Authority |
|---|---|---|---|
| Feature availability | Built / Planned / Evidence-gated | Planned behavior described as live | canonical [Commitment Pooling Google Doc](https://docs.google.com/document/d/16LNXMr5voQUgWC3iyULbL4iEhRrFo4DezZZLgNtA4hc/edit) |
| Ongoing Offer history | “This offer has been kept N times across M cycles” when derived from linked Fulfilled instances | “Reliable,” “trusted score,” “verified impact,” or a participant count inferred from commitment history | `standing-commitments-spec.md` §§5, 9 |
| Availability | “N places available” only for current Offered instances with provider capacity already reserved | A series-level desired quantity, claim-spawned instance, or unreserved draft described as available | `standing-commitments-spec.md` §4 |
| Settlement proof | Queued / dispatched / executed-ack-pending / confirmed / failed / delayed | Dispatched or executed-ack-pending = arrived | `settlement-spec.md` §3.0 (state vocabulary) and §3.1.2 (`DisbursementState`) |
| Confirmation actor | Authenticated `CeloSettlementExecutor` acknowledgment through CCIP; plus the owner-only `failStrandedSubject` Failed-disposition (`SourceStranded`) for a subject whose snapshotted peer was retired — never Confirmed, never from delay | Human verification, operator report, timeout as payment failure | `settlement-spec.md` |
| Chain placement | Proof/control on Arbitrum; canonical G$ on Celo | Bridged G$, Arbitrum G$ custody | `settlement-spec.md` |
| CCIP availability | CCIP is the frozen command/ack interface; the official directory currently publishes Arbitrum One↔Celo Mainnet in both directions at v1.5.0, while the exact Arbitrum Sepolia↔Celo Sepolia pair is not listed and Celo's current support page lists CCIP only for mainnet | “Exact Arbitrum Sepolia↔Celo Sepolia lifecycle proven”; “Celo Sepolia CCIP is live”; “dispatch means support arrived” | current Chainlink CCIP directory + Celo cross-chain docs + `settlement-spec.md` §8; fresh mainnet directory/code-hash/fee/ping-ack proof before value authority; ephemeral Arbitrum Sepolia↔Ethereum Sepolia endpoint proof is explicitly non-exact |
| Arbitrum Sepolia protocol dependencies | Safe v1.4.1 and official EAS/SchemaRegistry cover `421614`; Hats does not currently publish a `421614` deployment | “EAS must be test-deployed on 421614”; “Hats is officially deployed on 421614”; reuse of Ethereum Sepolia addresses without chain-local proof | official EAS repository + Hats supported chains + Safe deployments + `contract-spec.md` §7.3; verify EAS/SchemaRegistry bytecode and deploy only version-pinned test Hats |
| Fourth garden | “a fourth slot for a mature MRV-adoption anchor — open; candidates under consideration, none selected” | Any named fourth garden in any artifact, or any claim that a fourth garden is selected or participating | `plan.todo.md` Decision Log #29 (supersedes Decision Log #25 and Decision Log #27) |
| Community comparison | Alphabetical/neutral aggregates with small-community suppression | Rankings, leaderboards, credit scores | `uiux-spec.md` §7.2 |
| Funding | Supports the garden; not per-Need escrow or steering | Funding buys priority/control | Community spec |
| Borrow-and-repay / vouchers | `CreditRegistry` is an active August-wave companion at `../commitment-credit-follow-on/spec.md`, but its contracts lane stays undispatchable until the in-code interface freeze, spec revalidation, and human legal/operations review clear; transferable vouchers remain evidence-gated follow-on scope | Treating credit as already dispatchable; transferable vouchers, `settlementAdapter` activation, or voucher exchange as August capability | `../commitment-credit-follow-on/spec.md`, PRD-697 for credit; PRD-651 for vouchers |
| Pilot outcome | Observed change, mixed result, or strengthened settlement capacity only at the approved claim class | Settlement/transaction volume, feature use, or fulfillment alone described as causal livelihood/community impact | `pilot-evidence-spec.md` |

## 4. Final role / route / state proof

| Surface | Role | Required journey | Proof owner | GREEN evidence |
|---|---|---|---|---|
| Client `/home/:garden/pool` | member/lead/contributor/recipient | create, claim, join/team, evidence/work credit, Ready-submit, confirm/dispute, payout receipts, offline retry | `ui_client` | targeted tests + authenticated Brave + real device |
| Client `/home/:garden/pool/standing/:seriesId` | series holder/pool member | Story, exact availability, add places, claim one pre-created place, rest/resume/retire, ask-again next cycle | `ui_client` | targeted tests + authenticated Brave + real device |
| Personal Things I can offer | signed-in member | saved/private Offer metadata, choose Offer once or Offer over time, link an ongoing Offer to one pool series, device-change persistence explanation | `ui_client` | signed-profile integration tests + authenticated Brave + real device |
| Admin `/garden/pool` | steward/evaluator | readiness, cycles, seeding, claims, roster/credit, recognition/payment review, retention, override/dispute | `ui_admin` | targeted tests + authenticated Brave |
| Admin `/community/pools` | protocol steward | Protocol pool plus current-garden pool only; no other-garden rows | `ui_admin` | targeted tests + authenticated Brave |
| Admin Hub Confirm queue | local or protocol steward | Local fallback rows plus opted-in cross-garden protocol-fallback rows; each row names garden, ordinary reachability, actor scope, and fallback provenance, without granting full other-garden pool browsing | `ui_admin` | targeted tests + authenticated Brave |
| Admin Operations | deployer, protocol steward/module owner, or settlement operator according to capability | alphabetical cross-garden oversight, batch/CCIP command-ack controls, explicit seed/top-up form, typed Funding result, native fee reserves, peer/Safe/cap status; each write independently gated | `ui_admin` | targeted tests + authenticated Brave |
| WalletDrawer | member | delivery-disabled and enabled Celo send | `ui_client` + `settlement` | AA evidence + authenticated real device |
| Public Garden / impact | funder/collaborator | planned/live/reporting labels, privacy thresholds, no ranking | `editorial` | component tests + rendered public proof |
| Indexer replay | operator | preserve bare-address `Garden.id` compatibility; replay chain-scoped Commitment Pooling entities, independently cursor-gated pool/cycle/series/commitment lifecycles and confirmer rules, and relationships; rollback rehearsal | `indexer` + `release_ops` | dry run, snapshot, replay, Garden-query compatibility check; opposing Pool Open/Pause/Resume and Cycle Open/Close/Compost/Cancel events delivered before/after creation and in either order converge to the latest state, reason, open-cycle relationships, and immutable cycle snapshots; duplicate/reverse commitment terminal-event fixtures with Accepted/Ready/Fulfilled/Cancelled/Expired/Disputed/resolved-dispute delivery before creation and atomic typed-buffer drain after creation; opposing `ConfirmerRuleSet` opt-in/opt-out updates converge to the latest full rule tuple; rollback record |
| Release | protocol release owner | every boundary passes the common test, explicit-authorization, selected-review, receipt/post-state, and rollback floor; paused/no-authority deployment is tier 1; non-custodial and non-transferable coordination with all value dependencies paused or disabled is tier 2 and additionally requires explicit temporary-owner risk acceptance, exact current/rollback-owner proof, and emergency pause; protocol authority, custody, transferability, peer wiring, allowances, or value movement is tier 3 and requires the approved Safe plus applicable audit/timelock/soak gates or a dated human disposition naming substitute evidence; peer/fee/Safe, ping/ack, and real-exit stages remain separately gated | `release_ops` | signed checklist in `handoffs/human-release-ops.md` |

## 5. Runtime, post-QA documentation, and September gates

Runtime UI GREEN aggregates `ui_client`, `ui_admin`, and `editorial` before QA Pass 1.
Post-QA `docs` polish follows QA Pass 1 and must complete before QA Pass 2. Final
`walkthrough_videos` follow QA Pass 2 and do not block release certification. The September
`community` PWA records its own GREEN later and is not a dependency of these gates. Membership
persistence is conditional within Community and remains excluded until RESR-64; it does not block
the non-membership Community core.

## 6. Pilot operational measurement definitions

These targets measure product and operating reliability. They do not by themselves establish that
pooling strengthened settlement capacity. `pilot-evidence-spec.md` owns the required cohort,
baseline, claim hierarchy, safeguard/repair review, circulation-integrity rules, privacy tiers, and
stop conditions for that outcome claim.

| Target by 2026-09-30 | Canonical calculation |
|---|---|
| At least 90% passkey onboarding completion | Gardeners who complete passkey setup divided by gardeners who start their first authenticated action. |
| At least 99% eligible offline-job sync success | Eligible queued jobs that reach confirmed submission divided by eligible jobs that begin a normal send attempt. Exclude user-cancelled jobs and all time spent in `waiting_for_hat`; a retry-recovery cut uses only jobs that entered retry as both numerator population and denominator. |
| At least 95% steward validation without data repair | Eligible triage, seeding, and confirmation tasks completed without a corrective edit divided by all eligible observed tasks. |
| Median field submission at or below two minutes | Median time from first input to saved or submitted state, compared with a separately measured pre-pilot baseline; no baseline value is assumed. |

Every report includes numerator, denominator, sample size, observation window, excluded states, and qualitative context. Public reporting excludes wallet addresses, join-request records, and participant identifiers. Unavailable, suppressed, mixed, and zero results remain distinct.
