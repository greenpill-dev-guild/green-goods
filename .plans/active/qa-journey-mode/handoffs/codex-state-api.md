# State and API Handoff

This lane is not applicable. Journey Mode reads the existing catalog and persists verdicts and
notes through the existing test-ID and wallet-keyed state. It adds no endpoint, store shape, query,
or migration.
