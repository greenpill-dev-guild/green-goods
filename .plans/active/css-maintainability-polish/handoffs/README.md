# Handoffs

Use this directory for lane-specific notes, CSS ownership inventories, guardrail findings, and visual QA evidence created while executing the CSS maintainability polish plan.

Keep handoffs source-grounded:

- cite file paths and line numbers for confirmed CSS ownership;
- separate confirmed drift from inferred risk;
- include command output or screenshots for validation claims;
- do not duplicate the full Warm Earth spec here.

Current active handoffs:

- `claude-ui-revamp.md` - active revamped UI lane for full CSS maintainability audit, scope lock, and targeted cleanup.
- `claude-qa-pass-1.md` - QA pass after the revamped UI lane completes or explicitly defers with evidence.

Historical handoffs:

- `claude-ui.md` - first low-friction UI pass; keep as provenance, not closure evidence.
- `codex-state-api.md` - completed custom-property guardrail lane.
