# Community Needs Interface: Research-Doc Corrections Log

**Feature Slug**: `community-interface`
**Stage**: `active`
**Created**: 2026-07-04
**Scope**: Every repo-dependent claim in the community-interface research pass (compass artifact, "Green Goods Community Interface: Spec Direction", 2026-07-04) resolved against the codebase at the worktree branch `claude/ecstatic-vaughan-206fb1` (post-PR-#619 tree). Verdicts follow the commitment-pooling log convention: **VERIFIED**, **CORRECTED**, **SUPERSEDED** (user decision 2026-07-04 overrode the claim). `spec.md` in this folder builds on THIS log, not on the research doc as written.

Vocabulary note: the research doc says "problem" throughout; the locked vocabulary is **Need** (decision #1). Claims below quote the doc's word where quoting, and the corrected truth uses Need.

---

## 1. Research-doc §2 verification checklist, resolved

| # | Claim (research doc) | Verdict | Actual truth + evidence |
|---|---|---|---|
| C1 | Resolvers validate via Hats helpers (`isWearerOfHat` vs `isAdminOfHat`) | **CORRECTED** | House resolvers never call Hats directly; they call `IGardenAccessControl` on the garden TBA: `isGardener`/`isOperator` (`packages/contracts/src/resolvers/Work.sol:98-106`), `isOperator` only (`WorkApproval.sol:147-151`), `isEvaluator OR isOperator` (`Assessment.sol:118-122`). Community check exists today: `isCommunity(address)` (`packages/contracts/src/interfaces/IGardenAccessControl.sol:45`). Need resolvers use `isCommunity`; NeedStatusResolver uses `isOperator`. |
| C2 | Problem/signal schemas revocable; "operator revocation sets revocationTime"; indexer hides revoked | **CORRECTED** | EAS revocation is **attester-only** — an operator cannot revoke a member's attestation, ever. Moderation = `NeedStatus` states (merged/hidden/declined) honored by all readers. Revocability is kept on `Need`/`NeedSignal` but grants only self-retraction/un-signal (spec §1 sub-decision a). All existing house resolvers return `false` from `onRevoke` (Work.sol:145-148 et al.); Need/NeedSignal deliberately diverge (`onRevoke → true`) for self-retraction — an explicit, recorded divergence. |
| C3 | Community Hat minted per-garden; derivation in `HatsModule.createGardenHatTree()` | **VERIFIED** | Six-role tree incl. Community: `GardenHats.communityHatId` (`packages/contracts/src/modules/Hats.sol:37-46`), created in `createGardenHatTree()` (lines ~400-402); `enum GardenRole {Gardener, Evaluator, Operator, Owner, Funder, Community}` (`IHatsModule.sol`). Only live use today is GreenWill genesis-badge eligibility (CP corrections-log H6) — this layer gives it its first app surface. |
| C4 | Batch-mint via `Hats.batchMintHats` | **VERIFIED (unused today)** | The repo never calls `batchMintHats`; `HatsModule` mints individually (`hats.mintHat()`, Hats.sol:381-382). The Hats protocol singleton supports batch minting; the admin console calls the protocol directly. The join-request queue it drains is net-new off-chain state (spec §9). |
| C5 | Thin-schema + CID convention; `config/schemas.json` structure | **VERIFIED + POLICY AMENDMENT REQUIRED** | `packages/contracts/config/schemas.json` is the canonical schema definition set and has three entries today. The readiness lock permits append-only approved immutable additions but never mutation of existing definitions/keys; `packages/contracts/AGENTS.md` must carry that narrow exception before dispatch. |
| C6 | Register schemas via deploy `--update-schemas` | **CORRECTED** | Bulk `--update-schemas` remains prohibited. The planned `need-schemas` deploy target registers only four exact additions, persists unique resolver/schema keys without replacing siblings, dry-runs on a fresh chain, and passes post-broadcast verification. |
| C7 | Offline job-queue kinds enum; "PRD-674 added five kinds"; add `problem`, `problemSignal`, `problemStatus`, `testimony` | **CORRECTED** | Kinds today are exactly `work` and `approval`; later lanes add their own kinds. This layer requires `need`, `needSignal`, and `testimony`; all three can persist in `waiting_for_hat` without consuming retries. **No `needStatus` or `fundingAttribution` kind** — those are online wallet actions. |
| C8 | "Envio computes derived states"; indexer dedups signals and hides revoked | **CORRECTED (load-bearing)** | **Envio never indexes EAS** (CP corrections-log "Envio boundary"; CLAUDE.md indexer boundary). Status derivation is an app-side join in shared hooks: Envio commitment entities (carrying `needUID` from `CommitmentCreated`) × easscan reads (Need/NeedSignal/NeedStatus). Signal dedup = distinct-attester count app-side. Indexer scope shrinks to: persist `needUID` on commitment entities + need-keyed aggregates. Transcribed verbatim, the research doc's indexer issue would have dead-ended the lane. |
| C9 | easscan.org GraphQL is the EAS read path; `revocationTime` first-class | **VERIFIED** | `packages/shared/src/modules/data/eas.ts` + `createEasClient`; `https://arbitrum.easscan.org/graphql` (`config/blockchain.ts:81`); queries already filter `revoked: { equals: false }`. |
| C10 | Paymaster: verifying-paymaster pattern; rate limiting in paymaster policy; policy allowlist in-repo | **CORRECTED (location)** | Provider is Pimlico; the repo holds only the policy ID (`VITE_PIMLICO_SPONSORSHIP_POLICY_ID`, default in `packages/shared/src/workflows/authServices.ts:10`); **policy rules live on the Pimlico dashboard**. Extending sponsorship to need/signal/testimony writes + per-account/per-window caps is ops/config work, near-zero code. Burst-test before the first sponsored community write. |
| C11 | `problemUID` addable to the commitment record "without a state-machine change" | **VERIFIED — and time-sensitive** | No commitment code exists (August build, PRD-672). `Commitment` already carries `bytes32 assessmentUID` (CP contract-spec §6, struct line ~292); `needUID` added beside it, in `CreateCommitmentParams`, and in `CommitmentCreated` — amended into the CP contract-spec 2026-07-04, **before** the August build starts. Waiting until September would have made it a post-deploy upgrade. |
| C12 | Passkey/ERC-4337 counterfactual deployment + sponsorship on first action; retry holds pre-hat writes | **CORRECTED** | Auth/passkey primitives exist, but five ordinary retries cannot span operator-paced membership. `need`, `needSignal`, and `testimony` require a persistent `waiting_for_hat` state that consumes no retry attempts and resumes only after membership is observed. Join-request transport itself remains RESR-64-gated. |

## 2. Further corrections beyond the checklist

| Claim | Verdict | Truth |
|---|---|---|
| "Signal is NOT a Gardens V2 conviction pool" — implied greenfield choice | **VERIFIED, sharpened** | ActionSignalPool (priority-signal CVStrategy pool) exists at contract level with zero app surface (CP corrections-log H5); uiux-spec §8.2 had left the signal data layer open ("through it or an app-level store"). Resolved: `NeedSignal` EAS attestations; ActionSignalPool stays dormant. |
| Voice capture is to-be-built | **CORRECTED** | `AudioRecorder`/`AudioPlayer` + `useAudioRecording` (260s cap) exist in shared; work submissions already upload `audioNotes` to IPFS; the agent package already runs server-side transcription (`ai.transcribe`) — the transcribe-on-sync fallback reuses it behind a small endpoint. Web Speech on-device dictation remains progressive enhancement (es/pt packs not guaranteed). |
| MDR is a stepper component | **CORRECTED (nuance)** | MDR is the draft-flow grammar `DraftStep = intro\|media\|details\|review` + dialogs, not a literal stepper component. The Create flow mirrors the grammar, not a component. |
| Three-tab shell Home/Signals/Profile (uiux-spec §8) | **SUPERSEDED** | Decision #4: **Needs / Create / Profile**; pool story folds into the board header + detail threads; solution-proposal objects dropped. |
| v1 fund button = pledge-and-contact | **SUPERSEDED** | Decision #5: embedded direct donation + endowment (the `/fund` paths) in need context + `FundingAttribution` attestation in v1; totals on detail only. |
| Cross-garden exploration = curated subset | **SUPERSEDED** | Decision #7: global read-only browse; my-garden default; same-garden signal gate unchanged. |
| ProblemStatus enum {1 acknowledged, 5 declined} | **SUPERSEDED** | Clean numbering with moderation states: 1 acknowledged · 2 merged · 3 hidden · 4 declined (spec §3.1). |
| CP decision #1/#14 "EAS gains exactly two schemas" | **AMENDED** | The needs layer adds four (Need, NeedSignal, NeedStatus, FundingAttribution). Letter changes, spirit holds: commitments remain module-native, never EAS. Recorded as CP decision-register addendum #28. |
| Six-role tree, offline retry semantics, two-indexer read path, Greenpill Commons reuse boundary (UX only, zero EAS code) | **VERIFIED** | Match CP corrections-log §§1-6 findings verbatim; no re-litigation. |

## 3. Non-goals confirmed against the locked registers

No claim flow (PRD-682 cut + CP decision register). No leaderboards or funding-ranked boards anywhere. This Community hub adds no settlement rail: Commitment Pooling owns canonical G$ settlement on Celo, and Community/public joined reads may display only its correctly classified reported or oracle-verified context. No push notifications (the gathering is the loop). No docs-site promotion of this spec (hub is the spec home, CP decision #1 precedent).

## 4. Instrument restructure — survey-first (2026-07-11)

RESR-62's instrument was restructured from a two-pass interview (both passes open-text, mirrored wholesale into the Linear "Garden Mandate Survey" doc) into survey-first: Pass 1 is now a self-serve Google Forms build sheet (45-question main path, ~69% closed with Other escapes, UNICEF/methodology branches, separate consent block) and Pass 2 an action-oriented call guide. Changes of record:

- `research-plan.md` slimmed to plan level; the instrument now lives in `survey-instrument.md` and `onboarding-call.md` (1:1 with Linear doc slugs `c9271149e328` and `d3690de04864`).
- Research outcomes revised from five to six evidence-for-decision statements: seeding go/no-go, needs-vocabulary ship-or-revise, Cycle-1 commitment units, mutual-credit design lane, measurement anchoring (absorbs canceled RESR-53), G$ rollout order. Join-request persistence moved from outcome #4 to a "feeds RESR-64" pointer — it is RESR-64's decision, not survey evidence.
- The "Locked implementation prompts" moved from the survey doc to the call guide's Appendix A (facilitator guardrails); the join-request decision matrix no longer appears in the survey doc (it stays in `research-plan.md`).
- Cohort wording follows commitment-pooling decision #26: three named gardens plus “a fourth garden — in outreach, named only once participation is confirmed.” The outreach target remains only in the internal decision/history record. A same-day named-cohort pass was a presentation-boundary regression and was corrected across `research-plan.md`, `survey-instrument.md`, Commitment Pooling public/operational artifacts, and their Linear mirrors on 2026-07-11.
