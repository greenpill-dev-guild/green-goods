# Agent PostHog Observability Brief

Formalizes the existing active PostHog observability plan as a plan hub. The work gives agents read-only query access to production telemetry so bug intake and interactive debugging can match reports to real errors, sessions, and recurring patterns.

The first delivery is a durable query script and routine integration. A PostHog MCP server remains out of scope until script usage proves that need.
