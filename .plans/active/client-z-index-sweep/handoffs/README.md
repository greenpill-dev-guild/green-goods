# Client Z-Index Sweep Handoffs

These handoffs keep `status.json` machine-readable while the actual work remains a single bounded z-index cleanup.

- `claude-ui.md` tracks the primary surface work and the remaining browser check.
- `codex-state-api.md` and `codex-contracts.md` record why those lanes are `n/a`.
- `claude-qa-pass-1.md` and `codex-qa-pass-2.md` capture the pending verification path.
