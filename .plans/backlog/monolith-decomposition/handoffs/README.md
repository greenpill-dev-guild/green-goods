# Handoffs

Keep future slice handoffs short and factual. Use one file per active lane when a slice is promoted.

Expected handoffs:

- `codex-state-api.md` for agent DB/server decomposition.
- `claude-ui.md` only if the parked admin Cookie Jar slice is unblocked.
- `claude-qa-pass-1.md` and `codex-qa-pass-2.md` for review gates.

Each handoff should capture:

1. What changed
2. What remains
3. Validation run
4. Known risks or blockers
5. Repo-truth references from this hub, not tool-local memory claims
