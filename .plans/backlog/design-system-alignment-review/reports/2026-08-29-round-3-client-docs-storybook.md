# Round 3 — Client, Docs, Storybook & Guidance Alignment Review — 2026-08-29

**Scope**: installed PWA + public browser (`packages/client`), docs UI (`docs/`), Storybook surface, guidance stack + DesignMD coherence. Read-only; no fixes applied.
**Protocol**: `.claude/skills/design/system-alignment-review.md`. Validators run first — ALL GREEN, including `lint:vocab` (first run this cycle: clean across 3 i18n files), `check:design-md`, `check:design-generated`, `check:design-tokens` (token_version 2.7.0), story contract 255/255, `check:story-quality`, `check:guidance-links` (65 files).
**Method**: two parallel evidence sweeps (client ×73 tool calls; docs/guidance ×63), every claim carrying file:line on both sides. Another session's in-flight files (`PwaRuntime.tsx`, `PresentationHydrationFallback.tsx`, `index.html`, `useServiceWorkerUpdate.ts` + tests) reviewed at HEAD only.

## Confirmed drift (7 clusters, ranked)

1. **Agent-facing guidance still describes the pre-deletion admin library** — today's Round-2 sweep missed the roster-restating files. `prompt-contract.md:180-184` lists the deleted trio and keeps an AdminFab row (":28" names it as translucent chrome); `ARCHITECTURE.md:87-88` says "16 today" incl. the trio; root `AGENTS.md:236` says "16 today"; `packages/admin/DESIGN.md:34` + `:81` still put `AdminFab` in the shell table/diagram (contradicting `:127` in the same file — and this file is *published* to design.greengoods.app by `prepare-design-assets.mjs`); `defect-grammar.md:39,41,46` maps casual terms to `data-component` values that resolve to zero DOM nodes (and the shipped FAB root carries `data-component="NavigationBar"` at `FabButton.tsx:132`, no tag of its own); `qa-triage/codex-prompt.md:96` and `docs/reference/glossary-community.md:110` name AdminFab + the retired Left/RightSheet renderers. *Fix: one guidance commit syncing all rosters to the 20-wrapper truth in `packages/admin/DESIGN.md:127`; add `data-component="FabButton"`.*
2. **The browser/PWA dialect briefs lag shipped truth on three counts** — (a) `/vaults` is a full tested, gated, WebMCP-registered public route family (`routes.tsx:110-114`, `presentationMode.ts:16`, `webmcp.ts:40`, 698-line view + 10-file subtree) that `DESIGN.browser.md:27`'s closed route table forbids; (b) the § Motion & Dialogs roster names nonexistent `PublicFundingMethodSelector` and mis-names the garden field-note and `/impact` overlays (real: `PublicRecordDrawer`, `PublicEvidenceDialog`), omitting `PublicInstallDialog`/`VaultCheckoutDialog`/`VaultManagePositionsPanel`; (c) the AppBar hides on a third, undocumented route family (`/home/:id/commitments/*`, `AppBar.tsx:27-29,48`, sound in-code rationale) plus the any-drawer-open case. *Fix: spec updates — the code side is deliberate in all three.*
3. **The homepage ships eight sections against a seven-section canon** — `PublicWhoTendsAGarden` (276 lines, five personas) renders fifth (`Home.tsx:104`) but appears in no canon, and `Home.tsx:27-29`'s own docblock lists seven. No test asserts the section list/order (`PublicHome.test.tsx` stubs sections), which is how this stayed invisible; the section also has zero tests and zero stories. *Decision: document it as section 5 or remove it; either way add the order test.*
4. **Below the fold, public pages have no wayfinding** — `SiteHeader` correctly fades to `pointer-events:none` + `aria-hidden` after 220px (`SiteHeader.tsx:35,66,149,152`), but `PublicFooter.tsx:14-45` carries only glossary/Twitter/Admin/Docs/GitHub — not the Gardens/Impact/Fund/Actions + mailto set `DESIGN.browser.md:34,63` assigns it, and `PublicFooter.test.tsx:3-4` locks the shipped set, so both sides are deliberate. Keyboard/AT users lose all route affordance mid-page. *Decision: add the four nav links + Contact to the footer (recommended) or re-spec.*
5. **Storybook's responsive proof is silently dead** — 27 story files use `viewport: { defaultViewport: … }`, an API removed in Storybook 10 (zero occurrences in the installed `storybook@10.4.6` dist); `preview.tsx:29-35` defines `mobile/tablet/desktop` options, so `"mobile1"` and two never-defined names (`adminMobile390x844`, `confirmMobile390x844`) resolve to nothing and every "Mobile" story renders desktop-width. `storybook.mdx:233` teaches the dead form. *Fix: codemod to `viewport: { value: "mobile" }` + docs update.*
6. **"Admin FAB-large is 16px" is canon in four places; the shipped FAB is a capsule at both sizes** — `language.md:104`, `implementation.md:13`, `prompt-contract.md:18`, `packages/admin/DESIGN.md:147` vs `FabButton.tsx:240-241` (`rounded-full`, 48px dock / 56px floating). The rule rode through the 2.7.0 bump unchallenged. *Decision: code or spec; if spec (likely), strike the clause in four files + token_version bump.*
7. **Two one-line shipped-code regressions** — (a) `/fund`'s always-visible Manage Endowments is a bottom-bordered text link (`Fund.tsx:635`), not the specified secondary warm capsule (`DESIGN.browser.md:103`; shape-is-hierarchy per `language.md:97-102`) — the only public withdrawal entry point, under-emphasized by one tier; (b) docs dark code blocks ship Prism Dracula `#282A36` on the warm `#171717` canvas because the declared `code-surface` (`docs/DESIGN.md:43,54,88-93`) was never projected (`docusaurus.config.ts:482-484`; two custom-property lines fix it).

## Inferred risks (5)

- `PublicFundingCard` is the one public overlay with no portal (`fixed inset-0` at `:362`, no `createPortal`) — safe only because `Fund.tsx` mounts it outside every `.editorial-section-reveal` transform; the dialect's own rule (`DESIGN.browser.md:170`) says never rely on that.
- `AppShell.tsx:24` reserves `calc(100lvh-69px)` unconditionally while the AppBar hides on three route families + drawer-open — a 69px dead band on immersive routes.
- `AdminRevampTokens.stories.tsx:14` anchors to a deleted handoff path and uses `--font-mono` (zero definitions in admin CSS); the "Tier 1a" gallery will drift silently.
- The published `componentNamespaces` manifest (`prepare-design-assets.mjs:144-175`) covers ~⅓ of shipped story roots (15 distinct `Admin/*` roots vs 3 documented; 3 client stories sit at bare `Public/*`).
- `Shared/Tokens` galleries lag `theme.css` (Shadows story omits `--shadow-elevation-1/2/3`/`--shadow-float`/`--shadow-primary-glow`; no material-thickness gallery; `storybook.mdx:143-147` lists five of eight token stories). `routes/` stays outside the story-coverage contract on both packages.

## Missing proof (4)

- `PublicWhoTendsAGarden`: no test, no story (every sibling section has both).
- "Same Install App CTA across header, drawer, sections, receipt" (`DESIGN.browser.md:194`): consumers verified by grep, no cross-surface test.
- `/vaults` shipped path (`VaultCheckoutDialog`/`VaultCheckoutShell`/`VaultManagePositionsPanel`) has no stories — only the staged, unreachable Card path does.
- Docs builder-teal active-nav selector (`custom.css:165-169`) unverified against built HTML; deployed Storybook manager chrome/logo resolution unverified (source-level only).

## Considered and rejected (highlights)

PWA serif ban: clean (zero `font-serif` in the PWA tree). `SendTab`'s green-on-green CTA: sanctioned tertiary/on-tertiary pair, not the banned bright-green-with-white. `.storybook/theme.ts` literals: all 13 match root DesignMD exactly and are audited baseline. No pre-DL-009 dark-fill language anywhere outside the updated `language.md` (root `DESIGN.md:113` is the *light* invariant, preserved by design). Coverage-checker exclusion sets survived today's deletions intact (`verifyExclusionsExist` would have failed otherwise). `/gardens/:id` editorial grammar, em-dash-not-zero discipline, receipt-token safety, and chrome separation all verified exact. Card Endow remains truly unreachable.

## State of health

Client behavior is the strongest surface reviewed this cycle — every falsifiable hard rule held; the drift is concentrated in the *descriptive* layer (`DESIGN.browser.md` route table, overlay roster, homepage count). Docs UI matches its dialect nearly exactly (one unprojected code-surface). Storybook is structurally sound with one quiet functional rot (dead viewport API) and lagging galleries/manifest. The guidance stack absorbed today's admin round unevenly: derive-don't-restate files stayed true; every roster-restating file drifted. Overall: **healthy, with a stale map** — the highest-leverage fixes are spec/guidance syncs plus four small code changes (footer nav, capsule, viewport codemod, Prism surfaces). Single biggest load-bearing risk: the guidance rosters (cluster 1) actively mislead AI design tools today.
