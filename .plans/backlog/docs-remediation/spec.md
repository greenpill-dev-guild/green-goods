# Spec — Docs Remediation

The plan is the spec for this hub: [plan.todo.md](plan.todo.md) carries the full four-phase scope, ground rules, per-item evidence anchors, and the state ceiling for commitment pooling claims. Acceptance per phase is its "Definition of done": PR to develop with green `build:docs` + `docs:audit` (+ `lint:vocab` / `check:ontology` when touched) and per-claim re-verified evidence in the body.
