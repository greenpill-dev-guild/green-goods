{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "docusaurus-2",
  "cleanUrls": true,
  "installCommand": "cd .. && bun install --frozen-lockfile",
  "buildCommand": "cd .. && node docs/scripts/docs-audit.mjs --ci && bun run check --only docs-generated && bun run check --only ontology && bun run check --only skill-behavior && bun run --cwd docs test && bun run --cwd docs build",
  "outputDirectory": "build"
}
