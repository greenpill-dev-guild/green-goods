---
title: Mint Impact Certificate
sidebar_label: Mint Impact Certificate
slug: /community/operator-guide/creating-impact-certificates
description: "Mint a Hypercert-backed impact certificate from an assessment frame and approved garden work."
audience: operator
owner: docs
last_verified: 2026-04-23
feature_status: Live
image: /img/social/mint-impact-certificate.webp
goal: "Help operators open the certification flow, select approved work, review certificate metadata, allocate units, and mint."
difficulty: "standard"
estimated_time: "20-40 minutes for a prepared claim."
prereqs: "An assessment frame, approved work, operator permission, contributor addresses, and a supported Hypercert deployment."
next_steps: "./managing-certificates"
persona_context: "Operators turning a defensible assessment and approval trail into an impact certificate."
source_of_truth:
  - packages/admin/src/views/Hub/CreateHypercert.tsx
  - packages/admin/src/components/Hypercerts/HypercertWizard/index.tsx
  - packages/admin/src/components/Hypercerts/Steps/AttestationSelector.tsx
  - packages/admin/src/components/Hypercerts/Steps/MetadataEditor.tsx
  - packages/admin/src/components/Hypercerts/Steps/DistributionConfig.tsx
  - packages/admin/src/components/Hypercerts/Steps/HypercertPreview.tsx
  - packages/shared/src/hooks/hypercerts/useMintHypercert.ts
keywords:
  - impact certificate
  - hypercert
  - mint hypercert
  - operator
  - certificate
---

import {GuideOpener, NextBestAction, OperatorPathNav, StatusBadge, StepFlow} from "@site/src/components/docs";

# Mint Impact Certificate

<StatusBadge status="Live" />

<OperatorPathNav />

<GuideOpener
  image="/img/social/mint-impact-certificate.webp"
  alt="An operator minting an impact certificate from verified garden work"
  label="Operator chapter"
  roleAccent="assessment"
>
  Bundle the assessment frame and approved work into a Hypercert-backed certificate.
</GuideOpener>

## What this helps you do

Mint an **impact certificate** for a garden after the assessment frame and approval trail are ready. This guide uses impact certificate as the plain-language concept; the current admin UI still labels the product flow **Create Hypercert** or **Mint Hypercert**.

Use this page after you have created the garden, made the assessment frame, and approved the work that belongs in the claim.

:::important Permission boundary
Only garden owners or operators can mint the Hypercert. Evaluators and funders can inspect the evidence, but they cannot finalize the certificate unless they also have operator authority.
:::

## Before you start

- The garden has a clear assessment frame: domain, reporting window, outcomes, and context.
- The work you want to include is **approved**, not pending.
- Contributor wallet addresses and unit allocation rules are ready.
- You know whether the target chain has active Hypercert and marketplace module addresses.
- You are signed in as a garden owner or operator.

:::tip Product wording
If the guide says **impact certificate** and the admin says **Hypercert**, follow the admin label. The certificate is implemented through the Hypercert minting flow today.
:::

## Step-by-step flow

Start from the current admin **Hub** workspace. The certification surface is labelled **Certify**, and the mint action opens the **Create Hypercert** wizard for the selected garden.

<img src="/img/screenshots/admin-garden-impact.png" alt="Current admin impact surface with assessments and certificate context" style={{maxWidth: '100%', borderRadius: '8px', border: '1px solid var(--ifm-color-emphasis-300)'}} />

<StepFlow
  steps={[
    {
      title: "Open the Certify workspace",
      detail: "Open Hub, switch to Certify, and pick the assessment bundle you want to mint. You can also use the Mint Hypercert action, which opens `/hub/certify/create` for the selected garden.",
    },
    {
      title: "Select approved work",
      detail: "In the Attestations step, choose the approved work records that belong to this certificate. If you selected an assessment, use it to filter by reporting period and domain before selecting records.",
    },
    {
      title: "Review certificate metadata",
      detail: "In Metadata, check the title, description, work scope, impact scope, timeframes, capitals, and SDG tags. Assessment-backed defaults are a starting point, not a substitute for review.",
    },
    {
      title: "Allocate certificate units",
      detail: "In Distribution, assign units to contributors. Keep the allowlist aligned with who did the verified work and make sure the total matches the required unit count.",
    },
    {
      title: "Preview and mint",
      detail: "In Preview & Mint, review the certificate, selected attestations, contributor allocation, and chain. Submit only when the claim is accurate, then sign the mint transaction.",
    },
  ]}
/>

## How to know it worked

- The minting dialog reaches a completed state and shows a transaction hash.
- Admin redirects to the Hypercert detail page.
- The detail page shows the expected title, description, work scopes, attestation count, minted date, and total units.
- If indexing is still catching up, the detail page shows a syncing state rather than a broken record.
- When marketplace listing is part of the garden plan, the detail page is where operators continue into approval and listing work.

### Behind the scenes

The wizard selects approved work attestations, builds Hypercert metadata, creates the contributor allowlist, uploads supporting data, and calls the Hypercert minting transaction. The certificate is only as strong as the assessment and approval trail behind it, so do not include weak or unrelated work just to make the certificate look larger.

## If something goes wrong

- **No garden selected.** Pick the garden in admin, then reopen the Certify workspace.
- **No permission.** You are not a garden owner or operator. Ask the garden owner to grant the right role.
- **No approved attestations.** Return to [Approve Work](./reviewing-work) and clear the relevant submissions first.
- **Assessment filter hides expected work.** Check the assessment domain and reporting window. The filter is doing its job if the work falls outside that frame.
- **Distribution validation fails.** Recheck addresses and unit totals before trying to mint.
- **Mint transaction fails.** Confirm the chain, signer, gas funds, and deployed Hypercert contract addresses before retrying.

## Next step

<NextBestAction
  title="Manage minted certificates"
  why="After minting, keep the certificate detail, listings, trade history, and evidence chain easy to inspect."
  actionLabel="Managing Certificates"
  actionHref="./managing-certificates"
  alternatives={[
    {label: "Approve Work", href: "./reviewing-work"},
    {label: "Vaults & Hypercerts", href: "../funder-guide/vaults-and-hypercerts"},
  ]}
/>
