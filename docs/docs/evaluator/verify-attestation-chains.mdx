---
title: Verify Attestation Chains
sidebar_position: 4
slug: /evaluator/verify-attestation-chains
audience: evaluator
owner: docs
<<<<<<< HEAD
last_verified: 2026-02-18
=======
last_verified: 2026-03-01
>>>>>>> release/1.1
feature_status: Live
goal: "Complete Verify Attestation Chains with clear, role-specific next actions."
difficulty: "standard"
estimated_time: "10-30 minutes."
prereqs: "Work + approval attestation query outputs."
next_steps: "./cross-framework-mapping"
persona_context: "Evaluators validating attestations and translating impact data into analysis outputs."
source_of_truth:
  - packages/shared/src/modules/data/eas.ts
  - packages/contracts/deployments/42161-latest.json
keywords:
  - attestation chain
  - verification
  - work approval linkage
---

<<<<<<< HEAD
import {AtAGlanceCard, NextBestAction, StatusBadge, StepFlow} from "@site/src/components/docs";
=======
import {NextBestAction, StatusBadge, StepFlow} from "@site/src/components/docs";
>>>>>>> release/1.1
import {EAS_EXPLORER_ENDPOINTS} from "@site/src/data/endpoints";

# Verify Attestation Chains

<StatusBadge status="Live" />

<<<<<<< HEAD
<AtAGlanceCard
  goal="Complete Verify Attestation Chains with clear, role-specific next actions."
  difficulty="standard"
  estimatedTime="10-30 minutes."
  prereqs="Work + approval attestation query outputs."
  outcome="You can prove each approved record has a valid attestation trail."
  personaContext="Evaluators validating attestations and translating impact data into analysis outputs."
  lastVerified="2026-02-18"
/>

## Verification flow

<StepFlow
  steps={[
    {title: "Extract work UID", detail: "Collect work attestation `id` values from EAS query."},
    {title: "Match approvals", detail: "Confirm approval `refUID` points to the expected work UID."},
    {title: "Check schema + chain", detail: "Validate schema UID and endpoint chain alignment."},
    {title: "Explorer proof", detail: "Open sampled UIDs on EAS explorer and compare decoded fields."},
  ]}
/>

## Pass criteria

- Work attestation exists and is not revoked.
- Approval attestation references that work via `refUID`.
- Attester role matches expected operator context.
- Timestamps are logically ordered.

## Explorer URLs

- <code>{`${EAS_EXPLORER_ENDPOINTS[42161]}/attestation/view/<uid>`}</code>
- <code>{`${EAS_EXPLORER_ENDPOINTS[42220]}/attestation/view/<uid>`}</code>
- <code>{`${EAS_EXPLORER_ENDPOINTS[11155111]}/attestation/view/<uid>`}</code>
=======
## Overview

Attestation chain verification is the core evaluator skill. Every approved piece of work in Green Goods creates a linked pair of attestations: a work submission attestation and an approval attestation that references it. Verifying this chain confirms that impact claims are backed by real, operator-validated evidence.

{/* IMAGE PLACEHOLDER */}

## How It Works

<StepFlow
  steps={[
    {title: "Extract work UID", detail: "Collect work attestation id values from your EAS query results. Each work submission has a unique attestation UID."},
    {title: "Match approvals", detail: "Find the corresponding approval attestation and confirm its refUID points to the expected work UID. This link proves the approval references the exact work submission."},
    {title: "Check schema and chain", detail: "Validate the schema UID matches the expected Green Goods schema. Confirm the EAS endpoint matches the chain the attestation was created on."},
    {title: "Explorer proof", detail: "Open sampled UIDs on the EAS explorer and compare decoded fields against your query data."},
  ]}
/>

**Pass criteria:**

- Work attestation exists and is not revoked
- Approval attestation references that work via `refUID`
- Attester role matches expected operator context
- Timestamps are logically ordered (work created before approval)

**Explorer URLs:**

- Arbitrum: <code>{`${EAS_EXPLORER_ENDPOINTS[42161]}/attestation/view/<uid>`}</code>
- Celo: <code>{`${EAS_EXPLORER_ENDPOINTS[42220]}/attestation/view/<uid>`}</code>
- Sepolia: <code>{`${EAS_EXPLORER_ENDPOINTS[11155111]}/attestation/view/<uid>`}</code>

## Best Practices

- Spot-check a random sample of attestation chains rather than only checking the most recent ones
- Watch for revoked attestations — a revoked work submission with an active approval indicates a data integrity issue
- Verify attester addresses against known operator addresses for the garden in question
- Log any broken chains (missing refUID links, schema mismatches) for follow-up investigation
- When working across chains, always double-check you are querying the correct EAS endpoint — Arbitrum and Celo attestations are on different contracts

## What's Next
>>>>>>> release/1.1

<NextBestAction
  title="Next best action"
  why="After validating attestation chains, translate verified records into reporting frameworks."
<<<<<<< HEAD
  actionLabel="Cross-framework mapping"
  actionHref="./cross-framework-mapping"
  alternatives={[
    {label: "Export and analysis", href: "./export-and-analysis"},
=======
  actionLabel="Cross-Framework Mapping"
  actionHref="./cross-framework-mapping"
  alternatives={[
    {label: "Export and Analysis", href: "./export-and-analysis"},
>>>>>>> release/1.1
  ]}
/>
