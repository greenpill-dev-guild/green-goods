---
title: Product Experience QA
sidebar_label: Product Experience QA
slug: /builders/quality/product-experience-qa
audience: developer
owner: product
last_verified: 2026-09-01
feature_status: In progress
source_of_truth:
  - AGENTS.md
  - packages/admin/AGENTS.md
  - packages/client/AGENTS.md
  - packages/admin/src/routes/views.tsx
  - packages/client/src/config/routes.tsx
  - scripts/data/qa-test-catalog.json
  - scripts/agents/qa-workbook-build.ts
  - docs/routines/qa-call-report.md
  - .claude/skills/qa-triage/SKILL.md
  - .claude/context/qa.md
  - .claude/skills/debug/SKILL.md
  - .claude/skills/design/review-checklist.md
  - .claude/skills/design/interaction-patterns.md
  - .claude/skills/design/decision-log.md
  - .claude/context/validation-pipeline.md
  - playwright.config.ts
  - .github/workflows/design.yml
  - .github/workflows/client.yml
  - .github/workflows/admin.yml
keywords: [QA, product experience, QA app, test cases, UI regression, admin, PWA, editorial website, accessibility]
---

# Product Experience QA

Product-experience QA asks the one question the test suite cannot: can a real person understand,
complete, recover from, and trust this flow? Passing builds prove the code runs; a QA walk proves
the experience works. Anyone on the team can take one — if you can open the app and describe
honestly what you saw, you are qualified.

## Where QA happens

Walks are recorded in the [QA app](https://qa.greengoods.app), a shared checklist where each
tester signs in with their wallet and owns their own verdicts. Every test case gets a Pass, Fail,
or Blocked call and a short note, saved as you go:

<img src="/img/screenshots/qa-app-checklist.png" alt="The QA app checklist: one tab per surface, priority chips on each case, and Pass / Fail / Blocked buttons beside a notes field" style={{maxWidth: '100%', borderRadius: '8px', border: '1px solid var(--ifm-color-emphasis-300)'}} />

Two more places complete the picture:

- The QA app orders cases by priority or area. [Test Cases](./test-cases) is the public catalog
  behind it, adding each scenario's kind, role, and expected evidence.
- Findings land in Linear after the call (one report issue per session, fix slices as sub-issues),
  and screenshots or recordings go to the private Drive QA folder — never into public artifacts.

:::tip First walk?
Start with [`PUB-001`](./test-cases#public-website-p0): open the public site and check that public
routes use the website shell, not the PWA shell. No wallet, no role, about ten minutes.
:::

Most cases walk the product itself. The heart of Green Goods is a gardener submitting real work —
pick a garden, choose an action, add media, review, submit:

<div style={{display: 'flex', gap: '0.5rem', flexWrap: 'wrap', margin: '1rem 0'}}>
  <img src="/img/screenshots/qa-pwa-gardens.webp" alt="Gardens list in the PWA" style={{width: 'calc(25% - 0.4rem)', minWidth: '150px', flexGrow: 1, borderRadius: '8px', border: '1px solid var(--ifm-color-emphasis-300)'}} />
  <img src="/img/screenshots/qa-pwa-action-selection.webp" alt="Choosing an action for a work submission" style={{width: 'calc(25% - 0.4rem)', minWidth: '150px', flexGrow: 1, borderRadius: '8px', border: '1px solid var(--ifm-color-emphasis-300)'}} />
  <img src="/img/screenshots/qa-pwa-media.webp" alt="Adding photos to a work submission" style={{width: 'calc(25% - 0.4rem)', minWidth: '150px', flexGrow: 1, borderRadius: '8px', border: '1px solid var(--ifm-color-emphasis-300)'}} />
  <img src="/img/screenshots/qa-pwa-review.webp" alt="Reviewing a work submission before upload" style={{width: 'calc(25% - 0.4rem)', minWidth: '150px', flexGrow: 1, borderRadius: '8px', border: '1px solid var(--ifm-color-emphasis-300)'}} />
</div>

## Walking a flow

However you enter a session — on a call, solo, or paired with an agent — the loop is the same:

1. Walk the real surface with the intended identity and environment.
2. Capture each observation before diagnosing it.
3. Reproduce visible defects from the rendered surface inward.
4. Fix only bounded, reversible work that fits the agreed session scope.
5. Revalidate in the same surface and record the remaining risk.
6. Hand deferred findings to the private QA and Linear workflow.

## The QA session

A team session is a scheduled call where two or more people walk different surfaces at the same
time, recording as they go. The QA app is the shared checklist; this page is what to read before
opening it.

### Before the call {#before-the-call}

- Confirm every tester's wallet is on the QA app allowlist and each person can sign in once — a
  five-minute smoke the day before saves twenty minutes of call time.
- Pick the scope and split the surfaces. Walk priority-first (P0s before P1s), and where
  possible each person tests surfaces they did not build.
- Agree the write boundary up front: which funding and on-chain flows run end to end with real
  transactions, and which stop at the review step. The catalog's Transaction kind marks the
  cases this decision touches.
- If test cases changed since the last deploy, redeploy the QA app first — a deployment pins the
  catalog revision it shipped with.

### During the call {#during-the-call}

- Record a verdict and a short note per test case in the QA app as you walk. Screenshots and
  recordings go to the private Drive QA folder, never into public artifacts.
- The meeting notes capture the discussion on their own; nobody takes minutes. Spend the last
  half hour walking the merged fail list together and aligning on the cross-surface calls —
  those decisions are the most valuable output of the call.

### After the call {#after-the-call}

- Before everyone drops, spend five minutes on four questions: What did we cover, and what did we
  consciously skip? What worked well enough to leave alone? Which fails worry us most? Would we
  welcome a new garden onto this build tomorrow?
- Trigger the `qa-call-report` routine (or run `/qa-triage --call` at a desk). It joins the QA
  app's verdicts with the meeting notes and writes the session to Linear: one report issue per
  session, with fix slices attached as sub-issues sized for one branch and one PR each.
- Fix sessions then work slice by slice. Every fix is a repair grounded in the feature's history
  and existing structure — updating or removing code before adding any — and is done when the
  slice's test cases pass again.
- Re-QA later in the week is a scoped walk of the affected cases. Whoever recorded a fail
  re-records the pass: the standing verdict keeps each tester's most severe entry, so someone
  else passing the case does not clear it.

## What every journey must prove

- The entrypoint, role, and selected Garden or chain are clear.
- Loading, empty, disabled, error, offline, and recovery states are usable.
- A visible interaction changes state and provides legible feedback.
- Keyboard, focus, labels, touch targets, contrast, and reduced motion remain accessible.
- Protocol confirmation, indexed data, and local queue state are not conflated.
- Private identities, wallets, session data, and defect evidence stay out of public artifacts.

## Ownership and related pages

The generated [test-case catalog](./test-cases) owns public scenario definitions only.
`.claude/context/qa.md` owns recording, attribution, privacy, result rollup, and artifact
locations; the QA skills own session and triage mechanics.

- [Test Cases](./test-cases) — every scenario, grouped by priority within each surface.
- [GitHub Actions](../deployments/gh-actions) — the automated gates that run before a human ever
  walks.
- [Getting Started](../getting-started) — local setup, if you want a dev build to walk against.
