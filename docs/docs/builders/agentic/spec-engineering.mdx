---
title: Specification Engineering
sidebar_label: Spec Engineering
slug: /builders/agentic/spec-engineering
audience: developer
owner: docs
last_verified: 2026-04-11
---

import {NextBestAction} from "@site/src/components/docs";

# Specification Engineering

Specification engineering is writing structured documents that both humans and AI agents can execute. In Green Goods, specs drive agent workflows through clearly defined phases, acceptance criteria, and verification steps.

## Specification Types

### Agent Specifications (`.claude/agents/*.md`)

Define agent identity, capabilities, and constraints:

```yaml
---
name: cracked-coder
description: Implements complex features using strict TDD workflow.
model: opus
tools: [Read, Glob, Grep, Edit, Write, Bash, Task]
skills: [testing, react, contracts]
maxTurns: 50
---
```

The project maintains four agent specs:

| Agent | Purpose | Model |
|-------|---------|-------|
| `cracked-coder` | Feature implementation with TDD | Opus |
| `code-reviewer` | PR review and pattern enforcement | Opus |
| `oracle` | Architecture guidance and analysis | Opus |
| `triage` | Issue classification | Haiku |

### Template Specifications (`.claude/specs/`)

`.claude/specs/` is now a **template library**, not the live feature backlog. It stores reusable task templates that agents and humans can adapt when starting a new piece of work. Typical contents:

1. **Problem statement** -- What problem does this solve?
2. **User stories** -- Who benefits and how?
3. **Technical design** -- Architecture, data flow, dependencies
4. **Acceptance criteria** -- Testable conditions for completion
5. **Edge cases** -- Known failure modes and how to handle them

Examples in the live template library include admin view additions, shared hook work, contract feature work, indexer handler work, and offline bugfix workflows.

### Plan Files (`.plans/`)

`.plans/` is the live planning surface for active work, backlog candidates, ADRs, ideas, and archive material. When a user asks for planning without execution, the output belongs here rather than in `.claude/specs/`.

```markdown
## Plan: Cookie Jar Payout Integration

### Phase 1: Hook Layer
- [ ] Create `useCookieJarPayout` in shared
- [ ] Test: query key correctness, enabled guard, error path

### Phase 2: UI Layer
- [ ] Add PayoutPanel component in admin
- [ ] Wire to existing GardenDetail view

### Phase 3: I18n
- [ ] Add strings to en.json, es.json, pt.json
```

## Writing Effective Specs

### Testable Acceptance Criteria

Every criterion should be verifiable without human judgment:

```markdown
# Good
- [ ] `useGardenActions(undefined, chainId)` returns `{ enabled: false }`
- [ ] Calling `bun run test` passes with 0 failures
- [ ] The barrel export exists in `packages/shared/src/hooks/index.ts`

# Bad
- [ ] The hook works correctly
- [ ] Performance is acceptable
- [ ] Code is clean
```

### Scope Boundaries

Specs must define what is NOT in scope to prevent agents from over-engineering:

```markdown
## Out of Scope
- Visual design changes that need story coverage (handled through the `ui` skill and Storybook guide)
- Contract modifications (requires separate spec + security review)
- i18n for languages other than en/es/pt
```

### Dependency Mapping

For cross-package work, explicitly list which packages are touched and in what order:

```markdown
## Packages Affected
1. `packages/shared` -- New hook + tests (build first)
2. `packages/admin` -- New component consuming the hook
3. `packages/client` -- No changes
```

## Skill Specifications (`.claude/skills/`)

Skills are specialized instruction sets loaded by agents. Each skill has:

- **Activation criteria** -- When to use this skill
- **Patterns** -- Code patterns with examples
- **Constraints** -- What to avoid
- **Verification** -- How to confirm correct application

The project keeps a compact active skill set with sub-files covering domains from `testing` to `web3` to `ui`.

## Local Project Memory (`.claude/projects/`)

`.claude/projects/` is local agent memory, not tracked planning state. It should remain untracked and should not be used as a roadmap, backlog, or source of truth for feature work.

## Spec Review Checklist

Before handing a spec to an agent:

1. Are all acceptance criteria mechanically testable?
2. Is the scope bounded (explicit out-of-scope section)?
3. Are package dependencies listed with build order?
4. Does the spec reference existing patterns (Cathedral Check)?
5. Are edge cases listed with expected behavior?
6. Is the model requirement appropriate for the complexity?

## Governance

The template library in `.claude/specs/` is version-controlled alongside the agent operating docs. The live execution roadmap lives in `.plans/`. The guidance consistency check (`node .claude/scripts/check-guidance-consistency.js`) validates that agent instructions do not contradict the rules and constraints defined in `CLAUDE.md` and `.claude/rules/`.

<NextBestAction
  title="Next best action"
  why="Explore the primary AI coding tool used in the Green Goods workflow."
  actionLabel="Claude Code"
  actionHref="./claude-code"
  alternatives={[
    {label: "Intent Engineering", href: "./intent-engineering"},
    {label: "Codex", href: "./codex"},
  ]}
/>
