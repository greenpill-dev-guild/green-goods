---
title: Tokenbound Accounts
sidebar_label: Tokenbound Accounts
slug: /builders/integrations/tokenbound
audience: developer
owner: docs
last_verified: 2026-09-02
feature_status: Live
source_of_truth:
  - packages/contracts/src/accounts/Garden.sol
  - packages/contracts/src/tokens/Garden.sol
  - packages/shared/src/ontology/green-goods-ontology.json
keywords:
  - erc-6551
  - token-bound account
  - garden account
---

# Tokenbound Accounts

[ERC-6551](https://eips.ethereum.org/EIPS/eip-6551) token-bound accounts are why a Green Goods
garden is more than a database row: each garden is an ERC-721 token whose bound account is the
garden on-chain. That account holds the treasury, wears the [Hats](./hats) role tree, and receives
the garden's [attestations](./eas). Transfer the token and you've transferred the garden, assets
and history included; no app of ours has to exist for a garden to keep being real.

## Where it lives in the code

| Layer | Source |
|---|---|
| Garden token | `packages/contracts/src/tokens/Garden.sol` |
| Garden account and action routing | `packages/contracts/src/accounts/` |
| Account addresses per chain | The `accountProxy` and `gardenAccountImpl` fields in the [deployment record](../deployments/status) |

One consequence for consumers: the indexer's `Garden.id` is the garden account address, which is
why it stays a bare address rather than a composite id (a documented exception in the
[indexer package](../packages/indexer)).

## Working with it locally

The [full-stack path](../getting-started#path-b-run-the-full-local-stack) forks Arbitrum One, so
every live garden account exists locally; creating a new garden on the fork exercises the full
token-plus-account flow without gas.

## Canonical upstream docs

- [EIP-6551](https://eips.ethereum.org/EIPS/eip-6551), the standard itself; short and worth
  reading in full.
- [Tokenbound documentation](https://docs.tokenbound.org) for the reference implementation the
  ecosystem builds against.
