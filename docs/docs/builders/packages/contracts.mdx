---
title: Contracts
sidebar_label: Contracts
slug: /builders/packages/contracts
audience: developer
owner: docs
last_verified: 2026-09-02
feature_status: Live
source_of_truth:
  - packages/contracts/AGENTS.md
  - packages/contracts/package.json
keywords:
  - solidity
  - foundry
  - resolvers
  - deployments
---

# Contracts

`packages/contracts` is the Solidity protocol, built with [Foundry](https://getfoundry.sh). It
owns everything the chain enforces: garden accounts, authorization, attestation schemas and their
resolvers, and the deployment artifacts that record what exists where. The
[deployment status page](../deployments/status) is the generated view of those artifacts.

## Map

| Directory | What lives there |
|---|---|
| `src/accounts/` | Garden token-bound accounts, action routing, Celo relay and coordinator |
| `src/resolvers/` | EAS resolvers for work, approval, assessment, testimony, and yield |
| `src/modules/` | Opt-in integrations plus the pooling and settlement control planes |
| `src/registries/` | Action, commitment, credit, deployment, ENS, power, GreenWill |
| `src/tokens/` | The Garden and Goods tokens |
| `src/Schemas.sol` | EAS schema definitions |
| `deployments/` | Per-chain artifacts, the canonical record of what is deployed |

## Run and test

```bash
cd packages/contracts
bun run build
bun run test
```

For one behavior, `bun run test:match -- test/<path>.t.sol` is the fast loop; add
`bun run build:target -- src/<path>.sol` when the compiled surface moves. The full audit suite is
`bun run test:audit:full`.

## How it connects

Contracts sit at the top of the dependency arrow. Their events feed the
[indexer](./indexer)'s read models, their ABIs and addresses reach the apps through
[shared](./shared), and their EAS schemas give the [attestation layer](../integrations/eas) its
meaning. The [Architecture page](../architecture) shows the whole loop.

## Conventions that will bite you

- Deploys, upgrades, and migrations go through package scripts, never raw `forge` commands. The
  root `contracts:*:arbitrum` wrappers set the keystore account for you.
- A deployment artifact records that something was deployed, not that it is live and healthy.
  Never present an address as an active feature without separate evidence.
- Storage-layout, contract-size, and fork checks are selected by the root validation pipeline for
  risky changes; when selected, they all have to pass.
- Shared Solidity helpers live in `src/lib/` (EAS, strings, token-bound accounts); reach for them
  before writing new ones.

## Go deeper

- [`packages/contracts/AGENTS.md`](https://github.com/greenpill-dev-guild/green-goods/blob/develop/packages/contracts/AGENTS.md)
  is the full package contract, deployment lanes included.
- [Deployment Status](../deployments/status) for what's recorded per chain.
- [Data Model & Ontology](../architecture/data-model) for how contract records relate.
