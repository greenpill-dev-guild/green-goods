# Growth Pulse — Week 2026-22

_Generated by the `growth-pulse` weekly routine on 2026-05-25 (UTC)._

> **Baseline now live.** Week 2026-21 was the first run (no prior baseline); this run compares against it, so week-over-week (WoW) and month-over-month (MoM) deltas are populated. The prior digest lives on the unmerged branch `claude/growth-pulse/2026-21` (its PR has not yet merged to `develop`), so it was read from that branch as the baseline.

**Sources**: PostHog (App project `163591`, Admin project `262122`), Envio indexer (Arbitrum `42161`), Arbitrum RPC, Google Calendar. PostHog reads resolved by curated-question name from `.claude/skills/posthog-questions/SKILL.md`, HogQL run verbatim via the PostHog query API (connector tool not provisioned — see Known setup failures).

## Onboarding funnel — `funnel.onboarding` (30d, App)

| Step | Count | Rate | WoW |
|---|---|---|---|
| Registered (passkey) | 9 | — | 10 → 9 (−10%) |
| Joined a garden | 3 | 33.3% of registered | 30.0% → 33.3% (+3.3pp) |
| First work submitted | 1 | 33.3% of joiners | 33.3% → 33.3% (flat) |

No step dropped >25% absolute WoW → **funnel-breakage threshold not crossed**. The register→join rate actually improved.

**Cohort caveat** (per question definition): this funnel tracks only the new-user cohort registered within the 30d window. Raw counts over the same window — `auth_passkey_register_success` = 11 events / 9 distinct persons, `garden_join_success` = 8, `work_submission_success` = 8 — show joins and work submissions are also coming from returning users who registered before the window. The 33% cohort conversion is therefore **not** a breakage signal.

## Early retention — `funnel.work-repeat` (30d → 7d, App)

| Metric | Value | MoM |
|---|---|---|
| First-time work submitters (30d) | 6 | 6 → 6 (flat) |
| Repeated within 7d | 2 | 2 → 2 (flat) |
| Repeat rate | 33.3% | 33.3% → 33.3% (flat) |

Repeat rate flat WoW → **retention-cliff threshold not crossed** (>15% absolute MoM drop required).

## Per-garden engagement — `gardens.engagement-summary` (7d, App)

Two gardens emitted member activity in the last 7d; neither recorded a work submission:

| Garden | Active members 7d | Work submitted 7d | Work approved 7d |
|---|---|---|---|
| `0xf401f343…858a` (Green Goods Community Garden) | 1 | 0 | 0 |
| `0x35722eEd…E0dB` (Greenpill Nigeria) | 1 | 0 | 0 |

Top-3-by-work: **none** — zero work submissions across all gardens in the last 7d.

`gardens.operator-activity` (7d, App): no approvals or rejections recorded.

## Dormancy — `gardens.dormant` (App; corroborated on-chain)

| Band (no work in…) | Gardens | WoW |
|---|---|---|
| 7d+ | 1 | 1 → 1 (flat) |
| 14d+ | 0 | 0 → 0 (flat) |
| 30d+ | 7 | 7 → 7 (flat) |

- **7d+**: `0x1121218D…0338` (DeCleanup Network Action garden — last work 2026-05-12).
- **30d+** (last work date): `0x2A13Bb15…`(2026-01-13), `0x83fD7aA9…`(2026-01-18), `0xc5c54f33…`(2026-01-29), `0xB294c561…`(2026-02-08), `0xf401f343…`(Green Goods Community Garden, 2026-03-27), `0x3f0f1551…`(Greenpill Kenya, 2026-04-07), `0x35722eEd…`(Greenpill Nigeria, 2026-04-23).
- Dormant-surge **threshold not crossed**: the 30d+ band is unchanged at 7 (a surge requires +≥3 WoW).
- On-chain (indexer, Arbitrum `42161`): **18 gardens, 64 gardeners** total — unchanged WoW.

## Action template trend — on-chain indexer `Action` (Arbitrum `42161`)

> `actions.template-creation-rate` (PostHog) is **BLOCKED** (`posthog-questions` SKILL v1.2.0) — its event `admin_action_create_success` is defined but unwired and never fires. The indexer `Action` entity (`createdAt`) is the source of truth (tracked in **PRD-550**).

- **Created last week (7d)**: 0
- **Last 12 weeks**: 1 (`42161-22` "Repair Event", 2026-03-27)
- **Most recent template**: `42161-22`, created **2026-03-27** — **58 days (~8 weeks) ago**.
- **4-week trend**: ↓ flat at zero.

12-week per-week count of `Action.createdAt`:

| ISO week (Mon) | Templates created |
|---|---|
| 2026-W22 (05-25) | 0 |
| 2026-W21 (05-18) | 0 |
| 2026-W20 (05-11) | 0 |
| 2026-W19 (05-04) | 0 |
| 2026-W18 (04-27) | 0 |
| 2026-W17 (04-20) | 0 |
| 2026-W16 (04-13) | 0 |
| 2026-W15 (04-06) | 0 |
| 2026-W14 (03-30) | 0 |
| 2026-W13 (03-23) | 1 |
| 2026-W12 (03-16) | 0 |
| 2026-W11 (03-09) | 0 |

(The 22-template seed batch on 2026-02-19 predates the 12-week window. 23 templates total on Arbitrum.)

**Action-template stall anomaly** (most recent `createdAt` > 21 days) is crossed (58 days). It is **already tracked by the open issue PRD-550** (action-template scope, `package:admin`), filed by this routine last week and covering both the telemetry gap and the on-chain behavioral stall. Per the dedupe rule, this run **appended a refresh comment to PRD-550** rather than opening a parallel issue.

## Conversion-kill failures — `failures.conversion-kill` (7d; App + Admin merged)

| Step | Failure % | Failed / attempts | WoW |
|---|---|---|---|
| garden_join | 0% | 0 / 2 | flat |
| auth_passkey_register | 0% | 0 / 1 | flat |
| work_submission | — | 0 attempts | flat |
| work_approval | — | 0 attempts (App + Admin) | flat |

No step crossed the anomaly threshold (failure > 50% **and** failed count ≥ 5). Admin project `262122` returned zero `work_approval_*` rows in 7d (consistent with last week); the Admin project is healthy overall, so this is genuinely low approval volume, not a project-wide wiring outage.

## Funnel health context

The funnel is **thin** — 9 registered persons in 30d, only 3 instrumented conversion events in the last 7d (2 `garden_join`, 1 `auth_passkey_register`), 0 work submissions in 7d. Clean failure rates here reflect very low *instrumented* volume, not an absence of friction. The Product team carries open Urgent/High `activity:qa` + `protocol:green-goods` defects on the auth / PWA-onboarding / garden-navigation surfaces that plausibly suppress conversion **before** users reach instrumented `*_failed` paths:

- **PRD-508** (Urgent/P0) — Admin garden switch hangs the entire UI.
- **PRD-497** (Urgent) — Connect Wallet button persists after connect, blocks all wallet transactions.
- **PRD-507** (High) — cannot log back in after passkey logout.
- **PRD-505** (High) — passkey created in browser doesn't transfer to PWA on same device.
- **PRD-506** (High) — "Log in with Google" errors on first attempt.
- **PRD-509 / PRD-510** (High) — garden-join navigation/refresh defects.
- **PRD-499 / PRD-502 / PRD-503 / PRD-500** (High) — PWA install / update friction.

This is commentary, not an anomaly — no new issue filed for the correlation. Read the thin funnel against this backdrop.

## Anomalies opened this run

- **0 new issues.** The only crossed threshold (action-template stall, on-chain) deduped to the existing open **PRD-550**, which was refreshed with a comment (now 58 days / 0 templates in 7d / 1 in 12w).
- **Not fired**: funnel breakage (register→join improved), retention cliff (flat), dormant-garden surge (30d+ band flat at 7).
- Open anomaly backlog on Product (`protocol:green-goods` + `activity:qa`): **31** (unchanged WoW).

## Calendar context (2026-05-18 → 2026-06-08)

- Product / engineering cadence: Product Sync (05-20, 05-27), Tech & Sun Weekly Sync (weekly), Community Chat (05-19, 05-26), Builder Space (05-29).
- Funding-adjacent: **Capital Sync** (recurring — 05-25, 06-08). Internal funding cadence, not a grant report deadline.
- **No grant report / milestone / external demo / deadline** in the next 14 days → **no `#funding` cross-post** this week. No grant-stated KPI crossed a milestone (2 active gardens vs. a "10 active gardens" type target; ~0 actions/week), and no funnel step improved >25% MoM.

## Vault & yield (indexer, Arbitrum `42161`)

- `GardenVault` / `VaultEvent` history present (lifetime); no new vault activity surfaced in the last 7d. (Carried as context, unchanged from last week's read.)
- Yield-split pipeline: no `YieldAllocation` activity this week.

## Known setup failures

- **PostHog connector tool not provisioned** in this routine environment (`green-goods-routines-extended`). Curated questions were resolved by running the canonical HogQL from `posthog-questions/SKILL.md` **verbatim** against the PostHog query API with `POSTHOG_API_KEY` — real data, no fabrication. (Known since 2026-W21.)
- **Discord connector tool not provisioned.** The primary `#product` post was delivered via the Discord bot REST API using `DISCORD_BOT_TOKEN` + `DISCORD_PRODUCT_CHANNEL_ID`. (New formalization this run; the same bridging was implied last week. Not a novel functional failure — posting succeeded.)
- `LINEAR_API_KEY` env var unset, but the Linear MCP connector is wired via OAuth and functioned normally (read, dedupe, comment). Not a functional failure.

These are persistent/known environment gaps, surfaced without an `@mention` per the routine's alert-fatigue rule.

## Notes

A quiet, healthy week: every delta-based threshold held (funnel improved or flat, retention flat, dormancy flat), so no new anomalies were filed. The one standing concern remains the on-chain action-template stall (now ~8 weeks with no new template) — refreshed on PRD-550 rather than re-filed. The thin instrumented funnel should be read against the open Urgent/High auth + PWA-onboarding defect backlog (PRD-508/497/507/505/506), which plausibly suppresses conversion upstream of the tracked steps.
