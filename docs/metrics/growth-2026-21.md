# Growth Pulse — Week 2026-21

_Generated by the `growth-pulse` weekly routine on 2026-05-24 (UTC)._

> **First run.** No prior digest exists, so week-over-week (WoW) and month-over-month (MoM) deltas have no baseline this week and all delta-based anomalies are deferred. Trends populate from week 2026-22 onward.

**Sources**: PostHog (App project `163591`, Admin project `262122`), Envio indexer (Arbitrum `42161`), Arbitrum RPC, Google Calendar. PostHog reads resolved by curated-question name from `.claude/skills/posthog-questions/SKILL.md`.

## Onboarding funnel — `funnel.onboarding` (30d, App)

| Step | Count | Rate |
|---|---|---|
| Registered (passkey) | 10 | — |
| Joined a garden | 3 | 30.0% of registered |
| First work submitted | 1 | 33.3% of joiners |

WoW: first run, no baseline.

**Cohort caveat** (per question definition): this funnel tracks only the new-user cohort registered within the 30d window. Raw counts over the same window — `auth_passkey_register_success` = 12 events / 10 distinct persons, `garden_join_success` = 8 — show joins are also coming from returning users who registered before the window. The 30% cohort conversion is therefore **not** a breakage signal.

## Early retention — `funnel.work-repeat` (30d → 7d, App)

| Metric | Value |
|---|---|
| First-time work submitters (30d) | 6 |
| Repeated within 7d | 2 |
| Repeat rate | 33.3% |

MoM: first run, no baseline.

## Per-garden engagement — `gardens.engagement-summary` (7d, App)

Two gardens emitted member activity in the last 7d; neither recorded a work submission:

| Garden | Active members 7d | Work submitted 7d | Work approved 7d |
|---|---|---|---|
| `0xf401f343…858a` | 1 | 0 | 0 |
| `0x35722eEd…E0dB` | 1 | 0 | 0 |

Top-3-by-work: **none** — zero work submissions across all gardens this week.

`gardens.operator-activity` (7d, App): no approvals or rejections recorded.

## Dormancy — `gardens.dormant` (App; corroborated on-chain)

| Band (no work in…) | Gardens |
|---|---|
| 7d+ | 1 |
| 14d+ | 0 |
| 30d+ | 7 |

- **7d+**: `0x1121218D…0338` (last work 2026-05-12).
- **30d+** (last work date): `0x2A13Bb15…`(2026-01-13), `0x83fD7aA9…`(2026-01-18), `0xc5c54f33…`(2026-01-29), `0xB294c561…`(2026-02-08), `0xf401f343…`(2026-03-27), `0x3f0f1551…`(2026-04-07), `0x35722eEd…`(2026-04-23).
- On-chain (indexer, Arbitrum `42161`): 18 gardens, 64 gardeners total. Dormant-surge anomaly **not evaluated** (no prior-week baseline).

## Action template trend — `actions.template-creation-rate` (Admin)

- **PostHog**: 0 `admin_action_create_success` in 12w (and 52w). **The metric is unwired** — the event constant is defined but never fired in code. 4-week trend: → flat at zero, telemetry-blind. Filed as **PRD-550**.
- **On-chain** (Arbitrum `42161`): most recent template is `42161-22` "Repair Event", created **2026-03-27** — **no new template in ~8 weeks**. 23 templates total on Arbitrum.

The behavioral stall (on-chain) and the telemetry gap (PostHog) are independent and both real.

## Conversion-kill failures — `failures.conversion-kill` (7d; App + Admin merged)

| Step | Failure % | Failed / attempts |
|---|---|---|
| garden_join | 0% | 0 / 2 |
| auth_passkey_register | 0% | 0 / 1 |
| work_submission | — | 0 attempts |
| work_approval | — | 0 attempts (App + Admin) |

No step crossed the anomaly threshold (failure > 50% **and** failed count ≥ 5).

**Caveat**: clean failure rates here reflect very low *instrumented* volume, not an absence of friction. The Product team carries open P0 / auth / onboarding defects — e.g. PRD-508 (admin garden-switch hangs the UI), PRD-497 (Connect Wallet button blocks transactions), PRD-507 (cannot log back in after passkey logout), PRD-505 (passkey doesn't transfer to PWA), PRD-506 (Google login fails on first attempt) — that cause silent upstream drop-off *before* users reach instrumented `*_failed` paths. Read the thin funnel against that backdrop.

## Anomalies opened this run

- **PRD-550** — Telemetry gap: `admin_action_create_success` never emitted; action-template metric blind. Unprojected on Product; labels `protocol:green-goods` + `activity:qa` + `package:admin` + `agent:routine` + `task:evidence`. (1 of 3 anomaly cap used.)
- **Not fired** (require WoW/MoM baseline, unavailable on first run): funnel breakage, retention cliff, dormant-garden surge.
- Open anomaly backlog on Product (`protocol:green-goods` + `activity:qa`): **31** including this one.

## Calendar context (2026-05-12 → 2026-05-24)

- Ecosystem / funding-adjacent: Octant Town Hall (weekly — 05-14, 05-21), Octant × Gitcoin Live Stream (05-14), Greenpill Growth Sync (05-12), Capital Sync (05-11).
- Product cadence: Product Sync (weekly), Gardens Core Weekly Sync.
- **No grant report / milestone / deadline** in the next 14 days → **no `#funding` cross-post** this week.

## Vault & yield (indexer, Arbitrum `42161`)

- 47 vault events lifetime; most recent ~mid/late April (a WITHDRAW plus several DEPOSITs); none in the last ~30 days.
- **0 yield allocations recorded ever** (`YieldAllocation` empty) — the yield-split pipeline has produced no allocations.

## Notes

First growth-pulse run, so these numbers are a baseline rather than a trend and every delta-based anomaly is deferred to next week. The headline finding is that the action-template metric is structurally blind (PRD-550) while on-chain template creation has independently stalled ~8 weeks. The PostHog connector tool was unavailable in this environment, so curated questions were resolved against the documented HogQL via the PostHog query API directly — real data, no fabrication.
