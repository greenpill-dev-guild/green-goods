[tools]
node = "22.22.1"
# Keep local setup exactly aligned with packageManager and GitHub Actions.
bun = "1.3.14"
# Forge formatter output is version-sensitive. Keep local setup and CI on the
# exact release that owns the checked-in Solidity format.
foundry = "1.7.1"
