# Design Decision Log

Append-only ledger of locked design and UX decisions. This is the canonical `DL-NNN` registry:
any `DL-NNN` cited anywhere in the design skill or `.claude/rules/frontend-design.md` must exist
as a row here (checked by `bun run check --only guidance-links`).

**Append paths**: the `qa-session` skill's close-phase decision lock gate, or any design round
where the user locks a decision. **IDs are never reused** — supersede, don't delete.

**Status values**:

- `locked` — binding on all design work immediately; not yet written into a canonical guidance
  surface. Agents doing design work check locked rows touching their surface.
- `codified` — the rule now lives in a canonical surface (the `Codified into` column points at
  it); the row remains as the citation anchor.
- `superseded` — replaced; the `Codified into` column names the superseding `DL-NNN`.

## Graduation ladder

1. **Locked** (at session close or design-round end): append the row with a one-sentence
   contract-style decision, the rationale, `Status: locked`, `Codified into: —`.
2. **Codified** — when the decision is a reusable rule rather than a one-view outcome, rewrite
   it into the owning surface and flip the row's status:
   - admin interaction behavior → [interaction-patterns.md](./interaction-patterns.md)
   - cross-surface implementation rules → `.claude/rules/frontend-design.md`
   - AI-design-tool vocabulary and bans → [prompt-contract.md](./prompt-contract.md) /
     [client-prompt-contract.md](./client-prompt-contract.md) § Never Use
   - token or visual-language changes → root `DESIGN.md` + [language.md](./language.md)
     (bump `token_version` per [ARCHITECTURE.md](./ARCHITECTURE.md) § Version Coupling)
   The codified text cites its `DL-NNN` inline so the provenance stays traceable.
3. **Mechanical check** — only after a rule is violated post-codification, add enforcement at an
   existing point (a guard test beside the component, a `contracts[]` entry in
   `scripts/quality/check-skill-behavior-contracts.mjs`, or the design-token lint) — never a new
   standalone script.

## Ledger

| ID | Date | Decision | Rationale | Status | Codified into |
|----|------|----------|-----------|--------|---------------|
| DL-001 | 2026-04-07 | Interaction model is a complement: lift-and-press for cards, shape morph for buttons | Different elements get different physics; richer tactile vocabulary | codified | [language.md](./language.md) § Design Decisions Log |
| DL-002 | 2026-04-07 | Motion uses named spring tokens, never hardcoded beziers | Semantic names enable motion-scheme switching; consistent vocabulary | codified | [language.md](./language.md) § Motion System |
| DL-003 | 2026-04-07 | Button shape is context-dependent: capsule = primary, squircle = secondary | Shape as emphasis hierarchy; capsule draws the eye, squircle recedes | superseded | DL-026 |
| DL-004 | 2026-04-07 | Component vocabulary scope is the admin-relevant subset, extended only as needed | Focus on what the revamp needs now; extend vocabulary later | codified | [language.md](./language.md) § Component Patterns |
| DL-005 | 2026-04-07 | `language.md` is a comprehensive standalone spec | Self-contained enough to guide implementation without file-hopping | codified | [language.md](./language.md) |
| DL-006 | 2026-04-07 | Spatial architecture integrates deeply: all beziers → tokens, radii → concentric types | Spatial architecture is the first Warm Earth consumer; coherence matters | codified | [language.md](./language.md) § Design Decisions Log |
| DL-007 | 2026-08-16 | Admin flows reuse the client composer grammar (what → how much → proof/protection → sectioned review, with an Advanced detour); admin gets denser fields, never parallel step patterns | Reuse shipping rhythms; one flow grammar across surfaces | codified | [interaction-patterns.md](./interaction-patterns.md) § 3 |
| DL-008 | 2026-08-16 | Workspace tabs that earn it use a two-column split — left column majority width, ~300–340px right rail — collapsing below ~900px with nothing hidden | Operational density with a stable status rail; no hidden critical data (pool-tab decision) | codified | [interaction-patterns.md](./interaction-patterns.md) § 4 |
| DL-009 | 2026-08-29 | Dark filled actions are tonal: the workspace `-200` fill with `-900` ink and a lighter `-100` hover; the deep-fill + white-text dark pair is retired | Afo locked it during the design-system review round — M3-dark convention and typical dark-mode reading beat the 2026-07 keep-saturation ruling; measured AA 4.58–11.74:1 across the five tones | codified | [language.md](./language.md) § Dark Mode Palette |
| DL-010 | 2026-08-29 | The admin FAB is a capsule at both sizes (`rounded-full`: 48px dock circle, 56px extended floating); the "FAB-large is 16px" clause is struck — capsule is the 9999 step of the fixed admin radius set | Shipped `Shell/FabButton` never implemented a 16px large variant, and the clause rode through two token bumps unchallenged; spec follows shipped truth (round-3 fix-everything lock) | codified | [language.md](./language.md) § Shape System |
| DL-011 | 2026-08-29 | The cockpit rides a compact control metric — 28/32/36/40/44: buttons 28/32/40 (sm/md/lg, `admin-hit-target` preserves 44px), fields 44 with 14px text, inline field 32 on the md-button axis, toolbar pills/tabs 36, chips 32, identity pill 36. Shell chrome (AppBar 56, FAB per DL-010, nav dock) is deliberately outside the scale | Afo locked "compact scale" in the critique round — the M3-default 40/56 proportions read oversized in a steward workbench, and the 40dp AdminInlineField already existed as the escape hatch; guard test `AdminDensityScale.guard` pins the tiers | codified | [packages/admin/DESIGN.md](../../../packages/admin/DESIGN.md) § M3 Strict Anatomy |
| DL-012 | 2026-08-29 | Button/action labels are Title Case in English across admin and client ("Create Garden", "Submit Work"); es/pt keep their native sentence-style casing; field labels, helper text, banners, and body copy stay sentence case | Afo locked it in the critique round, reversing the sentence-case rule — the ~20 pre-existing Title Case strings (every "Create Garden") were the ones that read right; 345-string en sweep applied | codified | [interaction-patterns.md](./interaction-patterns.md) § Voice |
| DL-013 | 2026-08-30 | The Title Case boundary is *named things and named acts*: card/section/dialog/flow/step titles and action labels (incl. aria-only action labels, uniformly) take Title Case; status and outcome copy ("Connection lost", "Update needs a restart"), empty/connect-state titles, kickers/eyebrows, placeholders, field labels, and validation stay sentence case — even when title-shaped keys hold them | The post-DL-011/012 adoption audit found both sweep tails (~95 missed actions, ~24 over-swept statuses) and a 7-vs-7 pool dialog-title split; the correction batch closed them and this row pins the boundary the sweep tripped on | codified | [interaction-patterns.md](./interaction-patterns.md) § Voice |
| DL-014 | 2026-09-12 | Installed-PWA bottom sheets use one height scale: `compact` (content-sized, 50% at most), `half` (50%), `tall` (70%), `full` (85%), plus full-screen viewers; each sheet names its tier and never sets its own height | Afo flagged too many sheet heights (15 distinct values at 390 x 844, from 22% to 85%); a named scale keeps sizes deliberate, and capping `compact` at the half height adds no extra percentage while keeping longer es/pt confirmations' actions on small phones | codified | [DESIGN.pwa.md](../../../packages/client/DESIGN.pwa.md) § Layout philosophy |
| DL-015 | 2026-09-12 | The installed PWA's bottom AppBar hides whenever any sheet or dialog is open; sheet primitives register themselves while open instead of the AppBar listing specific sheets | The AppBar hid for five named drawers only, so it sat over the Notifications sheet and the Profile photo sheet's only action (Afo: "a major regression"); self-registration covers every present and future sheet | codified | [DESIGN.pwa.md](../../../packages/client/DESIGN.pwa.md) § Layout philosophy |
| DL-016 | 2026-09-12 | Installed-PWA sheet actions live in one shared action bar pinned to the bottom of the sheet: below 640px they stack full width with the primary on top, the second action (Cancel included) is an outlined button, a destructive primary uses the error fill, and step navigation (Back / Continue) keeps its one row; from 640px the same bar is one right-aligned row with the primary rightmost | Afo picked stacked-primary-on-top, outlined Cancel, and the wide-screen row from live mocks after flagging six inconsistent action layouts, most scrolling with the sheet body. Measured on 10 two-button sheets in en/es/pt plus a German stress test: one row cut labels off in up to 22 of 40 bars at 360px with the medium Button, English included, while stacking cut none from 320 to 412px and Back / Continue never cut | codified | [DESIGN.pwa.md](../../../packages/client/DESIGN.pwa.md) § Layout philosophy |
| DL-017 | 2026-09-12 | Any green fill that carries text, a number, or a glyph (filled CTAs, count badges, step markers, selected chips, pills) uses `primary-action` with white `primary-action-foreground`; bright `tertiary` green stays on text-free marks only, and the token audit treats any text colour on a bright green fill as a contrast risk with no exceptions | Afo found the dark-green-on-bright count badges and step numbers hard to read and asked for white. White on today's bright green measures 2.36:1 and the dark pair 4.64:1, while white on `primary-action` is 5.72:1 in both app themes, so the fill moves rather than only the text; picked option A with chips, pills, and the Send and Withdraw buttons in scope (PWA Paper Cuts review) | codified | Root `DESIGN.md` § Relationship to Codebase; [language.md](./language.md) § Dark Mode Palette (state roles); [DESIGN.pwa.md](../../../packages/client/DESIGN.pwa.md) § Color Adaptation |
| DL-018 | 2026-09-12 | Installed-PWA form fields reserve two lines (`min-block-size: 2lh`) under the control for a hint or error and sit 8px apart; a longer message grows the slot rather than scrolling inside it | The 6 Sep three-line reserve left 68px between a control and the next label in Submit Work › Details. Afo chose two lines because many real messages wrap on a 360px phone in en/es/pt, with a smaller gap so a two-line error still sits close to the next field | codified | [DESIGN.pwa.md](../../../packages/client/DESIGN.pwa.md) § Forms and Lists |
| DL-019 | 2026-09-12 | Work and draft list cards use a fixed 88px square thumbnail with the photo absolutely positioned inside it | The work card's thumbnail had a width but no height, so portrait photos stretched cards to about 109px and phone photos to about 144px instead of 90px; 88px matches the existing draft card, and Afo asked for both cards to behave the same | superseded | DL-034 |
| DL-020 | 2026-09-12 | The garden page's Share action is a 32px icon button in the banner's top-right action stack, placed last; the title row keeps at most one text action (Join Garden or Request to Join) at the client Button `compact` size: 32px tall, 14px label, no icon, 48px tap area | Share Garden and the join action were two full-size labeled buttons beside the location and founded date, leaving about 80px at 390px (38px in Portuguese) so the date spilled under Share. Afo asked for compact buttons that sit with that text, and M5 (Share in the banner) won the round-2 mockups; Share stays because the installed app has no address bar | codified | [DESIGN.pwa.md](../../../packages/client/DESIGN.pwa.md) § Layout philosophy |
| DL-021 | 2026-09-12 | Button shape follows emphasis and nothing else: a primary (filled, error fill when destructive) is a capsule, a secondary (outlined, Cancel included) is the 12px squircle from the new DesignMD `rounded.squircle` step, a tertiary is text with a 12px fill on hover and focus and a 44px hit area, an icon-only button is a circle, and a chip is a capsule. While pressed a capsule or circle tightens to 12px and a squircle to 8px, with no morph under reduced motion. The shared `Button`, `IconButton`, and `Chip` encode it and have no shape prop | DL-003 was codified in April but encoded nowhere: `language.md` named `rounded-xl` for the 12px squircle after DesignMD had made that token 20px, so secondary buttons shipped at 20px, which is a capsule at 40–46px, and only 22 of 62 PWA primaries were capsules. Afo chose a 12px token over amending DL-003 to 16px because 16px collapses into a capsule at the 32px compact size | superseded | DL-026 |
| DL-022 | 2026-09-12 | Fields (text inputs, textareas, selects, select triggers, amount inputs) are a 16px rounded rectangle; an action inside a field is concentric, 12px with a 4px inset | `.gg-control` had drifted to 20px, so fields read as pills beside capsule buttons and their neighbors mixed 8px, 16px, and 20px corners; `language.md` already said 16px | codified | [language.md](./language.md) § Radius Scale |
| DL-023 | 2026-09-12 | Buttons and fields share one height scale: lg 48 (page-level primaries, public hero actions), md 44 (the default: sheets, forms), sm 40 (dense rows, public row actions), compact 32 (text-adjacent actions, header icon buttons) with a 48px hit area; no action is shorter than 32px and no hit area is smaller than 44px | Buttons rendered 46 and 50px while fields rendered 44 and 48px, so every field-and-button row was 2px off; 28px Copy buttons and 13px dismiss controls sat below the 44px hit target | codified | [language.md](./language.md) § Button System |
| DL-024 | 2026-09-12 | The public site uses the same button and field system (DL-021 to DL-023), vault and cookie flows included; its cards, dialogs, and panels stay square as editorial surfaces, and its underline field is the documented editorial field surface | The public site mixed capsule editorial atoms at seven heights with square 0px/48 vault buttons, while `DESIGN.browser.md` promised rounded dialogs and one Install App CTA; Afo kept the square editorial surfaces and moved the actions onto the shared system | codified | [DESIGN.browser.md](../../../packages/client/DESIGN.browser.md) § Buttons and Fields |
| DL-025 | 2026-09-12 | Client buttons and controls come only from the shared family (`Button`, `IconButton`, `Chip`, `TextInput`, `Textarea`, `NativeSelect`, `Switch`, `FormattedAmountInput`); a raw element is allowed only when it declares why (a `role` of tab, switch, or radio, a `data-pressable` card, row, scrim, media, trigger, fab, or tab, or an input of type file, radio, checkbox, hidden, or range), and no shape or height class rides on the shared family | 198 raw buttons in 96 client files and 57 raw controls had re-created every variant by hand, which is how the shapes drifted; a rule without a guard had already failed once | codified | [frontend-design.md](../../rules/frontend-design.md) § Rule 19; enforced by `rule-19-client-shared-controls` in `bun run check --only react-patterns` |
| DL-026 | 2026-09-13 | Buttons take one corner per surface, whatever their emphasis: the 12px squircle (`rounded.squircle`) in the installed app and 16px (`rounded.lg`) on the public website, one step tighter while pressed (8px and 12px). Emphasis comes from fill, outline, and colour alone. Icon buttons stay circles and chips stay capsules. Supersedes DL-003 and the shape clause of DL-021 | Applied everywhere, DL-003 put a capsule primary beside a 12px secondary in every button pair, while on develop each pair had shared one shape. Afo chose 12px for the app and 16px for the website after the pre-merge screenshots | codified | [language.md](./language.md) § Button Corners and Emphasis; [frontend-design.md](../../rules/frontend-design.md) § Rule 19 |
| DL-027 | 2026-09-13 | The public `/fund` page always shows Donate beside Endow: every Garden row carries a Donate primary and an Endow warm secondary (40px, DL-023), and § 02 Ways to Support explains both paths. Donate is never hidden; a Garden without a donation jar keeps the button, and its funding card says donations are not enabled yet | `DESIGN.browser.md` still hid public Donate from `/fund` for the June 1 vault sprint after the release readiness pass had put Donate back on every row, so the brief told agents to remove a live action. Afo ruled that Donate stays on the page | codified | [DESIGN.browser.md](../../../packages/client/DESIGN.browser.md) § /fund |
| DL-028 | 2026-09-14 | Every installed-PWA sheet and dialog renders one header (`SheetHeader`): a grip that drags to dismiss on every bottom sheet, an 18/600 title on a 24px line and an optional 14/400 description on 20px that wrap rather than clip, no leading icon (the tone lives in the filled action and the dialog role), a 44px borderless Close labelled "Close" on the title's line, no rule under the header (a hairline shows only while the body scrolls under it), and a tab rail directly below it with its own rule; the title names the dialog, and the client stylesheet carries no element type rules so the header renders as declared. Below the header, every heading in a sheet body (section headings, group labels over lists, and empty, error, and success state titles) renders one style through `SheetHeading`: 14/600 on a 20px line in the strong text colour, sentence case. The steward feedback drawer on a work page keeps its drawer and action bar and renders `SheetHeader`. Filter Gardens moves to `full` and Profile Photo to `half`; the tier scale of DL-014 stays | Afo locked it on 14 Sep after the sheet anatomy round: no sheet rendered its declared header, because the client's utilities-layer `h2` and `p` rules inflated the shared title to 24px bold on a 24px line and the AppSheet utilities read tokens nothing defines (Wallet's title rendered 16px regular, like its subtitle), while three hand-copied headers and an icon only on danger confirmations kept the families apart. He kept the four tiers, chose the declared 18/600 title over the accidental 24px, no icons over a danger-only square, and search plus domain chips to fill the full-tier filters. On 15 Sep an in-app census of 25 sheet states found every header uniform but five heading styles in sheet bodies (14/600 muted, 14/600 strong, 12/500 capitals, and 14/500 and 16/500 state titles) and the feedback drawer's own 14/500 header; he chose one body heading style and the shared header in the drawer, confirmed the only title icons left were in develop-era captures, and left the website panels for their own round | codified | [DESIGN.pwa.md](../../../packages/client/DESIGN.pwa.md) § Layout philosophy |
| DL-029 | 2026-09-14 | Buttons take the 16px field corner in the installed app (`rounded.lg`, 12px while pressed) so a button and the field beside it share one shape, and no corner on the public website (`rounded.none`, no press morph) to match its square editorial cards, dialogs, and panels. Icon buttons stay circles and chips capsules on both. The legacy `variant` contract and its `gg-button-<variant>` classes are retired; EmptyState takes a ready action element on surfaces with their own button family, and toast actions are tertiary. Supersedes the values in DL-026; its one-corner-per-surface principle stands | Afo (2026-09-14) after the Storybook design-system review: the 12px squircle beside 16px selects read as two shapes, and 16px website buttons fought the editorial squares. At the 32px compact size 16px is a capsule; accepted, since compact actions sit beside text, not fields | codified | [language.md](./language.md) § Button Corners and Emphasis; [frontend-design.md](../../rules/frontend-design.md) § Rule 19 |
| DL-030 | 2026-09-14 | The cockpit standardizes its controls: one 14px label on every AdminButton size, a 44px finger box on the whole 40px tier (lg buttons and icon buttons, dialog and side-sheet close buttons, AppBar icons, the search clear button) via `admin-hit-target-lg`, tabs on the 36px tier, `rounded-xl` / `rounded-2xl` landing on 16px through an unlayered remap, and a responsive field tier: 44px with 16px text below 640px (touch, matching the installed app) and 40px with 14px text from 640px, with the 12px floating label. `cn()` registers the custom `text-*` size utilities with tailwind-merge so a size alias survives beside a colour class | Afo (2026-09-14): "admin looks good, just standardized and clean things up"; admin inputs were too large and unbalanced against the 28 / 32 / 40 buttons, and the 2026-09-13 measurements found 25 class merges dropping a type size, 40px controls without a finger box, 38px tabs, and 20 / 24px corners | codified | [packages/admin/DESIGN.md](../../../packages/admin/DESIGN.md) § Cockpit M3 1a Invariants; [frontend-design.md](../../rules/frontend-design.md) § Rule 18 |
| DL-031 | 2026-09-15 | Shared components rendered inside the admin cockpit ride the shared family, and the cockpit sets that family's tokens. `Button`, `IconButton`, `Chip`, `Switch`, and the `gg-control` classes read `--gg-button-block-*`, `--gg-button-inline-*`, `--gg-icon-size-*`, `--gg-hit-block`, and `--gg-label-*` beside the corner and weight tokens, with the app scale as the fallback; admin `index.css` maps them onto the compact metric (lg and md land on 40, sm on 32, compact on 28; pills; one 14px / 500 label; a 44px finger box), the admin control surface takes the responsive field tier (44px / 16px on touch widths, 40px / 14px from 640px) and the switch the M3 52 × 32 track. FileUploadField's upload well and the DatePicker trigger are shared controls with a `surface` prop, ConfidenceSelector is shared radio chips, AudioRecorder's controls are the shared Button, ImagePreviewDialog's app controls are the shared IconButton, and every switch carries a 44px hit box | Afo (2026-09-14): fix the six shared pieces the cockpit renders and align them under a shared anatomy; each hand-rolled its control (a 46px / 8px upload trigger, 42px / 16px date and audio buttons, 16px confidence segments, a 24px switch with no finger box), so a primitive update never reached them | codified | [frontend-design.md](../../rules/frontend-design.md) § Rules 18 and 19; [packages/admin/DESIGN.md](../../../packages/admin/DESIGN.md) § Cockpit M3 1a Invariants; [language.md](./language.md) § Button System |
| DL-032 | 2026-09-16 | A list header that pairs a status line with filters stays one row: the status line and its compact Refresh never truncate, and the filters give way first. Such filters use `NativeSelect density="condensed"` (10px inset, 28px chevron lane, ellipsis) sized to the selected option with `field-sizing: content` and a 64px floor; their height and hit area stay on the size scale (DL-023) | At 360px the Work Dashboard header wrapped its item count to three lines and pushed Month past the sheet edge in Spanish and Portuguese, because a native select sizes to its widest option. Afo rejected a two-row header and asked for condensed filters that take the ellipsis instead, since the open picker still shows every option | codified | [DESIGN.pwa.md](../../../packages/client/DESIGN.pwa.md) § Forms and Lists |
| DL-033 | 2026-09-18 | Installed-PWA bottom sheets move like native sheets. They slide up in one move and never rise above their resting edge: no overshoot, no surface fade, and an upward drag stops at rest. A drag from the grip or the shared header's title block under it moves the sheet one to one while the scrim lifts with it; the grip stays the visible affordance and the close button stays outside the gesture. Letting go closes the sheet after a downward flick or past a quarter of its height and otherwise settles it back, and an upward flick always keeps it open. A tap on the dimmed backdrop closes it like Escape and the close button, a press that began inside the sheet does not count, and `preventClose` holds every path. A close from rest accelerates out on the new `--spring-spatial-exit` token (`cubic-bezier(0.3, 0, 1, 1)`, 200ms); a flicked sheet already has speed and leaves on `--spring-spatial`, and both exits end within `--spring-spatial-duration`, which sheets time their unmount on | Afo asked on 18 Sep for a drag that feels native and an open animation that stops showing the page under the sheet, then chose backdrop dismissal, the header grab area, and a dedicated exit token. Measured at 390 x 844: the enter keyframe's 2% overshoot waypoint lifted the bottom-anchored sheet 4.7px (compact) to about 14px (full) off the screen edge for roughly 200ms; a 100px drag moved the sheet 0px, because a finished keyframe animation outranks an inline `transform` in the cascade, so the gesture now writes `translate`; backdrop taps had been inert since the scrim element began intercepting the click; the grip's hit area was 388 x 22px; and the decelerating exit covered up to 128px in its first frame, where the exit token starts at 8px and its largest step is 49px. A curve that starts from rest would stall a flicked sheet under the finger, which is why a flick keeps the decelerating token | codified | [DESIGN.pwa.md](../../../packages/client/DESIGN.pwa.md) § Layout philosophy · [language.md](./language.md) § Motion System |
| DL-034 | 2026-09-18 | Compact work and draft rows own one 88px height, and their media derives a square from that row height rather than setting an independent width and height | A fixed 88px thumbnail inside a minimum-height bordered row left a small gap at the bottom; Afo chose to preserve the row height and square media so future height changes move both together | codified | [DESIGN.pwa.md](../../../packages/client/DESIGN.pwa.md) § Forms and Lists |
| DL-035 | 2026-09-18 | Loading, error, and empty states in installed-PWA sheet tabs center within the same available content region; tab-specific headers and controls reserve their space above it | Afo asked that Drafts, Pending, and Completed align and that the same anatomy cover the other tabbed sheets, so switching tabs does not move an equivalent state to a different vertical zone | codified | [DESIGN.pwa.md](../../../packages/client/DESIGN.pwa.md) § Layout philosophy |
| DL-036 | 2026-09-19 | Installed-PWA sheet-tab empty states place their icon and title at one upper-middle anchor, independent of description or action height | Afo found the first optical offset still too low and noticed that centering the entire content stack shifted the icon and title whenever the description wrapped; the anchor now stays fixed and the copy grows below it | codified | [DESIGN.pwa.md](../../../packages/client/DESIGN.pwa.md) § Layout philosophy |
| DL-037 | 2026-09-19 | Filter Gardens offers only Name (A-Z) and Newest first, treats Name as the reset state, stacks scope and sort choices as full-width rows, and places domains two equal-width choices per row with an odd final choice spanning the full row | Default order duplicated Name (A-Z). Afo confirmed the original full-width selection rows read better than side-by-side cards and asked for domains to occupy two equal columns without leaving a half-empty final row; additional options scroll above the pinned action bar | codified | [DESIGN.pwa.md](../../../packages/client/DESIGN.pwa.md) § Layout philosophy |

## Related

- [SKILL.md](./SKILL.md) — activation routing; "check locked rows" step
- [interaction-patterns.md](./interaction-patterns.md) — primary codification target for admin interaction rules
- The qa-session skill — its close-phase decision lock gate is the primary append path
- [ARCHITECTURE.md](./ARCHITECTURE.md) — file map and version coupling
